# AGENTS.md — 课程实现工作手册

本仓库是一门 Obsidian 课程：**《分布式 AI 系统基础》（Foundations of Distributed AI Systems）**，面向「分布式训练/推理的网络与系统优化」方向（MLSys × 网络）的入门研究生，目标是扫清读论文的术语与背景障碍。

你（维护者侧的实现者 AI）的任务：**每次会话实现一节课**——把 `_课程设计/` 里的设计稿展开成一篇完整的课程笔记。课程架构与设计稿已完成，**不要改动课程结构**；你的价值在于把每节课写得清楚、准确、好读。

## 角色与优先级

本文是**维护者实现新课程**时的工作手册，不是面向外部贡献者的续写邀请。外部人类或 AI agent 提交 PR 时，必须先读 [CONTRIBUTING.md](CONTRIBUTING.md)；其中只接受已发布课程的可核验勘误、局部优化和补充，明确不接受新增或整篇生成 `Lxx` 课程。

若本文与 `CONTRIBUTING.md` 对外部 PR 的范围有冲突，外部 PR 以 `CONTRIBUTING.md` 为准；维护者按本文完成课程后仍须满足该贡献规则中的引用与校验要求。

## 仓库地图

| 文件/目录 | 作用 | 你的权限 |
|-----------|------|----------|
| `00 课程总览.md` | 课程定位、全部 70 课课程表（含标准文件名）、质量标准 | 只读 |
| `01 进度看板.md` | 任务清单、设计偏差记录、勘误区 | 打勾、登记 |
| `02 术语总表.md` | 全课程术语索引 | 追加条目 |
| `03 约定与符号.md` | 统一单位、符号、硬件参数、经验公式 | 只读（发现错误走勘误区） |
| `_课程设计/M*.md` | 每节课的设计稿（内容要点、必收术语、定量环节、延伸阅读）——设计稿标注「须核实」的公式/数字必须查证后再写 | 只读 |
| `_模板/课程模板.md` | 课程笔记的标准骨架与写作规范摘要 | 只读 |
| `M0 全景与方法/` … `M9 前沿专题/` | 课程正文所在目录（首次写该模块时自行创建目录） | 写入 |

## 标准工作流（每节课）

1. **领任务**：用户指定某课（如「写 L23」）则写该课；未指定则取 `01 进度看板.md` 中第一个未勾选的课。
2. **读上下文**（按此顺序，够用即止）：
   a. `00 课程总览.md` 的「质量标准」节 + 课程表中本课那一行；
   b. `_课程设计/` 中本课所在模块文件里**本课的设计稿**（这是内容契约）；
   c. `03 约定与符号.md`（数字与符号必须从这里取）；
   d. `_模板/课程模板.md`；
   e. 若非本模块第一课，速览**前一课**成稿，衔接语气并避免重复讲解。
3. **写课**：文件路径 = `M{x} {模块名}/L{xx} {课名}.md`，文件名**严格照抄**课程总览的课程表（wikilink 全库依赖它）。按模板骨架组织，展开设计稿的全部「内容要点」，覆盖全部「必收术语」，完成「定量环节」和指定图示。
4. **登记术语**：把本课术语按格式追加到 `02 术语总表.md` 对应字母节（已存在的条目不重复加）。
5. **收尾**：在 `01 进度看板.md` 给本课打勾并附日期；若偏离了设计稿，在「设计偏差记录」登记一行。本地 `git commit`，提交信息格式：`L23 Roofline模型`（不要 push，除非用户要求）。
6. **汇报**：向用户简述本课覆盖了哪些术语、算一算算了什么、有无偏差。

一次会话只写一节课；用户明确要求批量时才连写。

## 内容红线（必须遵守）

1. **不造假**：论文引用只用设计稿给出的 + 你能确信存在的；**禁止编造 arXiv 编号、年份、会议名、实验数字**。拿不准 → 定性表述或省略。论文题名以正式发表版为准（arXiv 与会议版可能不同），引用前核对或注明所引版本。
2. **数字统一**：硬件规格、记账常数一律引用 `03 约定与符号.md`（含其「其他常用量级」表），不要凭记忆另写（哪怕你认为自己记得）。两张表都没有且课程需要 → 用「约/量级/区间」表述，并在进度看板勘误区提议补录 03，**不得自造精确数字**。
3. **稠密算力口径**：MFU、峰值算力一律用 dense 值，不用 2:4 稀疏营销值。
4. **单位精确**：Gb/s 与 GB/s、单向与双向、FLOPs 与 FLOPS，一次都不能混。
5. **设计稿标注「须核实」的公式/数字**：查证后再写；无法查证就按设计稿给的定性方式处理。

## 写作风格

- **读者画像**：网络/系统方向研一学生，聪明但没学过 AI；读英文论文，日常说中文。
- 中文正文 + 英文术语（首现 **加粗英文**（中文注解））；术语译名先查 `02 术语总表`，冲突时以总表为准。
- 语气：师兄带读，不端着；每个抽象概念配一个类比或一个具体数字；宁可多一句大白话，不要多一个没解释的名词。
- 先动机后机制：每节先回答「没有它会怎样」，再讲「它怎么做」。
- 与已写课程互链（`[[Lxx 课名]]`），前置概念点链接而不是重讲（≤3 句带过）。
- Mermaid 图优先；表格常用于「对比 / 参数 / 术语卡片」；禁止引用不存在的图片。
- 实践课代码完整可运行、含无 GPU 路径（CPU/Apple Silicon）、注明依赖版本。

## 质量自检清单（提交前逐项过）

- [ ] 设计稿「必收术语」全部正面解释过，且列进术语卡片 + frontmatter `terms`
- [ ] 理论课「论文里的这段话」开场 + 结尾逐句回读，实践课「本次实践你要亲眼看到什么」开场 + 结尾回看目标，闭环成立
- [ ] 「算一算」给全算式与口径来源，可独立复算
- [ ] 自测 5–8 题含 ≥1 道计算题，答案折叠在 `> [!note]-` callout
- [ ] 所有 wikilink 文件名与课程总览一致（未写的课允许未解析链接）
- [ ] 数字全部对得上 `03 约定与符号.md`；无编造引用
- [ ] frontmatter 完整（lesson/module/title/status/date/terms/prereqs/tags）
- [ ] `02 术语总表` 已追加、`01 进度看板` 已打勾、已 commit

## Obsidian 语法要点

- 内链用 wikilink：`[[L21 GPU内存体系]]`；带显示文本 `[[L21 GPU内存体系|上一课]]`。外部 URL 才用 `[text](url)`。
- Callout：`> [!abstract]` 速览、`> [!quote]` 论文引文、`> [!example]` 算一算、`> [!warning]` 误区、`> [!tip]` 直觉、`> [!note]-` 折叠答案（`-` 表示默认折叠）。
- 数学：`$...$` / `$$...$$`（LaTeX）。表格单元格内的 `|` 要写成 `\|`。
- frontmatter 为 YAML；wikilink 出现在 frontmatter 里必须加引号。
- 高亮 `==文本==`；不用 HTML 标签。
