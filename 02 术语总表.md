---
title: 术语总表
aliases:
  - Glossary
  - 术语表
tags:
  - course/meta
---

# 术语总表（Glossary）

> **用法**：读论文遇到不认识的词，`Ctrl/Cmd+F` 搜英文原词，一行速览含义，点链接进对应课程精读。
> **维护规则**（写课的同学/AI 必读）：每完成一节课，把该课「必收术语」按首字母追加到对应小节，格式：
> `- **英文术语**（中文译名）：一句话解释。→ [[Lxx 课程名]]`
> 同一术语多课涉及时，链接指向**主讲**那一课；已存在的条目不要重复添加，可补充链接。

## 符号与数字

- **$/M tokens**（每百万 token 成本）：基础设施成本除以实际交付的百万 input 或 output tokens；必须注明阶段、SLO 与资源口径。→ [[L61 推理服务框架与集群]]

- **3D parallelism**（三维并行）：经典的 DP × TP × PP 混合并行组合；阅读具体资料时仍应核对各轴定义。→ [[L47 混合并行组装]]
- **4D parallelism**（四维并行）：在三维组合上再加入 CP 等第四轴；名称本身不保证轴集合唯一。→ [[L47 混合并行组装]]

- **1F1B**（one-forward-one-backward，一前一后）：流水线 warmup 后尽可能交替执行一个 forward 与一个 backward，以降低在飞 activation 峰值。→ [[L44 流水线并行]]

- **α-β model**（$\alpha$-$\beta$ 代价模型）：用每消息固定代价 $\alpha$ 与每字节代价 $\beta$ 估算通信时间。→ [[L37 通信算法与代价模型]]

- **2D ring**（二维环）：把域内和域间当作两个环维度组合 collective 的算法。→ [[L37 通信算法与代价模型]]

- **3FS**（Fire-Flyer File System）：DeepSeek 面向 AI workload、结合 NVMe 与 RDMA 的开源分布式文件系统。→ [[L34 存储与数据供给]]

- **6ND**（训练计算量估算式）：训练一个 $N$ 参数模型过 $D$ 个 token 约需 $6ND$ FLOPs。→ [[L16 Scaling Law与算力账]]

## A

- **agent**（智能体）：在循环中选择生成、调用工具、读取结果或结束的应用控制实体。→ [[L70 选修-多模态与新负载]]
- **agentic workload**（智能体式工作负载）：LLM 段与工具段交替、会话长且 idle/长度长尾明显的 workload。→ [[L70 选修-多模态与新负载]]
- **ANN**（Approximate Nearest Neighbor，近似最近邻）：以可控召回损失换低时延和大规模的相似向量搜索。→ [[L70 选修-多模态与新负载]]

- **analytical simulation**（解析式仿真）：用公式或粗粒度事件近似 compute、memory 与 communication 的仿真。→ [[L67 测量仿真与评测]]
- **ASTRA-sim**（ASTRA-sim 分布式 AI 仿真器）：联合建模 workload、collective、compute/memory/network 的 distributed AI simulator。→ [[L67 测量仿真与评测]]

- **active-active**（双活 / 多活）：多个副本或地域平时同时接流量并可互相接管。→ [[L65 推理服务生存性]]
- **active-standby**（主备）：主路径服务、备用路径保持不同热度并在故障时接管。→ [[L65 推理服务生存性]]
- **anycast**（任播）：多个站点宣告同一 IP 前缀，由网络路由选择可达或较近入口。→ [[L65 推理服务生存性]]
- **availability**（可用性）：在定义窗口内服务可用或合格请求成功的比例，必须声明 time-based 或 request-based 口径。→ [[L65 推理服务生存性]]

- **asynchronous training**（异步训练）：worker 不经逐轮全局 barrier 即读取参数并提交更新，减少等待但引入参数陈旧。→ [[L63 跨域与跨集群训练]]

- **API gateway**（API 网关）：负责协议入口、鉴权、限流、配额、错误语义与 request ID 的服务边界。→ [[L61 推理服务框架与集群]]
- **autoscaling**（自动扩缩）：依据负载、容量和 SLO 信号调整 ready replicas 数量。→ [[L61 推理服务框架与集群]]；弹性资源池与昼夜潮汐见 [[L66 弹性算力与资源效率]]

- **acceptance rate**（接受率）：draft token 通过 target 验证的平均概率，常记为 $\alpha$。→ [[L59 投机解码]]

- **AWQ**（激活感知权重量化）：用 activation 统计识别并缩放保护重要 weights 的低 bit weight-only PTQ。→ [[L58 量化推理]]

- **admission control**（准入控制）：在过载前依据容量与 SLO 余量决定请求进入实例、排队、转发还是被拒绝。→ [[L57 连续批处理与调度]]

- **arrival rate ($\lambda$)**（到达率）：单位时间进入 serving 系统的请求数。→ [[L55 推理性能模型]]

- **actor**（策略模型 / 被训练模型）：在 RL 后训练中既生成 rollout，又由 optimizer 更新的 policy。→ [[L54 RL后训练系统]]
- **async RL**（异步 RL）：让 rollout 与训练通过队列并行推进、不再逐轮全局 barrier 的执行方式。→ [[L54 RL后训练系统]]

- **AMP**（Automatic Mixed Precision，自动混合精度）：框架按算子规则自动选择低精度或高精度执行路径的机制。→ [[L49 混合精度与FP8训练]]
- **amax**（绝对值最大值）：tensor 或量化分组内的 $\max_i|x_i|$，常用于估计 scale 与监控溢出风险。→ [[L49 混合精度与FP8训练]]

- **Alpa**（Alpa 自动并行系统）：以 intra-/inter-operator 两层空间联合搜索 operator 与 pipeline parallelism 的系统。→ [[L47 混合并行组装]]
- **auto-parallelism**（自动并行）：自动搜索或推导 sharding、pipeline 与 device mapping 并生成执行计划的方法。→ [[L47 混合并行组装]]

- **activation communication**（activation 通信）：TP 在层内传输 activation 或其梯度，消息规模主要随 $bSd$ 增长。→ [[L43 张量并行]]
- **attention head sharding**（注意力头切分）：把完整 attention heads 分给不同 TP ranks 本地计算，对应 QKV column parallel。→ [[L43 张量并行]]

- **activation scaling with $S$**（activation 随序列长度的增长关系）：hidden/QKV 等主要 activation 随 $S$ 线性增长，朴素 attention score 随 $S^2$ 增长。→ [[L45 序列与上下文并行]]

- **all-to-all head switching**（all-to-all 头维切换）：attention 前后用 all-to-all 在 sequence-sharded 与 head-sharded layout 之间转换。→ [[L45 序列与上下文并行]]

- **all-gather on demand**（按需 all-gather）：只在某参数单元即将计算时临时还原完整参数，用后释放非本地部分。→ [[L42 ZeRO与FSDP]]

- **asynchronous SGD**（异步 SGD）：workers 不经逐轮全局 barrier 就读取参数并提交梯度，可能以系统等待换取参数陈旧。→ [[L41 数据并行与DDP]]
- **ASP**（Asynchronous Parallel，异步并行）：不设置逐轮全局 barrier、允许 workers 独立推进的一致性语义。→ [[L41 数据并行与DDP]]

- **activation checkpointing**（激活检查点）：只保留部分 forward 中间结果，在 backward 时重算其余结果，以计算换显存。→ [[L40 训练显存全解剖]]；机制与代价见 [[L50 显存优化技术]]
- **activation recomputation**（激活重计算）：forward 少存中间 activation，backward 时重跑局部 forward 补回。→ [[L50 显存优化技术]]
- **activation memory**（激活显存）：为 backward 保留的 forward 中间结果及相关元数据所占显存。→ [[L40 训练显存全解剖]]

- **algorithm bandwidth (algbw)**（算法带宽）：用户 tensor 大小除以 collective 完成时间，即 $n/T$。→ [[L37 通信算法与代价模型]]

- **all-gather**（全汇集）：各 rank 的分片按 rank 顺序拼成全量，并复制给组内所有 ranks。→ [[L36 集合通信原语]]

- **allocation vs utilization**（分配率与利用率辨析）：资源已经分给作业，不等于资源正忙，更不等于正在产生同等比例的模型有效 FLOPs/goodput。→ [[L35 GPU集群调度]]
- **all-or-nothing**（全有或全无）：固定规模作业的全部所需资源同时可用才启动，否则整体等待。→ [[L35 GPU集群调度]]

- **async checkpoint**（异步检查点）：快速 staging 训练状态后恢复计算，由后台任务完成耐久写入。→ [[L34 存储与数据供给]]

- **ATP**（多租户在网聚合系统）：面向 multi-rack、multi-job training 的动态、best-effort switch aggregation 服务。→ [[L33 在网计算与智能网卡]]

- **adaptive routing**（自适应路由）：根据实时 queue、link utilization 或 congestion 状态，在多条合法 path 中动态选择较轻载路径。→ [[L32 路由与负载均衡]]

- **aggregation**（汇聚层）：三层 fat-tree 中连接 pod 内 edge/leaf、并上联 core 的中间交换层。→ [[L28 数据中心网络基础]]

- **availability zone**（可用区）：云 region 内相互隔离的部署位置，可包含一个或多个物理数据中心。→ [[L27 走进AI数据中心]]

- **Ascend**（昇腾）：华为面向训练与推理的 AI 加速器与系统产品族。→ [[L26 加速器生态与软件栈]]
- **ATen**（PyTorch 张量算子核心层）：定义 PyTorch operator schema，并把上层调用接到 dispatcher 与 backend 实现。→ [[L26 加速器生态与软件栈]]

- **accumulation precision**（累加精度）：乘加中保存部分和所用的数值格式，常高于乘法输入精度以减少求和舍入误差。→ [[L24 数值格式]]

- **arithmetic intensity (operational intensity)**（算术强度 / 操作强度）：每跨选定内存边界搬 1 Byte 所完成的 FLOPs；使用时必须注明 HBM、cache 或其他边界口径。→ [[L23 Roofline模型]]

- **arithmetic throughput**（算术吞吐）：程序实际每秒完成的算术操作速率，常以 FLOPS 表示。→ [[L22 算力度量与MFU]]
- **attention sink**（注意力汇聚点）：模型对最初少数 token 赋予异常高 attention、影响窗口缓存裁剪的现象。→ [[L18 注意力变体与长上下文]]

- **active parameters**（激活参数）：某个 token 的前向路径实际使用的参数量，主要决定稀疏模型每 token 的计算量。→ [[L17 MoE混合专家]]
- **all-to-all**（全对全通信）：每个 rank 都可能向所有 rank 发送不同数据；MoE 中用于把 token dispatch 到 expert owner、再 combine 回来源。→ [[L17 MoE混合专家]]；原语语义见 [[L36 集合通信原语]]
- **auxiliary loss**（辅助损失）：促使 MoE router 在各 expert 间均衡分配 token 的额外训练目标。→ [[L17 MoE混合专家]]
- **auxiliary-loss-free**（无辅助损失）：通过调整 router 的 expert bias 等方式平衡负载、而不把辅助项直接加入主 loss 的策略。→ [[L17 MoE混合专家]]

- **annealing**（退火）：预训练收尾阶段用更温和的学习率或更高质量数据做质量收束。→ [[L14 预训练]]
- **autoregressive**（自回归）：用序列已有 token 预测下一个 token，再把输出拼回输入继续预测。→ [[L13 自回归生成与KV缓存]]
- **alignment**（对齐）：让模型输出更符合人类意图、偏好和安全边界的后训练过程。→ [[L15 后训练与对齐]]
- **attention**（注意力）：让一个位置按与其他位置的匹配程度，动态聚合上下文信息的机制。→ [[L11 注意力机制]]
- **attention head**（注意力头）：MHA 中有自己 QKV 投影与权重图的一个子空间。→ [[L11 注意力机制]]
- **attention score**（注意力分数）：softmax 前由 query 与 key 的点积得到的匹配分数。→ [[L11 注意力机制]]
- **attention weight**（注意力权重）：score 经缩放、mask 和 softmax 后得到的非负归一化权重。→ [[L11 注意力机制]]
- **Adam**（自适应矩估计优化器）：用 gradient 的一阶矩估计方向、二阶矩估计尺度并自适应更新参数。→ [[L06 优化器]]
- **AdamW**（解耦权重衰减的 Adam）：把 weight decay 与 Adam 的自适应 gradient 更新分开处理。→ [[L06 优化器]]
- **ablation study**（消融实验）：保持其他条件尽量不变，移除或替换一个机制以定位收益来源。→ [[L02 如何读系统论文]]
- **accelerator**（加速器）：针对特定高并行计算提供高效率的处理器，GPU 是常见代表。→ [[L01 全景图]]
- **adapter**（适配器）：可独立保存、加载或切换的小规模任务特定参数模块。→ [[L15 后训练与对齐]]
- **activation**（中间激活值）：forward pass 产生且可能被 backward 使用的中间 tensor；不同于 activation function。→ [[L05 反向传播与梯度]]
- **activation function**（激活函数）：在线性变换之间加入非线性，使多层网络不能被合并成一个线性层。→ [[L04 神经网络与前向传播]]
- **AI Infra**（AI 基础设施）：支撑 AI 模型训练、部署与服务的完整软硬件技术栈。→ [[L01 全景图]]
- **all-reduce**（全规约）：每个 rank 出一份数据，规约（如求和）后所有 rank 得到完整结果；数据并行同步梯度的核心原语。→ [[L36 集合通信原语]]
- **Amdahl's Law**（阿姆达尔定律）：用不可并行部分的比例刻画并行加速上限的定律。→ [[L02 如何读系统论文]]
- **artifact**（研究制品）：与论文配套的代码、数据、配置、脚本和复现说明等材料。→ [[L02 如何读系统论文]]
- **autograd**（自动微分机制）：深度学习框架记录运算并自动反向计算梯度的功能。→ [[L05 反向传播与梯度]]
- **automatic differentiation**（自动微分）：由基本算子的导数规则和计算依赖自动构造整体导数计算。→ [[L05 反向传播与梯度]]

## B

- **Bamboo**（Bamboo 弹性训练系统）：用 pipeline bubble 中的冗余计算承受频繁抢占。→ [[L66 弹性算力与资源效率]]

- **brownout**（棕色降级）：过载时暂时关闭可选组件，使 essential path 继续工作的设计思想。→ [[L65 推理服务生存性]]

- **block**（KV 块）：容纳固定数量 token 的 K/V、作为 KV cache 分配和共享粒度的单元；不同于 GPU thread block。→ [[L56 KV缓存与PagedAttention]]
- **block table**（块表）：保存请求 logical block 到 physical block 映射的元数据表。→ [[L56 KV缓存与PagedAttention]]

- **batch effect**（批处理效应）：让多个当前 token 复用一次权重读取，以提高 arithmetic intensity 与 aggregate throughput。→ [[L55 推理性能模型]]
- **burstiness**（突发性）：请求在短时间成团到达、瞬时负载显著偏离长期平均的性质。→ [[L55 推理性能模型]]

- **blast radius**（故障爆炸半径）：一次故障波及的资源、作业、同步组或服务范围。→ [[L53 大规模训练可靠性]]

- **bitwise reproducibility**（逐 bit 可复现性）：两次运行对应结果的每一 bit 都一致的严格复现要求。→ [[L49 混合精度与FP8训练]]

- **bubble ratio**（气泡率）：理想均衡同步流水中，填充与排空造成的空闲时间占总流水时间的比例。→ [[L44 流水线并行]]

- **BSP**（Bulk Synchronous Parallel，批量同步并行）：每个逻辑轮次设置全局 barrier，等所有参与者完成后再推进。→ [[L41 数据并行与DDP]]

- **bandwidth term**（带宽项）：随传输字节数线性增长、主要由 $n\beta$ 决定的通信时间部分。→ [[L37 通信算法与代价模型]]
- **bandwidth-optimal**（带宽最优）：算法的字节传输项达到给定通信模型下的下界。→ [[L37 通信算法与代价模型]]
- **bus bandwidth (busbw)**（总线折算带宽）：algbw 乘原语流量系数得到的硬件利用折算值。→ [[L37 通信算法与代价模型]]

- **barrier**（屏障同步）：所有组成员到齐后才允许各自继续执行的同步 collective。→ [[L36 集合通信原语]]
- **broadcast**（广播）：把 root 的完整数据复制到通信组内所有 ranks。→ [[L36 集合通信原语]]

- **backfill**（回填）：不推迟受保护高优先级作业预计启动时间时，用较小作业填资源空洞。→ [[L35 GPU集群调度]]
- **bin packing**（装箱）：把不同资源需求的作业装入有限容量节点/域，并尽量减少浪费的问题抽象。→ [[L35 GPU集群调度]]

- **burst buffer**（突发缓冲层）：用 DRAM、NVMe 或专用快速层吸收短时 I/O 洪峰，再后台排空到耐久存储。→ [[L34 存储与数据供给]]

- **BlueField**（NVIDIA DPU 产品族）：组合 ConnectX networking、embedded Arm subsystem 与 DOCA/Linux software stack 的 DPU。→ [[L33 在网计算与智能网卡]]

- **buffer/queue depth**（缓冲区 / 队列深度）：可暂存等待服务数据的容量，能吸收 burst 但不能提高链路服务率。→ [[L30 无损网络与拥塞控制]]
- **bufferbloat**（缓冲膨胀）：过深 queue 减少 drop，却制造很大的排队与尾时延。→ [[L30 无损网络与拥塞控制]]

- **bisection bandwidth**（对分带宽）：把端点等分时，所有等分切面中最小的跨切面容量。→ [[L28 数据中心网络基础]]
- **bursty traffic**（突发流量）：强度在时间上呈明显 on/off、短时冲高的流量。→ [[L28 数据中心网络基础]]

- **back-end network**（后端计算网）：承载 GPU/accelerator 间 collective 与分布式计算流量的高性能网络。→ [[L27 走进AI数据中心]]
- **BMC**（基板管理控制器）：独立于主机 OS 的硬件管理控制器，可用于监控、远程控制和故障救援。→ [[L27 走进AI数据中心]]

- **bidirectional bandwidth**（双向合计带宽）：把两个相反方向可同时传输的带宽相加；不能直接当作单向搬运带宽。→ [[L25 节点内互联]]

- **BF16**（Brain Floating Point 16）：1s+8e+7m 的 16-bit 浮点格式，以较粗精度换取 FP32 量级的动态范围。→ [[L24 数值格式]]

- **bandwidth-bound**（带宽受限）：性能主要受某条数据通路的可持续带宽限制。→ [[L23 Roofline模型]]

- **base model**（基础模型）：完成预训练、主要会续写，尚未经过指令对齐的模型。→ [[L14 预训练]]
- **benchmark**（基准评测）：固定数据、提示与评分协议的能力测量。→ [[L14 预训练]]
- **bidirectional attention**（双向注意力）：允许每个位置同时聚合左右两边上下文的 attention。→ [[L11 注意力机制]]
- **backward pass**（反向传播）：从 loss 沿计算图逆序应用 chain rule、计算各变量 gradient 的过程。→ [[L05 反向传播与梯度]]
- **baseline**（基线）：用于判断新方案收益的已有方法、默认配置或对照版本。→ [[L02 如何读系统论文]]
- **BERT**（BERT 预训练模型）：以 Transformer encoder 为主体、使用 masked language model 目标预训练的模型家族。→ [[L08 CNN与RNN简史]]
- **BOS**（序列起始 token）：beginning-of-sequence 的缩写，标记一段序列的开始。→ [[L10 Token与嵌入]]
- **BPE**（字节对编码）：通过反复合并语料中高频相邻片段来构造子词词表的常见方法。→ [[L10 Token与嵌入]]
- **batch dimension**（批次维）：标记一次并行处理多少个 sample 的 tensor 维度，通常记为 $B$。→ [[L04 神经网络与前向传播]]
- **batch size**（批大小）：一个 batch 包含的样本数或 token 数，必须说明局部或全局口径。→ [[L06 优化器]]

## C

- **CLIP**（Contrastive Language-Image Pre-training，对比式图文预训练）：用成对图文的对比目标学习可比较的图像与文本表示。→ [[L70 选修-多模态与新负载]]

- **capacity headroom**（容量余量）：ready capacity 中尚未被当前负载使用、可承接 burst 或 failover 的比例。→ [[L65 推理服务生存性]]
- **chaos engineering**（混沌工程）：在受控、可回滚条件下持续验证系统对故障假设的工程方法。→ [[L65 推理服务生存性]]

- **computation offloading**（计算卸载）：把本地计算连同必要输入迁到远端执行，并把结果返回本地的机制。→ [[L64 边缘与端云协同]]

- **communication frequency**（通信频率）：训练推进过程中执行跨 worker/domain 同步的频度，local SGD 中约为每 $H$ steps 一次。→ [[L63 跨域与跨集群训练]]
- **cross-region**（跨地域）：通信或计算跨越云或基础设施定义的 region 边界。→ [[L63 跨域与跨集群训练]]

- **cache-aware routing**（缓存感知路由）：联合 prefix/KV overlap 与 active load 选择 serving 实例。→ [[L61 推理服务框架与集群]]
- **cold start**（冷启动）：新实例从不存在或未加载到 ready 的完整等待，包含权重加载、编译、预热等阶段。→ [[L61 推理服务框架与集群]]

- **calibration**（校准）：用代表性真实数据估计并调整模型参数；PTQ 中校准 scale/范围，仿真中校准带宽、时延或执行时间。→ [[L58 量化推理]]；仿真语境见 [[L67 测量仿真与评测]]

- **continuous batching**（连续批处理）：在相邻 model iterations 之间移除完成请求并补入 waiting 请求。→ [[L57 连续批处理与调度]]
- **chunked prefill**（分块预填充）：把长 prompt 的 prefill 分成多个 chunks，与 decode 分步混合调度。→ [[L57 连续批处理与调度]]

- **cache hit rate**（缓存命中率）：请求或 token-work 中从已有 KV 获得复用的比例，使用时必须注明统计口径。→ [[L56 KV缓存与PagedAttention]]
- **copy-on-write**（写时复制）：多分支先共享物理 KV blocks，某分支写入时才复制需要修改的块。→ [[L56 KV缓存与PagedAttention]]

- **compute-bound prefill**（计算受限的 prefill）：prompt tokens 并行形成大 GEMM，性能典型地先受算术吞吐限制。→ [[L55 推理性能模型]]
- **concurrency**（并发数）：同时处于指定系统边界内的 in-flight 请求数。→ [[L55 推理性能模型]]

- **colocated architecture**（共置架构）：让训练、生成或多个 RL roles 在同一 GPU pool 上分时复用资源。→ [[L54 RL后训练系统]]
- **critic**（价值模型）：PPO 中估计状态价值、辅助 advantage 计算并参与训练的模型。→ [[L54 RL后训练系统]]

- **checkpointing**（检查点机制）：周期性保存一致训练状态，以便失败后从已提交版本恢复。→ [[L53 大规模训练可靠性]]
- **checkpoint interval**（检查点间隔）：相邻 checkpoint 之间推进的有效计算时间。→ [[L53 大规模训练可靠性]]

- **Chrome trace**（Chrome trace 事件文件）：用统一时间事件格式保存 op、kernel 与区间，可在 trace viewer 中按时间浏览。→ [[L52 性能剖析与MFU核算]]
- **clock synchronization**（时钟同步）：让不同主机时间戳处于可比较基准，并用逻辑标记校正剩余偏差。→ [[L52 性能剖析与MFU核算]]

- **cost model**（成本模型）：估算候选 parallel plan 的计算、通信、显存与重叠代价，供搜索和比较。→ [[L47 混合并行组装]]

- **column-parallel linear**（列并行线性层）：沿 weight 输出维切分，各 rank 得到最终输出的不同列片。→ [[L43 张量并行]]

- **causal load imbalance**（因果 attention 负载不均）：连续序列切分下，晚段 Q 可见历史更多，导致各 rank 的有效 tile 数与轮次时长不均。→ [[L45 序列与上下文并行]]

- **context parallelism (CP)**（上下文并行）：把单个长样本的 context 沿序列维切给多个 ranks，并分布式完成全局 attention。→ [[L45 序列与上下文并行]]

- **cooldown**（流水线排空段）：停止注入新 forward、完成剩余 backward 的阶段。→ [[L44 流水线并行]]

- **communication volume overhead**（通信量额外开销）：相对基线方案增加的数据搬运量，不等同于墙钟时延或暴露通信时间。→ [[L42 ZeRO与FSDP]]

- **communication-computation overlap**（通信-计算重叠）：在网络处理已就绪梯度时继续执行其余 backward，以隐藏部分通信时间。→ [[L41 数据并行与DDP]]

- **channel**（NCCL 通信通道）：拥有独立逻辑连接和 buffer、可并行推进 chunks 的通信流水线。→ [[L38 NCCL解剖]]
- **chunking**（分块）：把大 tensor 拆成多个通信块，以支持并行、流水和负载均衡。→ [[L37 通信算法与代价模型]]

- **collective communication**（集合通信）：一个已知进程集合共同完成的数据移动、规约或同步操作。→ [[L36 集合通信原语]]
- **communicator**（通信域）：MPI 中界定通信参与者、顺序和消息上下文的对象。→ [[L36 集合通信原语]]

- **checkpoint size**（检查点体积）：一份可恢复训练状态的总字节数；本课 Adam 混合精度口径约为 $14\ \text{B/参数}$。→ [[L34 存储与数据供给]]

- **CNP**（Congestion Notification Packet，拥塞通知包）：receiver/NIC 收到 CE-marked RoCE packet 后发给 sender 的降速反馈。→ [[L30 无损网络与拥塞控制]]
- **congestion spreading**（拥塞扩散）：PFC 反压从热点逐跳传播到远处 link、queue 与 sender。→ [[L30 无损网络与拥塞控制]]

- **ConnectX**（NVIDIA 网络适配器产品族）：可提供 InfiniBand 或 Ethernet/RoCE 能力的适配器系列，产品名不等于协议。→ [[L29 RDMA原理]]
- **CQ/CQE**（完成队列 / 完成项）：NIC 用 CQE 向 CQ 报告操作成功、失败及对应请求等信息。→ [[L29 RDMA原理]]
- **CPU offload**（CPU 卸载）：把分包、顺序、ACK、重传和权限检查等传输工作下沉到 NIC。→ [[L29 RDMA原理]]
- **credit-based flow control**（基于信用的流控）：下游用接收 buffer credit 限制上游发送，抑制 buffer overflow。→ [[L29 RDMA原理]]

- **Clos network**（Clos 多级互联网络）：用多个较小交换单元和多条路径拼出可扩展高带宽 fabric。→ [[L28 数据中心网络基础]]
- **core**（核心层）：三层 fat-tree 顶层，负责连接不同 pod 的 aggregation。→ [[L28 数据中心网络基础]]

- **cold plate**（冷板）：贴合 CPU/GPU、让冷却液通过内部流道吸热的换热部件。→ [[L27 走进AI数据中心]]
- **colocation**（托管机房 / 工作负载混部，按语境）：设施语境指客户把设备部署在第三方机房；调度语境指不同 workloads 共用资源池、节点或 GPU。→ [[L27 走进AI数据中心]]、[[L66 弹性算力与资源效率]]

- **carbon-aware scheduling**（碳感知调度）：在 deadline、数据位置和 SLO 约束内，依据时空碳强度安排可迁移或可延迟 workload。→ [[L66 弹性算力与资源效率]]

- **CANN**（昇腾异构计算架构）：连接框架与 Ascend 硬件的算子、编译器、runtime、通信与工具软件栈。→ [[L26 加速器生态与软件栈]]
- **CUDA**（NVIDIA GPU 编程平台）：包含 programming model、compiler、runtime/driver、libraries 与工具的完整平台。→ [[L26 加速器生态与软件栈]]
- **CUDA kernel**（CUDA 内核）：在 GPU 上执行的函数；NCCL collective 的数据移动与规约也常由它推进。→ [[L38 NCCL解剖]]
- **CUDA Graph**（CUDA 图）：捕获一串 CUDA 工作及依赖，实例化后低开销重复 replay。→ [[L26 加速器生态与软件栈]]；launch 开销账见 [[L51 算子优化与FlashAttention]]
- **cuBLAS**（CUDA 线性代数库）：NVIDIA 在 CUDA 上提供的 BLAS/GEMM 优化库。→ [[L26 加速器生态与软件栈]]
- **cuDNN**（CUDA 深度学习 primitives 库）：为 convolution、attention、normalization 等常见 DNN 操作提供优化实现。→ [[L26 加速器生态与软件栈]]
- **CUTLASS**（CUDA 矩阵计算模板/DSL）：供 kernel/library 作者构建和特化高性能 GEMM 及相关计算的组件集合。→ [[L26 加速器生态与软件栈]]

- **CPU affinity**（CPU 亲和性）：把进程或线程绑定到指定 CPU cores，以匹配 GPU、NIC 与内存的 NUMA 拓扑。→ [[L25 节点内互联]]

- **compute-bound**（计算受限）：Roofline 中算力屋顶比带宽上限更低、性能首先受算术吞吐限制的区域。→ [[L23 Roofline模型]]

- **caching allocator**（缓存分配器）：保留已申请的 GPU 内存块供后续 tensor 复用的分配器；PyTorch 的 `reserved` 统计会包含这些缓存。→ [[L21 GPU内存体系]]；内存池治理见 [[L50 显存优化技术]]
- **checkpoint boundary**（重计算边界）：forward 中被保留下来、用于启动后续局部重算的输入或状态。→ [[L50 显存优化技术]]
- **capacity factor**（容量因子）：相对平均路由量设置每个 expert token 接待上限的倍率，常写作 $c$。→ [[L17 MoE混合专家]]

- **CXL**（Compute Express Link）：面向 CPU、内存和加速器协作的高速互联协议族。→ [[L21 GPU内存体系]]

- **CUDA core**（CUDA 核心）：GPU 中执行通用标量/向量浮点与整数运算的执行单元。→ [[L20 GPU体系结构入门]]
- **CUDA stream**（CUDA 流）：保持操作顺序的 GPU 命令队列；不同 stream 在资源允许时可能重叠。→ [[L20 GPU体系结构入门]]

- **CommonCrawl**（Common Crawl 网页档案）：持续抓取公开网页的开放档案，常作为 web-scale 语料原料。→ [[L14 预训练]]
- **causal attention**（因果注意力）：每个位置只能看自己和更早位置的 attention。→ [[L11 注意力机制]]
- **causal mask**（因果掩码）：在 softmax 前将未来位置的 score 设为 $-\infty$，使其权重变为 0。→ [[L11 注意力机制]]
- **catastrophic forgetting**（灾难性遗忘）：新任务训练导致模型旧能力明显退化的现象。→ [[L15 后训练与对齐]]
- **chain-of-thought (CoT)**（思维链）：最终答案前的中间推理步骤序列。→ [[L15 后训练与对齐]]
- **cross-attention**（交叉注意力）：Q 来自一段序列，K、V 来自另一段序列的 attention。→ [[L11 注意力机制]]
- **camera-ready**（出版终稿）：论文录用后按审稿意见和出版格式提交的最终版本。→ [[L02 如何读系统论文]]
- **CDF**（累积分布函数）：给出随机变量不超过某个取值的累计概率。→ [[L02 如何读系统论文]]
- **chain rule**（链式法则）：复合函数求导时，把计算路径上的局部导数组合起来。→ [[L05 反向传播与梯度]]
- **chat template**（聊天模板）：将角色、消息边界和正文转换为模型期望 token 序列的格式规则。→ [[L10 Token与嵌入]]
- **checkpoint**（检查点）：为恢复或续训而持久化的权重及相关训练状态快照。→ [[L01 全景图]]
- **classification**（分类）：从有限类别集合中预测一个或多个类别。→ [[L03 机器学习速成]]
- **CNN**（卷积神经网络）：用局部连接与参数共享处理网格状数据、尤其是图像的神经网络。→ [[L08 CNN与RNN简史]]
- **computational graph**（计算图）：用节点和依赖边表示 tensor 如何经一系列运算得到 loss。→ [[L05 反向传播与梯度]]
- **compute budget**（算力预算）：一次训练允许消费的总计算量，本文记作 $C$。→ [[L16 Scaling Law与算力账]]
- **compute-optimal**（算力最优）：在固定 compute budget 下使目标 loss 最低的模型参数量与 token 配比。→ [[L16 Scaling Law与算力账]]
- **Chinchilla**（Chinchilla 配比）：Hoffmann 等提出的计算最优训练研究及其经验配比，常记为 $D/N\approx20$。→ [[L16 Scaling Law与算力账]]
- **context length**（上下文长度）：用 token 数描述的上下文窗口长度，常与 context window 近义。→ [[L10 Token与嵌入]]
- **context window**（上下文窗口）：模型一次可处理的 token 上限。→ [[L10 Token与嵌入]]
- **convergence**（收敛）：优化过程逐步进入 loss 变化较小、参数更新趋稳的状态。→ [[L06 优化器]]
- **contamination**（评测污染）：训练语料含有评测题、答案或近似副本，导致 benchmark 分数虚高。→ [[L14 预训练]]
- **convolution**（卷积）：用同一组小窗口权重滑过输入、在各位置计算局部特征的操作。→ [[L08 CNN与RNN简史]]
- **corpus**（语料库）：供模型训练或评测的一批文本、代码及其元数据的集合。→ [[L14 预训练]]
- **cosine decay**（余弦衰减）：warmup 后按余弦形状逐渐降低 learning rate 的调度。→ [[L06 优化器]]
- **config.json**（模型配置文件）：记录 hidden size、层数、头数、词表等结构超参数的 JSON 文件，与 model card 一起是读模型的第一站。→ [[L12 Transformer全解剖]]
- **cross-entropy loss**（交叉熵损失）：分类中以 $-\log p_{\text{正确}}$ 惩罚对正确类别信心不足的 loss。→ [[L05 反向传播与梯度]]

## D

- **denoising step**（去噪步）：根据当前完整 latent、时间步与条件预测并更新下一状态的一次 forward。→ [[L70 选修-多模态与新负载]]
- **diffusion model**（扩散模型）：从噪声状态经过多步去噪生成样本的模型家族。→ [[L70 选修-多模态与新负载]]
- **DiT**（Diffusion Transformer，扩散 Transformer）：用 Transformer 处理 latent patches 的扩散模型骨干。→ [[L70 选修-多模态与新负载]]

- **digital twin**（数字孪生）：持续吸收真实 topology、telemetry 与 workload 状态，用于预测变更或故障影响的运营模型。→ [[L67 测量仿真与评测]]

- **diurnal pattern**（昼夜周期）：工作负载随一天时间呈重复峰谷的到达模式。→ [[L66 弹性算力与资源效率]]

- **degraded service**（降级服务）：故障或过载下实际交付的有限功能或质量服务模式。→ [[L65 推理服务生存性]]
- **downtime**（不可用时间）：time-based availability 窗口内服务不可用的累计时长。→ [[L65 推理服务生存性]]

- **decentralized training**（去中心化训练）：不依赖单个中心 server、由 peers 在通信图上协同优化的训练方式。→ [[L63 跨域与跨集群训练]]
- **dedicated line**（专线）：为站点间提供约定容量和服务保障的 WAN 连接，但仍可能共享、抖动或故障。→ [[L63 跨域与跨集群训练]]
- **DiLoCo**（Distributed Low-Communication，分布式低通信训练）：用多步 inner optimization 与低频 outer optimization 训练弱互联模型副本的方法。→ [[L63 跨域与跨集群训练]]

- **Dynamo**（Dynamo 分布式推理框架）：NVIDIA 用于连接 frontend、router、workers、KV 路由与多种 engine backend 的 serving 框架。→ [[L61 推理服务框架与集群]]

- **DistServe**（DistServe 分离式 serving 系统）：联合搜索 P/D parallelism、实例数与 placement，以最大化满足 TTFT/TPOT SLO 的 per-GPU goodput。→ [[L60 分布式推理与PD分离]]

- **draft model**（草稿模型）：低成本生成候选 token 的模型或 proposer，不拥有最终输出决定权。→ [[L59 投机解码]]

- **dequantization**（反量化）：用 scale/zero point 把低精度码恢复成近似高精度值或计算所需表示。→ [[L58 量化推理]]

- **disaggregated architecture**（分离架构）：把 rollout 与 training 等 roles 放到独立 GPU pools，以便独立优化和流水重叠。→ [[L54 RL后训练系统]]

- **DCGM**（Data Center GPU Manager）：面向集群的低开销 GPU 遥测、健康、诊断与作业统计框架。→ [[L52 性能剖析与MFU核算]]
- **desync**（不同步推进）：不同 ranks 的阶段时间或 collective 进入次序不再对齐。→ [[L52 性能剖析与MFU核算]]

- **delayed scaling**（延迟缩放）：用历史 amax 估计当前 scale，并把当前 amax 留给后续 step 的策略。→ [[L49 混合精度与FP8训练]]
- **determinism**（确定性）：固定状态、输入和环境时，执行结果由这些条件唯一决定的性质。→ [[L49 混合精度与FP8训练]]
- **dynamic loss scaling**（动态损失缩放）：根据 Inf/NaN 与连续健康 steps 自动调整 loss scale。→ [[L49 混合精度与FP8训练]]

- **device mesh**（设备网格）：把 global ranks 组织成多维坐标，并描述各维 tensor placement 与通信组。→ [[L47 混合并行组装]]

- **DeepEP**（DeepSeek EP 通信库）：面向 MoE dispatch/combine 的高性能、低精度、拓扑感知通信 kernel 库。→ [[L46 专家并行与MoE训练]]；推理低时延路径见 [[L60 分布式推理与PD分离]]

- **DeepSpeed-Ulysses**（DeepSpeed-Ulysses）：用两次 all-to-all 在 sequence-sharded 与 head-sharded layout 间切换的长序列并行方法。→ [[L45 序列与上下文并行]]

- **DualPipe**（双向流水调度）：DeepSeek-V3 中从两端注入 micro-batches、联合覆盖计算与通信的流水调度。→ [[L44 流水线并行]]；MoE A2A 语境见 [[L46 专家并行与MoE训练]]

- **dispatch/combine**（派发 / 汇合）：将 token hidden states 送往专家，再把专家输出送回来源并加权复原的两段通信。→ [[L46 专家并行与MoE训练]]

- **DTensor**（分布式张量）：用 device mesh 与 placement 描述 tensor 分布布局的 PyTorch 抽象；FSDP2 用它表示参数 shards。→ [[L42 ZeRO与FSDP]]

- **data parallelism**（数据并行）：每个 rank 复制完整模型、处理不同数据分片，再同步梯度或参数。→ [[L41 数据并行与DDP]]
- **DistributedDataParallel (DDP)**（分布式数据并行封装）：PyTorch 以多进程模型副本和 backward 梯度同步实现的数据并行模块。→ [[L41 数据并行与DDP]]

- **double binary tree**（双二叉树）：两棵互补树各处理一半数据，兼顾对数深度和高带宽。→ [[L37 通信算法与代价模型]]

- **device plugin**（设备插件）：向 kubelet 注册并上报 GPU 等 vendor-specific devices 的扩展机制。→ [[L35 GPU集群调度]]
- **DRF (Dominant Resource Fairness)**（主导资源公平）：以用户各类资源份额中的最大值为 dominant share 的多资源公平方法。→ [[L35 GPU集群调度]]

- **data pipeline**（数据管线）：原始数据经清洗、tokenize、sharding、读取、组 batch 到进入 GPU 的全流程。→ [[L34 存储与数据供给]]
- **dataloader stall**（数据加载停顿）：下一 batch 未及时就绪，使 GPU 在相邻训练 step 之间空等的现象。→ [[L34 存储与数据供给]]

- **DPU**（数据处理单元）：带 embedded compute、memory、accelerators 与 OS，面向 infrastructure services 的处理器。→ [[L33 在网计算与智能网卡]]

- **dual-plane**（双平面）：把 fabric 划为相互分离的 forwarding planes，以结构约束减少多级 hash 冲突。→ [[L32 路由与负载均衡]]
- **dual-ToR**（双 ToR）：NIC/host 同时接入两台 ToR，以提高接入冗余与故障切换能力。→ [[L32 路由与负载均衡]]

- **diameter**（网络直径）：任意端点对最短路径长度的最大值，使用时必须注明 hop 口径。→ [[L31 AI集群网络拓扑]]
- **dragonfly**（蜻蜓拓扑）：把 routers 分组，以密集 local links 和稀疏 global links 构成低直径网络。→ [[L31 AI集群网络拓扑]]

- **DCQCN**（Data Center Quantized Congestion Notification，数据中心量化拥塞通知）：以 ECN/CNP 驱动 sender NIC 调节 RoCEv2 rate 的端到端 congestion control。→ [[L30 无损网络与拥塞控制]]

- **doorbell**（门铃）：应用写设备映射寄存器或记录，通知 NIC 有新 WQE 可处理。→ [[L29 RDMA原理]]

- **datacenter**（数据中心）：容纳计算、网络、存储以及供电和散热设施的物理系统。→ [[L27 走进AI数据中心]]

- **D2D (die-to-die)**（裸片到裸片互联）：在封装内或紧耦合芯粒之间连接 dies 的短距离互联层级。→ [[L25 节点内互联]]
- **DGX**（NVIDIA 完整 AI 服务器系统）：集成 GPU、CPU、互联、网络、存储与软件的 NVIDIA 整机平台。→ [[L25 节点内互联]]

- **dynamic range**（动态范围）：一个数值格式从很小到很大能覆盖的数值量级范围，浮点格式主要由 exponent bits 决定。→ [[L24 数值格式]]

- **data reuse**（数据复用）：数据搬到近端存储后参与多次计算，以摊薄 HBM 等远端字节成本。→ [[L23 Roofline模型]]

- **dense vs structured sparsity (2:4)**（稠密与 2:4 结构化稀疏）：dense 处理全部值；2:4 要求每四个权重中两个为零，可使用专门稀疏路径。→ [[L22 算力度量与MFU]]
- **DRAM**（动态随机存取存储器）：主机内存和 HBM 的底层存储技术；主机 DRAM 容量大但离 GPU 更远。→ [[L21 GPU内存体系]]
- **dropless**（不丢 token）：即使路由不均也处理每个 token 的 MoE 容量策略，以动态 buffer 或调度换取质量。→ [[L17 MoE混合专家]]

- **data cleaning/filtering**（数据清洗/过滤）：抽取正文、识别语言并按质量、安全和格式规则筛除样本。→ [[L14 预训练]]
- **data curriculum**（数据课程）：随训练进度改变数据组成或难度的安排。→ [[L14 预训练]]
- **data mixture**（数据混配）：按类别和阶段配置代码、多语言、数学等子集的训练曝光比例。→ [[L14 预训练]]
- **decoding**（解码）：把模型的 next-token 分布反复转成 token 序列的完整推理过程。→ [[L13 自回归生成与KV缓存]]
- **decode phase**（解码阶段）：逐步处理新 token、读取并追加 KV cache 的阶段。→ [[L13 自回归生成与KV缓存]]
- **decoder-only**（仅解码器架构）：只堆叠带 causal mask 的 Transformer block、从左到右生成的架构，现代 LLM 主流。→ [[L12 Transformer全解剖]]
- **deduplication**（去重）：识别并删除重复或高度相似的文档、段落或 token 片段。→ [[L14 预训练]]
- **DPO**（直接偏好优化）：用偏好对和 reference 直接训练 policy，不显式训练 reward model。→ [[L15 后训练与对齐]]
- **dense model**（稠密模型）：每个 token 都激活全部参数的模型；与稀疏激活的 MoE 相对。→ [[L12 Transformer全解剖]]；MoE 对照见 [[L17 MoE混合专家]]
- **distillation**（蒸馏）：用教师模型产生的行为或分布训练学生模型。→ [[L15 后训练与对齐]]；推理压缩语境见 [[L58 量化推理]]
- **down projection**（降维投影）：FFN 中把 $d_{ff}$ 维压回 $d$ 维的输出矩阵。→ [[L12 Transformer全解剖]]
- **$d_{head}$**（单头维度）：单个 attention head 的特征宽度，MHA 中通常有 $d_{head}=d/h$。→ [[L11 注意力机制]]
- **DataLoader**（数据加载器）：把 dataset 的采样、读取、变换和组批组织成可迭代 batch 的组件。→ [[L07 训练循环解剖]]
- **dataset**（数据集）：用于训练或评估的一组 sample。→ [[L03 机器学习速成]]
- **depth**（深度）：网络沿数据流方向堆叠的层数，阅读时需确认具体计数口径。→ [[L04 神经网络与前向传播]]

## E

- **embedding model**（嵌入模型）：把 query/文档映射成便于相似度检索的向量。→ [[L70 选修-多模态与新负载]]

- **extrapolation**（外推）：把已校准区间的模型用于更大规模、未来配置或未直接测量条件。→ [[L67 测量仿真与评测]]

- **elasticity**（弹性）：随供给或负载改变资源规模与布局，并恢复目标语义和性能的能力。→ [[L66 弹性算力与资源效率]]

- **error budget**（错误预算）：对 success/availability SLO 由 $1-\text{target}$ 定义、在统计窗口内允许消耗的失败量。→ [[L65 推理服务生存性]]

- **edge inference**（边缘推理）：在靠近数据产生者的终端、接入边缘或园区节点执行模型推理。→ [[L64 边缘与端云协同]]
- **edge-cloud collaboration**（端云协同）：让 device、edge 与 cloud 按请求或执行阶段共同完成推理。→ [[L64 边缘与端云协同]]

- **error feedback**（误差反馈）：保存压缩残差并在后续更新中加回，以补偿有偏压缩的信息损失。→ [[L63 跨域与跨集群训练]]

- **EAGLE**（EAGLE 特征级投机）：在 target 的高层 feature 空间自回归预测候选，再交给 target 保持分布地验证。→ [[L59 投机解码]]

- **external fragmentation**（外部碎片）：总空闲量足够、却因空洞不连续而无法满足连续大块分配。→ [[L56 KV缓存与PagedAttention]]
- **eviction/LRU**（淘汰 / 最近最少使用）：容量不足时移除缓存条目；LRU 优先移除最久未访问者。→ [[L56 KV缓存与PagedAttention]]

- **E2E latency**（端到端时延）：从请求到达到最后一个输出 token 到达的总时延。→ [[L55 推理性能模型]]

- **experience phase**（经验构造阶段）：为 rollout 补齐 reward、value、reference logprob 等训练信号的阶段。→ [[L54 RL后训练系统]]

- **elastic training**（弹性训练）：worker membership 变化后重新组网、恢复并继续训练的运行方式。→ [[L53 大规模训练可靠性]]；spot、重配与数据语义见 [[L66 弹性算力与资源效率]]

- **exposed communication**（暴露通信）：未被计算重叠、直接进入训练关键路径的通信时间。→ [[L52 性能剖析与MFU核算]]

- **epilogue fusion**（尾声融合）：在 GEMM 结果写回 HBM 前融合 bias、activation 或量化等尾部操作。→ [[L51 算子优化与FlashAttention]]

- **expandable segments**（可扩展内存段）：让 allocator 的 segment 可增长，以缓解动态尺寸在段尾制造的碎片。→ [[L50 显存优化技术]]

- **EPLB**（专家并行负载均衡器）：根据估计负载复制热门专家并生成平衡的 expert placement。→ [[L46 专家并行与MoE训练]]；wide-EP 推理见 [[L60 分布式推理与PD分离]]

- **ECMP**（等价多路径）：对等价 next hops 做 hash 选路，常把同一 flow 固定在一条 path。→ [[L32 路由与负载均衡]]

- **ECN**（Explicit Congestion Notification，显式拥塞通知）：switch 在 drop 前给 packet 标 CE，向端点暴露拥塞。→ [[L30 无损网络与拥塞控制]]

- **EFA/SRD**（AWS 弹性网络适配器 / 可扩展可靠数据报）：以 OS-bypass、可靠数据报和多路径乱序容忍服务云上 AI/HPC 通信。→ [[L29 RDMA原理]]；SRD 的有损 transport 语境见 [[L30 无损网络与拥塞控制]]

- **elephant flow**（大象流）：持续较久或数据量大、能占据显著链路容量的 flow。→ [[L28 数据中心网络基础]]

- **east-west traffic**（东西向流量）：数据中心内部服务器、机柜或 pod 之间的流量。→ [[L27 走进AI数据中心]]

- **eager mode**（即时执行模式）：Python 遇到一个 op 就立即派发执行，灵活且便于逐步调试。→ [[L26 加速器生态与软件栈]]
- **export control**（出口管制）：对特定物项、软件、技术、目的地、用户或用途设置许可与限制的制度。→ [[L26 加速器生态与软件栈]]

- **E4M3**（4 指数位 3 尾数位 FP8）：范围较小而精度较高的 FP8 变体，常用于 forward tensor。→ [[L24 数值格式]]
- **E5M2**（5 指数位 2 尾数位 FP8）：范围较大而精度较低的 FP8 变体，常用于 gradient。→ [[L24 数值格式]]
- **exponent**（指数位）：浮点编码中决定 2 的幂、主要控制 dynamic range 的位域。→ [[L24 数值格式]]

- **elementwise operation**（逐元素操作）：对 tensor 每个元素独立执行相同计算的 op。→ [[L23 Roofline模型]]

- **expert**（专家）：MoE 中的一份独立 FFN 参数块，接收被 router 选中的 token。→ [[L17 MoE混合专家]]
- **expert parallelism**（专家并行）：把不同 expert 分片到不同 GPU/rank、通过通信协同计算的并行方式。→ [[L17 MoE混合专家]]；训练系统深化见 [[L46 专家并行与MoE训练]]
- **expert placement**（专家放置）：决定逻辑 expert 及其 replicas 驻留在哪些 GPUs/nodes。→ [[L46 专家并行与MoE训练]]

- **effective FLOPS**（有效 FLOPS）：训练工作量除以墙钟时间得到的平均实际计算速率。→ [[L16 Scaling Law与算力账]]
- **embedding**（嵌入）：将离散 ID 映射为可学习的连续向量的表示。→ [[L10 Token与嵌入]]
- **embedding matrix**（嵌入矩阵）：形状为 $V\times d$、每行对应一个 token 向量的参数表。→ [[L10 Token与嵌入]]
- **end-to-end**（端到端）：覆盖用户或任务经历的完整处理路径，而非只测某个局部组件。→ [[L02 如何读系统论文]]
- **emergent abilities**（涌现能力）：随模型规模增大看似突然出现的能力现象，需警惕离散指标与统计测量造成的假象。→ [[L16 Scaling Law与算力账]]
- **encoder-decoder**（编码器-解码器）：encoder 表示输入序列、decoder 基于该表示生成输出序列的结构。→ [[L08 CNN与RNN简史]]
- **EOS**（序列结束 token）：end-of-sequence 的缩写，常可作为生成停止信号。→ [[L10 Token与嵌入]]
- **epoch**（轮次）：模型原则上完整使用一次 training set 的过程。→ [[L03 机器学习速成]]
- **exploding gradient**（梯度爆炸）：深层反传中 gradient 被连续放大，导致更新和数值不稳定。→ [[L05 反向传播与梯度]]

## F

- **function calling**（函数调用能力）：让模型按给定 schema 选择函数并生成结构化参数的接口机制。→ [[L70 选修-多模态与新负载]]

- **fidelity**（保真度）：模型对当前研究问题所需真实行为的保留程度。→ [[L67 测量仿真与评测]]
- **flow-level simulation**（流级仿真）：以 flow 为基本对象、建模 path、共享带宽与完成时间的仿真。→ [[L67 测量仿真与评测]]

- **failover**（故障转移）：从检测、隔离到重路由、恢复状态与补足容量的完整切换过程。→ [[L65 推理服务生存性]]
- **fault injection**（故障注入）：主动制造组件、网络、状态或控制面异常以测量系统反应。→ [[L65 推理服务生存性]]；仿真评测方法见 [[L67 测量仿真与评测]]

- **federated learning**（联邦学习）：原始数据不汇集时，由多个 clients 或机构协同训练共享模型的范式。→ [[L63 跨域与跨集群训练]]
- **FedAvg**（Federated Averaging，联邦平均）：clients 本地更新多步后按本地样本量加权平均模型更新的经典联邦算法。→ [[L63 跨域与跨集群训练]]

- **FP8 serving**（FP8 推理服务）：让主要 serving GEMM 使用 FP8 weight/activation，并保留必要高精度路径。→ [[L58 量化推理]]

- **fairness**（公平性）：控制不同请求或租户长期获得资源份额与等待机会的性质，使用时必须注明份额口径。→ [[L57 连续批处理与调度]]
- **FCFS/SJF**（先来先服务 / 短作业优先）：前者按到达顺序，后者优先预计短或剩余工作少的请求。→ [[L57 连续批处理与调度]]

- **failure rate**（故障率）：单位时间发生故障的速率，常记为 $\lambda$。→ [[L53 大规模训练可靠性]]
- **fail-stop**（停止型故障）：组件明确停止、退出或失联，故障时刻通常较清楚。→ [[L53 大规模训练可靠性]]
- **fail-slow**（慢故障）：组件仍能响应但显著变慢，常以 straggler 或通信等待表现。→ [[L53 大规模训练可靠性]]

- **flight recorder**（飞行记录器）：用环形缓冲保留近期 collective 元数据、时间和可选调用栈的通信黑匣子。→ [[L52 性能剖析与MFU核算]]

- **FP8 training**（FP8 训练）：让主要 GEMM 使用 FP8 输入，同时保留高精度输出、累加、敏感算子或状态的训练配方。→ [[L49 混合精度与FP8训练]]

- **full recomputation**（完整重计算）：只存层或分段边界，在 backward 重跑边界间大部分 forward。→ [[L50 显存优化技术]]

- **f/g operators**（f/g 算子）：Megatron 中共轭的通信封装；$f$ 为前向 identity/反向 all-reduce，$g$ 相反。→ [[L43 张量并行]]

- **FSDP**（Fully Sharded Data Parallel，完全切分数据并行）：PyTorch 对 fully-sharded data parallel 的实现，full-shard 思想对应 ZeRO-3。→ [[L42 ZeRO与FSDP]]
- **flat parameter**（扁平参数）：FSDP1 把一个参数组 flatten、concatenate 后用于切分和 collective 的内部表示。→ [[L42 ZeRO与FSDP]]

- **five-tuple hash**（五元组哈希）：用源/目的 IP、源/目的 port 与 protocol 生成 ECMP bucket。→ [[L32 路由与负载均衡]]
- **flow entropy**（流熵）：可独立参与分路的活跃 flows 在数量与 header 多样性上的丰富程度。→ [[L32 路由与负载均衡]]
- **flowlet**（流片）：同一 flow 内由足够长 packet gap 分隔、可独立换路的 burst。→ [[L32 路由与负载均衡]]

- **Falcon**（Google 硬件辅助 transport）：组合 RTT 测量、traffic shaping、精确重传、多路径与灵活排序的有损 Ethernet transport。→ [[L30 无损网络与拥塞控制]]

- **fat-tree**（胖树）：带宽向上不变瘦的规则化多级 Clos；经典 k-ary 构造支持 $k^3/4$ 个端点。→ [[L28 数据中心网络基础]]；AI 集群拓扑对照见 [[L31 AI集群网络拓扑]]
- **flow completion time (FCT)**（流完成时间）：一个 flow 从开始到全部数据传完所经历的时间。→ [[L28 数据中心网络基础]]
- **full bisection**（全对分带宽）：最坏等分仍能承载一侧全部端点线速注入的容量性质。→ [[L28 数据中心网络基础]]

- **front-end network**（前端接入网）：承载用户请求、登录、控制与外部服务接入的网络。→ [[L27 走进AI数据中心]]

- **floating point**（浮点数）：用 sign、exponent 与 mantissa 表示跨数量级实数的二进制科学计数法。→ [[L24 数值格式]]
- **FP32**（32 位浮点）：1s+8e+23m 的单精度浮点格式，课内存储按 4 B。→ [[L24 数值格式]]
- **FP16**（16 位浮点）：1s+5e+10m 的半精度格式，局部较细但动态范围窄，课内存储按 2 B。→ [[L24 数值格式]]
- **FP8**（8 位浮点家族）：常指 E4M3 与 E5M2 两种格式，通常需要配合 scale factor 使用。→ [[L24 数值格式]]
- **FP4**（4 位浮点）：常见元素编码为 E2M1，通常依赖细粒度 scale 才能实用。→ [[L24 数值格式]]

- **fused kernel**（融合 kernel）：在一次 kernel 中完成多个 ops，减少中间结果的 HBM 读写与 launch。→ [[L23 Roofline模型]]；融合边界与流量账见 [[L51 算子优化与FlashAttention]]

- **fragmentation**（碎片）：空闲总量足够，但连续内存、GPU 位置、类型、拓扑或时间形状无法满足一次分配。→ [[L21 GPU内存体系]]；集群资源语境见 [[L35 GPU集群调度]]；allocator 诊断见 [[L50 显存优化技术]]
- **FlashAttention**（IO 感知精确注意力）：用 tiling 与 online softmax 减少 HBM 读写、但保持 dense attention 数学结果的算法。→ [[L18 注意力变体与长上下文]]；kernel 机制见 [[L51 算子优化与FlashAttention]]
- **FlashAttention-2 (FA-2)**（FlashAttention 2）：通过更好的并行度与 thread block/warp 工作划分改进 FlashAttention。→ [[L51 算子优化与FlashAttention]]
- **FlashAttention-3 (FA-3)**（FlashAttention 3）：面向 Hopper 的异步流水与 FP8 能力进一步特化 attention。→ [[L51 算子优化与FlashAttention]]

- **fine-grained experts**（细粒度专家）：把 FFN 拆成更多、更小的 expert 单元以提供更细的组合容量。→ [[L17 MoE混合专家]]

- **feature**（特征）：提供给模型、用于作出预测的输入信息。→ [[L03 机器学习速成]]
- **first moment**（一阶矩）：Adam 中 gradient 的指数移动平均 $m$，用于估计近期方向。→ [[L06 优化器]]
- **FLOPs**（浮点运算次数）：计算工作量单位；本课程把一次乘加记作 2 FLOPs。→ [[L04 神经网络与前向传播]]
- **forward pass**（前向传播）：按当前 parameter 从输入依次执行各层运算并得到输出的过程。→ [[L04 神经网络与前向传播]]
- **frontier model**（前沿模型）：处于能力和训练规模前沿、通常需要大规模基础设施支撑的模型。→ [[L01 全景图]]
- **fully-connected layer**（全连接层）：每个输入都连接到每个输出、主体可写成矩阵乘的网络层。→ [[L04 神经网络与前向传播]]
- **FFN**（feed-forward network，前馈网络）：Transformer block 中对每个位置独立作用的小型 MLP，参数量约占全模型 2/3。→ [[L12 Transformer全解剖]]

## G

- **graceful degradation**（优雅降级）：按预设优先级减少功能、质量或流量以维持核心服务。→ [[L65 推理服务生存性]]

- **gossip**（流言式传播）：节点只与邻居交换并混合状态，让信息经过多轮扩散到全图。→ [[L63 跨域与跨集群训练]]
- **gradient compression**（梯度压缩）：用稀疏化、量化或低秩表示减少训练更新的通信字节。→ [[L63 跨域与跨集群训练]]

- **guided / structured decoding**（引导式 / 结构化解码）：按 JSON schema、正则或 grammar 限制每一步合法 token 的解码机制。→ [[L61 推理服务框架与集群]]

- **GPTQ**（GPTQ 训练后权重量化）：用 calibration 输入和近似二阶信息逐层控制量化后的重构误差。→ [[L58 量化推理]]
- **group size (g128)**（量化分组大小）：g128 表示每 128 个权重共享一套 scale/zero point。→ [[L58 量化推理]]

- **generation stall**（生成停顿）：长 prefill 等工作让一批 active decodes 的下一个 token 集体延迟。→ [[L57 连续批处理与调度]]

- **generation phase**（生成阶段）：inference engine 用当前指定版本 actor 产生 rollout 的阶段。→ [[L54 RL后训练系统]]

- **Gemini**（内存检查点恢复系统）：用 CPU memory、冗余放置与 traffic scheduling 加速分布式训练恢复的 SOSP 2023 系统。→ [[L53 大规模训练可靠性]]

- **gap**（时间空隙）：timeline 中目标资源没有可执行工作的空白区间，是症状而非根因。→ [[L52 性能剖析与MFU核算]]

- **graph capture/replay**（图捕获/重放）：先记录可复用的 GPU 工作与依赖图，再低开销重复提交同一图实例。→ [[L51 算子优化与FlashAttention]]

- **GSPMD**（通用可扩展 SPMD 并行化）：根据少量 sharding annotations 传播 tensor placement 并插入通信的编译器方法。→ [[L47 混合并行组装]]

- **grouped GEMM**（分组 GEMM）：在一次或少数 kernel 中调度多组不同形状的 expert 矩阵乘。→ [[L46 专家并行与MoE训练]]

- **GPipe**（GPipe 流水方案）：先执行全部 micro-batch forward，再逆序执行全部 backward，并在 step 边界排空流水线的同步方案。→ [[L44 流水线并行]]

- **gradient bucketing**（梯度装桶）：把多个小梯度合并成较大通信桶，以摊薄 collective 固定开销并支持流水。→ [[L41 数据并行与DDP]]
- **gradient checkpointing**（梯度检查点）：activation recomputation 的常用同义名；保存计算边界并在 backward 重算中间 activation。→ [[L50 显存优化技术]]
- **gradient synchronization**（梯度同步）：聚合各 ranks 的本地梯度，使模型副本在更新前获得一致梯度。→ [[L41 数据并行与DDP]]

- **Gloo**（Gloo 通信库）：常用于 CPU collective 和框架后备路径的通信库。→ [[L38 NCCL解剖]]
- **gather**（汇集）：把各 rank 的分片按 rank 顺序拼接到一个 root。→ [[L36 集合通信原语]]

- **gang scheduling**（成组调度）：相关 workers 达到最低并发规模后才作为一组启动的调度语义。→ [[L35 GPU集群调度]]

- **GPFS**（通用并行文件系统）：IBM Storage Scale 的原名，面向多服务器共享、并行文件访问。→ [[L34 存储与数据供给]]
- **GPUDirect Storage**（GPU 直连存储）：让 storage-side DMA engine 与 GPU memory 直接搬数据、避免 CPU bounce buffer 的路径。→ [[L34 存储与数据供给]]

- **group/global link**（组 / 全局链路）：group 是 Dragonfly 的局部高密度互联单元；global link 连接不同 groups。→ [[L31 AI集群网络拓扑]]

- **go-back-N**（回退 N 帧重传）：检测到序号缺口后从丢失处重传尚未确认窗口的传统恢复方式。→ [[L29 RDMA原理]]
- **GPUDirect RDMA**（GPU 直接远程内存访问）：让 NIC 经 PCIe peer DMA 直接读写 GPU memory，绕过 host staging。→ [[L29 RDMA原理]]

- **graph mode**（图执行模式）：先收集一段 op graph，再跨 op 优化、编译和执行。→ [[L26 加速器生态与软件栈]]

- **GPUDirect**（GPU 直连技术家族）：通过 P2P、RDMA 或 Storage 等路径减少 host staging 的 NVIDIA 技术家族。→ [[L21 GPU内存体系]]
- **GPUDirect P2P**（GPU 直接点对点访问）：允许 GPU 直接访问或复制另一张 GPU 的显存，避免 host-memory staging。→ [[L25 节点内互联]]
- **grouped-query attention (GQA)**（分组查询注意力）：把 Q 头分组，组内共享一组 K/V，是 MHA 与 MQA 的折中。→ [[L18 注意力变体与长上下文]]

- **gating network**（门控网络）：router 的另一称呼；为 token 对候选 expert 打分并产生路由权重。→ [[L17 MoE混合专家]]

- **GPU-hours**（GPU 小时）：GPU 数量乘墙钟小时，记录集群资源消耗。→ [[L16 Scaling Law与算力账]]
- **greedy decoding**（贪心解码）：每一步都选择当前概率最大的 token。→ [[L13 自回归生成与KV缓存]]
- **gate projection**（门控投影）：SwiGLU FFN 中经 SiLU 后充当门控信号的投影矩阵。→ [[L12 Transformer全解剖]]
- **GELU**（高斯误差线性单元）：Transformer 中常见的一种平滑 activation function。→ [[L04 神经网络与前向传播]]
- **generalization**（泛化）：模型在未参与训练的新样本上仍然有效的能力。→ [[L03 机器学习速成]]
- **generative modeling**（生成式建模）：学习数据分布并生成新样本或序列的任务。→ [[L03 机器学习速成]]
- **GEMM**（通用矩阵乘）：General Matrix Multiply，linear layer 的主体计算。→ [[L04 神经网络与前向传播]]；性能口径见 [[L22 算力度量与MFU]]
- **GEMM efficiency**（GEMM 效率）：单个 GEMM 实测 arithmetic throughput 与匹配口径 peak FLOPS 的比值。→ [[L22 算力度量与MFU]]
- **GPU utilization**（GPU 忙碌时间占比）：`nvidia-smi` 采样周期内至少一个 kernel 在执行的时间比例，不等于 Tensor Core 利用率。→ [[L22 算力度量与MFU]]
- **GRPO**（组相对策略优化）：对同一 prompt 的多条回答比较组内 reward，再更新 policy 的方法。→ [[L15 后训练与对齐]]
- **goodput**（有效吞吐）：单位时间内满足正确性、服务或训练进度约束的有效工作量；推理中常指满足 SLO attainment 目标的最大可承载请求率。→ [[L52 性能剖析与MFU核算]]；推理语义见 [[L55 推理性能模型]]
- **grad norm**（梯度范数）：把全部或一组 gradient 汇总成长度的统计量，常用于监控和 clipping。→ [[L06 优化器]]
- **gradient**（梯度）：由 loss 对各变量的偏导数组成、指向局部最快上升方向的向量。→ [[L05 反向传播与梯度]]
- **gradient clipping**（梯度裁剪）：gradient 过大时按阈值限制其 norm，降低异常单步的破坏。→ [[L06 优化器]]
- **gradient descent**（梯度下降）：使用全部训练数据的 gradient，沿负梯度方向更新参数的方法。→ [[L06 优化器]]
- **gradient with respect to inputs**（对 inputs 的梯度）：loss 对 layer inputs 的偏导数，用于把 gradient 继续传向前一层。→ [[L05 反向传播与梯度]]
- **gradient with respect to weights**（对 weights 的梯度）：loss 对 layer weights 的偏导数，供 optimizer 更新参数。→ [[L05 反向传播与梯度]]
- **global batch size**（全局批大小）：一次参数更新在全部 data-parallel ranks 上共同贡献 gradient 的样本或 token 总量。→ [[L07 训练循环解剖]]
- **gradient accumulation**（梯度累积）：连续处理多个 micro-batch 并累加 gradient，达到规定次数后才更新一次参数。→ [[L07 训练循环解剖]]；混合并行的 batch/气泡预算见 [[L47 混合并行组装]]
- **GPU cluster**（GPU 集群）：由多台含 GPU 的服务器及网络、存储和调度设施组成的计算集群。→ [[L01 全景图]]
- **grid**（网格）：一次 GPU kernel launch 中全部 thread blocks 的集合。→ [[L20 GPU体系结构入门]]

## H

- **HNSW**（Hierarchical Navigable Small World，分层可导航小世界图）：用多层近邻图从粗到细导航的常见 ANN 索引。→ [[L70 选修-多模态与新负载]]

- **htsim**（htsim 网络仿真器）：聚焦 datacenter congestion-control 行为的轻量高性能 discrete-event simulator。→ [[L67 测量仿真与评测]]

- **Harvest VM**（资源收割型虚机）：随底层未分配资源伸缩、只在最低资源也不可用时被驱逐的 VM 类型。→ [[L66 弹性算力与资源效率]]

- **hedged request**（对冲请求）：原请求变慢后向另一副本发副本，用重复工作换较低尾时延。→ [[L65 推理服务生存性]]

- **heterogeneous deployment**（异构部署）：给 P/D 阶段选择算力/带宽形状不同的硬件与独立配置。→ [[L60 分布式推理与PD分离]]

- **hybrid engine**（训练—生成混合引擎）：让 actor 在训练与高效生成形态间切换，并管理显存、并行组和权重布局。→ [[L54 RL后训练系统]]
- **HybridFlow**（混合控制流 RLHF 框架）：以高层 single controller 与模型内 multi-controller 组织复杂 RLHF dataflow 的 EuroSys 2025 系统。→ [[L54 RL后训练系统]]

- **health check**（健康检查）：用旁路遥测或主动测试判断组件是否满足继续参与作业的条件。→ [[L53 大规模训练可靠性]]
- **hot spare**（热备节点）：预留且已验证健康、可在故障后快速补入作业的备用节点。→ [[L53 大规模训练可靠性]]

- **high-precision accumulation**（高精度累加）：低精度乘法输入配合更高精度部分和，减少长归约的舍入误差。→ [[L49 混合精度与FP8训练]]

- **hybrid parallelism**（混合并行）：在同一训练作业中组合两种或更多并行或切分方式。→ [[L47 混合并行组装]]

- **hot expert**（热门专家）：在一段 workload 中持续接收显著多于平均 token 的 expert。→ [[L46 专家并行与MoE训练]]

- **HSDP / hybrid sharding**（混合切分数据并行）：在本地组内 full-shard、跨组复制 shards 并同步梯度的二维 data-parallel 策略。→ [[L42 ZeRO与FSDP]]

- **HCCL**（华为集合通信库）：面向 Ascend/CANN 生态的高性能 collective library。→ [[L38 NCCL解剖]]
- **hierarchical all-reduce**（分层 all-reduce）：按 NVLink、节点与网络等带宽层级分段完成规约与分发。→ [[L37 通信算法与代价模型]]

- **hash collision**（哈希碰撞）：不同 flows 被映射到同一 path，可能让一条链路过载而别处空闲。→ [[L32 路由与负载均衡]]

- **headroom buffer**（刹车余量缓冲）：接住 PFC PAUSE 发出到上游真正停住期间仍会到达的数据。→ [[L30 无损网络与拥塞控制]]
- **HoL blocking / queue head-of-line blocking**（队头阻塞）：队头 packet、flow 或请求受阻时，挡住同 queue 中本可前进的工作。→ [[L30 无损网络与拥塞控制]]、[[L57 连续批处理与调度]]
- **HPCC/INT**（高精度拥塞控制 / 带内网络遥测）：由 packet 携带逐跳负载/queue 信息，供 sender 精确定位瓶颈和控速。→ [[L30 无损网络与拥塞控制]]

- **hyperscaler**（超大规模基础设施运营商）：自建或大规模运营众多数据中心与全球网络的平台型公司。→ [[L27 走进AI数据中心]]

- **H20**（Hopper 出口版 GPU）：课内口径下 BF16 较低、但 HBM 与 NVLink 保留较多，规格更偏 memory-bound 工作负载的型号。→ [[L26 加速器生态与软件栈]]
- **H800**（Hopper 出口版 GPU）：课内口径下保留 H100 的 BF16/HBM，NVLink 双向合计降至 400 GB/s 的型号。→ [[L26 加速器生态与软件栈]]

- **HGX**（NVIDIA 多 GPU 加速平台）：供服务器厂商集成、以 GPU 与 NVLink/NVSwitch 为核心的加速平台。→ [[L25 节点内互联]]

- **HFU**（Hardware FLOPs Utilization，硬件算力利用率）：硬件实际执行 FLOPs 相对峰值的比例，可能包含 activation 重计算。→ [[L22 算力度量与MFU]]
- **HBM**（高带宽内存）：通过堆叠和宽接口提供高显存带宽的 GPU 主存。→ [[L21 GPU内存体系]]
- **hybrid architecture**（混合架构）：组合 attention 与 SSM 等序列模块，权衡精确回忆能力与长序列效率。→ [[L18 注意力变体与长上下文]]

- **hardware lottery**（硬件彩票）：算法的流行与效果会受可获得硬件和软件生态偏好的影响这一观察。→ [[L08 CNN与RNN简史]]
- **held-out loss**（留出集损失）：在不参与参数更新的固定数据上测得的 loss，用于监控泛化趋势。→ [[L14 预训练]]
- **hidden layer**（隐藏层）：位于输入层和输出层之间、产生中间表示的网络层。→ [[L04 神经网络与前向传播]]
- **hidden size**（隐藏维度）：隐藏表示最后一维的大小，也就是该层的 width。→ [[L04 神经网络与前向传播]]
- **hidden state**（隐状态）：序列模型在当前时间步保存、供下一时间步使用的中间表示。→ [[L08 CNN与RNN简史]]
- **host/device**（主机/设备）：分别指 CPU 侧和 GPU 侧的执行与内存语境。→ [[L20 GPU体系结构入门]]
- **host-to-device copy (H2D/D2H)**（主机到设备/设备到主机拷贝）：在 host DRAM 与 GPU HBM 之间搬运 tensor 的操作。→ [[L21 GPU内存体系]]

## I

- **image token**（图像 token / 视觉 token）：占据 LLM 序列位置的视觉 embedding，不一定对应文本词表 ID。→ [[L70 选修-多模态与新负载]]

- **interference**（资源干扰）：混部 workloads 因争用执行、memory/cache 或调度资源而相互拖慢。→ [[L66 弹性算力与资源效率]]

- **idempotency**（幂等性）：同一操作重复执行仍产生等价、可接受业务结果的性质。→ [[L65 推理服务生存性]]

- **intermediate activation transfer**（中间激活传输）：把模型切点处的 hidden states 从一侧发送到另一侧继续计算。→ [[L64 边缘与端云协同]]

- **inner optimizer**（内层优化器）：在单个 domain 内连续执行 $H$ 步本地训练的优化器，DiLoCo 中为 AdamW。→ [[L63 跨域与跨集群训练]]
- **INTELLECT**（INTELLECT 全球协同训练项目）：Prime Intellect 公开的全球动态节点训练项目与技术报告谱系。→ [[L63 跨域与跨集群训练]]

- **inference parallelism**（推理并行）：为容量、TTFT、TPOT、吞吐与成本组合 TP/PP/CP/EP 的部署方法。→ [[L60 分布式推理与PD分离]]

- **iteration-level scheduling**（迭代级调度）：以一次模型迭代而非整条请求为 scheduler–engine 的调度粒度。→ [[L57 连续批处理与调度]]
- **in-flight batching**（在途批处理）：TensorRT-LLM 等系统对 continuous / iteration-level batching 的称呼。→ [[L57 连续批处理与调度]]

- **internal fragmentation**（内部碎片）：已分配块内部最终没有承载有效 token 的空间。→ [[L56 KV缓存与PagedAttention]]

- **ITL**（Inter-Token Latency，token 间时延）：相邻流式输出 token 之间的单次时间间隔。→ [[L55 推理性能模型]]

- **interruption**（训练中断）：作业停止产生有效训练进度的一次事件。→ [[L53 大规模训练可靠性]]
- **in-memory checkpoint**（内存检查点）：把恢复副本放在 host memory 等快速层，并靠跨故障域冗余保障可取回。→ [[L53 大规模训练可靠性]]

- **IO-aware algorithm**（IO 感知算法）：把不同内存层级之间的读写复杂度显式纳入算法设计目标。→ [[L51 算子优化与FlashAttention]]

- **intra-node parallelism**（节点内并行）：把高频并行通信组放在同一服务器的高速 GPU 互联域内。→ [[L43 张量并行]]

- **interleaved pipeline**（交错流水线）：每张物理卡持多个不连续 model chunks，以更多 P2P 消息换更短 bubble time。→ [[L44 流水线并行]]

- **in-place**（原地操作）：按 API 规则复用输入/输出 buffer；可省内存或本地 copy，但不消除跨 rank 通信。→ [[L36 集合通信原语]]

- **in-network aggregation**（在网聚合）：在 network fabric 内合并多个 ranks 的 sum/max 等 partial results。→ [[L33 在网计算与智能网卡]]
- **in-network computing**（在网计算）：在 packet 转发路径上的 network device 内执行部分 application/infrastructure computation。→ [[L33 在网计算与智能网卡]]

- **in-order delivery guarantee**（按序交付保证）：对 packet、message 或 operation 的可观察顺序作出的 transport/API 承诺。→ [[L32 路由与负载均衡]]

- **IRN**（Improved RoCE NIC，改进型 RoCE NIC）：以选择性恢复和 BDP flow control 放松 RoCE 对 PFC 的依赖。→ [[L30 无损网络与拥塞控制]]

- **InfiniBand**（InfiniBand 高性能互联）：原生包含 HCA、switch、传输、流控和 fabric 管理的 RDMA 网络体系。→ [[L29 RDMA原理]]
- **iWARP**（基于 TCP/IP 的 RDMA 协议族）：用 MPA/DDP/RDMAP 在 TCP 上提供远程内存语义的 IETF 路线。→ [[L29 RDMA原理]]

- **incast**（多对一瞬时汇聚）：多个发送者同时冲向同一接收端或出口而形成的拥塞。→ [[L28 数据中心网络基础]]；PFC/DCQCN 语境与定量例见 [[L30 无损网络与拥塞控制]]

- **ICI**（芯片间互联）：连接 TPU chips、承载 slice 内通信的专用 interconnect。→ [[L26 加速器生态与软件栈]]

- **INT8**（8 位整数）：用 256 个整数码配合 scale 表示实数，课内存储按 1 B。→ [[L24 数值格式]]
- **INT4**（4 位整数）：用 16 个整数码配合 scale 表示实数，课内主体存储按 0.5 B/元素。→ [[L24 数值格式]]

- **ImageNet**（图像数据集）：大规模图像数据集；其分类挑战推动了深层 CNN 的发展与评测。→ [[L08 CNN与RNN简史]]
- **Inf**（无穷大）：超出有限浮点范围或某些非法数值运算产生的无穷值。→ [[L05 反向传播与梯度]]
- **inference**（推理）：使用固定权重对输入执行预测或生成的过程。→ [[L01 全景图]]；ML 语义与训练/推理边界见 [[L03 机器学习速成]]
- **intermediate size**（FFN 中间维度）：FFN 内部的扩展维度 $d_{ff}$，Llama-3-8B 为 14336（= 3.5$d$）。→ [[L12 Transformer全解剖]]
- **iteration**（迭代）：一次循环迭代的泛称，可能指 micro-step 或 optimizer step，语义依实现而定。→ [[L07 训练循环解剖]]

## J

- **JCT (job completion time)**（作业完成时间）：作业从提交到完成的总时间，通常包含排队、运行与暂停/恢复。→ [[L35 GPU集群调度]]
- **job scheduler**（作业调度器）：决定作业何时运行、获得多少资源以及放到哪些节点/GPU 的控制系统。→ [[L35 GPU集群调度]]

- **Jupiter**（Google 数据中心网络 fabric）：从 Clos 演进到 OCS direct-connect、并联合 topology/traffic engineering 的生产网络。→ [[L31 AI集群网络拓扑]]

## K

- **KV lifecycle**（KV 生命周期）：KV 从 active、suspended、resuming 到 expired/finished 的保留、搬运与回收过程。→ [[L70 选修-多模态与新负载]]

- **KV cache fault tolerance**（KV 缓存容错）：通过副本、迁移、分层取回或重算，在 KV 故障后恢复会话的机制集合。→ [[L65 推理服务生存性]]

- **KV migration**（KV 迁移）：把已有 KV cache 搬到新执行位置，以避免从 token history 重做 prefill。→ [[L64 边缘与端云协同]]

- **keep-warm**（保温实例）：保留已加载、已预热的空闲 capacity，以持续 GPU-hour 换低启动时延。→ [[L61 推理服务框架与集群]]

- **KV cache transfer**（KV 缓存传输）：把 P 产生或缓存池保存的 K/V 状态搬到 D，使其无需重新 prefill。→ [[L60 分布式推理与PD分离]]
- **KVCache-centric**（以 KV 缓存为中心）：让请求放置、复用、迁移、淘汰与调度围绕 KV 位置和重算成本决策。→ [[L60 分布式推理与PD分离]]

- **KV cache quantization**（KV 缓存量化）：降低 K/V 存储位宽以节省每请求容量和 attention 读取流量。→ [[L58 量化推理]]

- **KV cache management**（KV 缓存管理）：负责 KV 的分配、增长、共享、回收、抢占与跨层搬运。→ [[L56 KV缓存与PagedAttention]]
- **KV offloading/tiering**（KV 换出 / 分层）：按热度把 KV 放在 GPU、CPU、SSD 或远端存储。→ [[L56 KV缓存与PagedAttention]]

- **kernel fusion**（kernel 融合）：把有 producer-consumer 关系的多个 ops 合进更少 kernels，减少中间 HBM 往返与 launch。→ [[L51 算子优化与FlashAttention]]

- **Kubernetes**（Kubernetes 容器编排系统）：以 Pod 为基本调度单位、通过扩展机制管理设备与批作业的编排平台。→ [[L35 GPU集群调度]]
- **Kueue**（Kubernetes 队列控制器）：在 workload admission 层按 quota、priority 与 topology 管理批作业排队。→ [[L35 GPU集群调度]]

- **kernel bypass**（内核旁路）：控制面完成设置后，用户态直接向 NIC 队列投递稳定态数据操作。→ [[L29 RDMA原理]]

- **key**（键）：表示候选位置用什么标签接受匹配的向量。→ [[L11 注意力机制]]
- **KL penalty**（KL 惩罚）：对当前 policy 与 reference policy 的分布差异加惩罚，抑制策略漂移。→ [[L15 后训练与对齐]]
- **kernel/filter**（卷积核/滤波器）：卷积中在输入局部窗口上复用的一小组可学习权重；不同于 GPU kernel 与 OS kernel。GPU kernel 是在设备上执行的一次函数/计算任务。→ [[L08 CNN与RNN简史]]；GPU 语义见 [[L20 GPU体系结构入门]]
- **kernel launch**（内核发射）：CPU 把一次 GPU kernel 调用入队的动作，存在微秒级固定开销。→ [[L20 GPU体系结构入门]]
- **KV cache**（KV 缓存）：自回归生成时缓存历史 token 的 Key/Value 张量以免重算，是推理系统显存管理的主角。→ [[L13 自回归生成与KV缓存]]
- **KV cache compression**（KV 缓存压缩）：通过头共享、低秩、量化等方式减少 KV cache 容量与访存的总称。→ [[L18 注意力变体与长上下文]]

## L

- **load shedding**（负载丢弃）：主动拒绝、延后或转移部分 offered load，以保护已接纳请求。→ [[L65 推理服务生存性]]

- **llama.cpp**（llama.cpp 本地推理项目）：面向多平台本地 LLM 推理的 C/C++ 工程与工具链。→ [[L64 边缘与端云协同]]

- **local SGD**（本地 SGD）：各 worker 独立更新多步后才平均参数或更新，以计算换通信轮次。→ [[L63 跨域与跨集群训练]]

- **latency-bound communication**（时延受限通信）：小消息下固定开销 $\alpha$ 主导、增加带宽也难显著缩短的通信区间。→ [[L60 分布式推理与PD分离]]
- **layer-wise streaming**（逐层流式传输）：某层 KV 产出后立即异步发送，以和后续层 prefill 重叠。→ [[L60 分布式推理与PD分离]]

- **lossless (distribution preservation)**（无损 / 分布保持）：正确采样语义下最终序列分布与 target 单独运行相同，不等于随机样本逐 bit 相同。→ [[L59 投机解码]]

- **lossless claim**（“无损”宣称）：在指定数据、指标和容差内未测出退化的经验性说法，不是数学等价。→ [[L58 量化推理]]

- **logical/physical block**（逻辑块 / 物理块）：前者表示请求内 KV 的顺序，后者表示数据在全局 block pool 的实际位置。→ [[L56 KV缓存与PagedAttention]]
- **LMCache**（LMCache KV 存储系统）：为推理引擎提供多层 KV offload、复用和跨实例传输的系统。→ [[L56 KV缓存与PagedAttention]]

- **latency-throughput tradeoff**（时延—吞吐权衡）：增大 batch 或负载可能提高吞吐，也会推高单请求等待或生成时延。→ [[L55 推理性能模型]]
- **Little's Law**（利特尔定律）：稳态下平均在系统请求数等于吞吐乘平均时延，即 $L=\lambda W$。→ [[L55 推理性能模型]]

- **link flap**（链路抖动）：链路在可用/不可用状态间短时反复切换的现象。→ [[L53 大规模训练可靠性]]

- **launch-bound**（受 kernel 发射限制）：大量短 kernels 使 host/driver 的逐次 launch 固定开销主导性能。→ [[L52 性能剖析与MFU核算]]

- **loss scaling**（损失缩放）：backward 前放大 loss、更新前还原 gradient，以减少 FP16 gradient underflow。→ [[L49 混合精度与FP8训练]]

- **load imbalance**（负载不均）：token、通信或计算在 experts/ranks 之间分配不均的状态。→ [[L46 专家并行与MoE训练]]

- **long-context training**（长上下文训练）：用很长的单条序列训练模型，同时承受线性 activation 与平方 attention 计算增长。→ [[L45 序列与上下文并行]]

- **LAMB**（大 batch 逐层自适应优化器）：按层调整更新尺度、用于扩大可训练 batch 区间的优化方法。→ [[L41 数据并行与DDP]]
- **linear scaling rule**（学习率线性缩放规则）：batch 放大 $k$ 倍时将 learning rate 近似放大 $k$ 倍并配合 warmup 的经验启发。→ [[L41 数据并行与DDP]]

- **LL**（低时延协议）：用细粒度 data+flag 单元降低等待、但 payload efficiency 较低的 NCCL protocol。→ [[L38 NCCL解剖]]
- **LL128**（128-byte 低时延协议）：每 128 bytes 中用 120 bytes 承载数据的时延/带宽折中 NCCL protocol。→ [[L38 NCCL解剖]]
- **latency term**（时延项）：与消息字节数无关、主要由启动步数和 $\alpha$ 决定的通信时间部分。→ [[L37 通信算法与代价模型]]
- **latency-optimal**（时延最优）：算法以最少数量级的消息轮数完成信息扩散。→ [[L37 通信算法与代价模型]]

- **Lustre**（Lustre 并行文件系统）：以 metadata 与 bulk data 分离、clients 直访 storage targets 支撑并行 I/O 的开源 PFS。→ [[L34 存储与数据供给]]

- **libfabric**（fabric 通信 API/库）：为 EFA/SRD、UET 等 provider 向 MPI/collective/应用暴露统一通信接口。→ [[L30 无损网络与拥塞控制]]
- **lossless network**（无损网络）：在受控 fabric 内避免拥塞导致 frame drop，不代表物理故障或配置错误下绝不丢包。→ [[L30 无损网络与拥塞控制]]

- **lkey/rkey**（本地键 / 远端键）：lkey 供本地 WR 引用 MR，rkey 与远端地址共同授权 peer 访问 MR。→ [[L29 RDMA原理]]

- **leaf-spine**（叶脊网络）：每个 leaf 连接每个 spine 的两角色 Clos 拓扑。→ [[L28 数据中心网络基础]]

- **liquid cooling**（液冷）：用液体作为关键传热介质、把高热流密度设备热量带走的冷却方式。→ [[L27 走进AI数据中心]]

- **L1 cache**（一级缓存）：靠近 SM、由硬件自动管理的缓存，常与 shared memory 共享片上资源。→ [[L21 GPU内存体系]]
- **L2 cache**（二级缓存）：多个 SM 共享的 GPU 缓存，位于 HBM 之前。→ [[L21 GPU内存体系]]
- **latent/low-rank compression**（潜在表示/低秩压缩）：先降维保存窄表示，再通过投影形成所需 K/V 表示的方法。→ [[L18 注意力变体与长上下文]]
- **linear attention**（线性注意力）：通过 kernel 分解或递推状态避免显式 $S\times S$ attention 的一类方法。→ [[L18 注意力变体与长上下文]]
- **long context**（长上下文）：长到位置外推、KV 容量、attention 计算或远程检索成为主要问题的上下文。→ [[L18 注意力变体与长上下文]]

- **load balancing**（负载均衡）：按上下文可能指均衡 MoE expert 的 token/概率质量，或在 serving 实例间按工作量与状态分配请求。→ [[L17 MoE混合专家]]、[[L61 推理服务框架与集群]]

- **LayerNorm**（层归一化）：对单个样本的特征维做减均值、除标准差，再缩放平移的归一化。→ [[L12 Transformer全解剖]]
- **lm_head**（输出词表投影）：把最终 hidden state 映射为词表 logits 的 $d\times V$ 线性层。→ [[L12 Transformer全解剖]]
- **label**（标签）：监督学习希望模型预测的目标答案。→ [[L03 机器学习速成]]
- **latency**（时延）：单个请求或操作从开始到完成所经历的时间。→ [[L02 如何读系统论文]]
- **latency hiding**（时延隐藏）：一个 warp 等待内存时切换到其他 ready warps，用并行工作填补等待空档。→ [[L20 GPU体系结构入门]]
- **latency-oriented**（时延导向）：优先缩短单个请求或操作的完成时间。→ [[L01 全景图]]；CPU/GPU 对照见 [[L20 GPU体系结构入门]]
- **linear layer**（线性层）：框架中通常指带 weight 和 bias 的 affine 变换，常与 fully-connected layer 同义。→ [[L04 神经网络与前向传播]]
- **learning rate**（学习率）：把 gradient 转成参数更新幅度的步长系数。→ [[L06 优化器]]
- **learning rate schedule**（学习率调度）：规定 learning rate 怎样随训练进度变化的策略。→ [[L06 优化器]]
- **long CoT**（长思维链）：包含更多分解、尝试和自检 token 的 chain-of-thought 行为。→ [[L15 后训练与对齐]]
- **LoRA**（低秩适配）：用低秩矩阵表示权重更新、只训练旁路参数的 PEFT 方法。→ [[L15 后训练与对齐]]
- **logits**（未归一化分数）：输出层对候选类别给出的原始分数，不要求为正或总和为 1。→ [[L04 神经网络与前向传播]]
- **loss function**（损失函数）：把预测的错误程度变成可供优化追踪的标量函数。→ [[L03 机器学习速成]]
- **loss curve**（损失曲线）：loss 随 step、sample 或 token 变化的训练轨迹。→ [[L06 优化器]]
- **loss spike**（损失尖峰）：loss curve 中突然显著上冲的异常点或短区间。→ [[L06 优化器]]、[[L14 预训练]]
- **logging**（日志记录）：按明确进度轴保存训练指标、配置和系统状态，供监控与诊断。→ [[L07 训练循环解剖]]
- **LSTM**（长短期记忆网络）：带门控记忆状态的 RNN 变体，用于缓解长程依赖遗忘。→ [[L08 CNN与RNN简史]]

## M

- **multimodal**（多模态）：共同处理文本、图像、视频、音频等信号，系统上意味着不同预处理、阶段与长度分布。→ [[L70 选修-多模态与新负载]]

- **measurement study**（测量研究）：以真实系统或 workload 的系统性观测与归纳为主要证据的研究。→ [[L67 测量仿真与评测]]
- **MLPerf**（MLPerf 行业基准）：用统一 workload、quality target、scenario 与规则比较 ML 系统的 benchmark 家族。→ [[L67 测量仿真与评测]]

- **multi-region deployment**（多地域部署）：把服务、模型与必要控制或状态能力分布在多个 region。→ [[L65 推理服务生存性]]

- **MEC**（多接入边缘计算）：在网络边缘向应用提供云计算能力和 IT 服务环境的 ETSI 体系。→ [[L64 边缘与端云协同]]
- **MLC LLM**（MLC LLM 部署引擎）：用机器学习编译把 LLM 优化并部署到多类 GPU、浏览器和移动平台的工程。→ [[L64 边缘与端云协同]]
- **model cascade**（模型级联）：先用便宜模型处理，再把不确定或困难请求升级给更强模型。→ [[L64 边缘与端云协同]]

- **multi-datacenter training**（多数据中心训练）：一个训练任务跨越多个物理数据中心或独立 cluster 共同推进。→ [[L63 跨域与跨集群训练]]

- **model loading**（模型加载）：把 weights、配置和相关工件读入运行时，并完成转换、编译与可执行准备。→ [[L61 推理服务框架与集群]]
- **multi-tenancy**（多租户）：多个用户或业务共享 serving 平台资源，同时需要 quota、隔离与公平。→ [[L61 推理服务框架与集群]]
- **multi-LoRA serving**（多 LoRA 推理服务）：共享 base weights，并按请求选择、装载和批处理不同 adapters。→ [[L61 推理服务框架与集群]]

- **Mooncake**（Mooncake serving 平台）：以分布式 KV cache 为中心组织 P/D、存储、传输与调度的工业系统。→ [[L60 分布式推理与PD分离]]

- **Medusa**（Medusa 多头投机）：给模型增加多个 future-token heads，常结合候选树与 tree attention 并行验证。→ [[L59 投机解码]]
- **multi-token prediction (MTP)**（多 token 预测）：训练时加入预测多个未来 token 的目标或模块，推理时可作为 proposer。→ [[L59 投机解码]]

- **Marlin**（Marlin 混合精度 kernel）：面向 autoregressive inference 的 FP16×INT4 数据布局与矩阵乘 kernel。→ [[L58 量化推理]]

- **M/M/1**（M/M/1 排队模型）：泊松到达、指数服务时间、一个服务台的基础排队模型。→ [[L55 推理性能模型]]
- **memory-bound decode**（显存带宽受限的 decode）：单步计算少、权重与 KV 搬运多，性能典型地先受 HBM 限制。→ [[L55 推理性能模型]]

- **MTBF**（平均故障间隔）：相邻故障之间的平均运行时间；恒定故障率模型下为 $1/\lambda$。→ [[L53 大规模训练可靠性]]
- **MTTR**（平均修复/恢复时间）：从指定故障起点到修复或恢复有效工作的平均时间，必须说明测量边界。→ [[L53 大规模训练可靠性]]

- **MFU accounting**（MFU 核算）：用全局 token 吞吐、每 token 模型 FLOPs、卡数和 dense 峰值复算 MFU。→ [[L52 性能剖析与MFU核算]]

- **memory traffic**（内存流量）：执行期间在内存层级之间实际搬运的累计字节数，不等于 tensor 驻留容量。→ [[L51 算子优化与FlashAttention]]

- **mixed precision**（混合精度）：在一次训练中按 tensor、operator 或阶段分配不同数值格式。→ [[L49 混合精度与FP8训练]]

- **mapping**（映射）：把逻辑 device-mesh 坐标和通信组落实到具体 GPU、节点与网络层级。→ [[L47 混合并行组装]]
- **micro-batch budget**（微批次预算）：global batch 能分配给 DP replicas 和 pipeline micro-batches 的有限份额。→ [[L47 混合并行组装]]

- **Megatron-LM**（Megatron 大模型训练系统）：提出并实现 Transformer 层内“先列后行”TP 组合的 NVIDIA 研究与软件项目。→ [[L43 张量并行]]
- **model parallelism**（模型并行）：一个模型由多卡共同执行的历史总称，现代应继续区分 TP、PP 等具体切法。→ [[L43 张量并行]]

- **MiCS**（Minimizing Communication Scale，最小化通信规模系统）：通过缩小模型状态切分组和分层同步，降低慢网络上的参与者规模与流量。→ [[L42 ZeRO与FSDP]]

- **master weights**（主参数）：混合精度训练中用于高精度累计更新的 FP32 参数副本。→ [[L40 训练显存全解剖]]
- **memory footprint**（内存占用）：某执行阶段同时占用设备内存的数据总量。→ [[L40 训练显存全解剖]]
- **memory snapshot**（内存快照）：记录 allocator segments、blocks、事件与调用栈，用于定位 OOM 来源。→ [[L50 显存优化技术]]
- **memory wall**（内存墙）：内存容量或带宽先于算力成为扩展瓶颈；L40 主要讨论 HBM 容量墙。→ [[L40 训练显存全解剖]]

- **MSCCL**（微软集合通信库）：支持面向硬件和应用定制 collective algorithm 的平台。→ [[L38 NCCL解剖]]
- **message size regime**（消息大小区间）：由 $n$ 相对 $\alpha/\beta$ 的大小决定时延项还是带宽项主导的区间。→ [[L37 通信算法与代价模型]]

- **makespan**（批次总完工时间）：一批作业中从起点到最后一个作业完成的时间跨度。→ [[L35 GPU集群调度]]
- **MIG (Multi-Instance GPU)**（多实例 GPU）：把支持的 GPU 硬件划分为带 memory/fault isolation 的预定义小实例。→ [[L35 GPU集群调度]]；与 MPS/time-slicing 的隔离权衡见 [[L66 弹性算力与资源效率]]
- **MPS (Multi-Process Service)**（多进程服务）：让多个合作型 CUDA processes 并发共享 GPU 执行资源的 runtime service。→ [[L35 GPU集群调度]]；共享与干扰边界见 [[L66 弹性算力与资源效率]]

- **metadata server**（元数据服务器）：处理 namespace、目录、权限、文件 layout 等 metadata 请求的服务。→ [[L34 存储与数据供给]]

- **match-action**（匹配—动作）：用 header/metadata 查表，并执行与命中项关联的受限 packet actions。→ [[L33 在网计算与智能网卡]]
- **multicast**（组播）：network 从一份输入复制出多份 packets，发送给指定 receiver group。→ [[L33 在网计算与智能网卡]]

- **multi-pathing**（多路径传输）：让一份逻辑通信同时或先后利用多条 network paths。→ [[L32 路由与负载均衡]]

- **MEMS**（微机电系统）：用微型可动结构控制光束或器件状态；Palomar OCS 用可动 mirrors 切换光路。→ [[L31 AI集群网络拓扑]]

- **MR**（内存区域）：经注册、映射稳定并带 NIC access permissions 的内存范围。→ [[L29 RDMA原理]]

- **mice flow**（老鼠流）：数据量小、很快完成且常受固定时延主导的短 flow。→ [[L28 数据中心网络基础]]

- **management network**（管理网）：承载配置、监控、设备管理和故障救援流量的网络。→ [[L27 走进AI数据中心]]
- **megawatt**（兆瓦）：等于 $10^6$ W 的功率单位，常用于描述集群或园区电力规模。→ [[L27 走进AI数据中心]]

- **mantissa**（尾数位）：浮点编码中描述同一指数区间内细分位置、主要控制 precision 的位域。→ [[L24 数值格式]]
- **MXFP**（微缩放浮点）：OCP 定义的 block-scaled 浮点格式家族，让一小块低精度元素共享 scale。→ [[L24 数值格式]]
- **microscaling**（微缩放）：为一小块低精度元素设置共享 scale，以较少元数据换取局部动态范围的表示方式。→ [[L24 数值格式]]

- **memory-bound**（访存受限）：数据访问而非算术工作成为主要性能限制的状态。→ [[L23 Roofline模型]]

- **MMA**（矩阵乘累加）：以 $D=A\times B+C$ 为核心的小块 matrix 指令/运算，Tensor Core 的基本工作形式。→ [[L22 算力度量与MFU]]
- **memory bandwidth**（内存带宽）：单位时间可传输的数据量，常用 GB/s 或 TB/s。→ [[L21 GPU内存体系]]
- **memory coalescing**（合并访存）：让 warp 相邻线程访问相邻地址，以合并底层内存事务。→ [[L21 GPU内存体系]]
- **memory hierarchy**（内存层级）：从寄存器、片上 SRAM、缓存到 HBM、主机 DRAM 和存储介质的分层组织。→ [[L21 GPU内存体系]]
- **Mamba**（选择性状态空间架构）：让状态更新依赖输入，并用硬件感知 scan 实现线性序列处理的 SSM 架构。→ [[L18 注意力变体与长上下文]]
- **multi-head latent attention (MLA)**（多头潜在注意力）：联合压缩 K/V 为低秩 latent，推理时缓存 latent 与位置相关分量的 attention。→ [[L18 注意力变体与长上下文]]
- **multi-query attention (MQA)**（多查询注意力）：多个 Q 头共享唯一一组 K/V，以最激进的头共享减少缓存与带宽。→ [[L18 注意力变体与长上下文]]

- **Mixture-of-Experts (MoE)**（混合专家）：用 router 为每个 token 选择少数 FFN expert 的稀疏模型结构。→ [[L17 MoE混合专家]]

- **max new tokens**（最大新生成 token 数）：调用方允许模型在 prompt 之后最多生成的 token 数。→ [[L13 自回归生成与KV缓存]]
- **model card**（模型卡）：模型仓库的 README.md（及其元数据），记录用途、训练与评测信息；与 config.json 是两个文件。→ [[L12 Transformer全解剖]]
- **machine learning**（机器学习）：用数据拟合带参数函数，使其能对新输入作出预测或生成。→ [[L03 机器学习速成]]
- **masked language model**（掩码语言模型）：遮住输入中的部分 token，再让模型从上下文预测原 token 的预训练目标。→ [[L08 CNN与RNN简史]]
- **MFU**（Model FLOPs Utilization，模型算力利用率）：模型有效计算量占硬件峰值算力的比例，衡量训练效率的黄金指标，大规模训练典型值 30%–45%。→ [[L22 算力度量与MFU]]
- **MMLU**（大规模多任务语言理解）：多学科选择题 benchmark，常用来观察知识覆盖。→ [[L14 预训练]]
- **mini-batch**（小批量）：一次参数更新中共同用于估计 gradient 的一小批样本。→ [[L06 优化器]]
- **micro-batch**（微批次）：单个 rank 一次 forward/backward 实际处理、受显存约束的一小批数据。→ [[L07 训练循环解剖]]；流水线调度见 [[L44 流水线并行]]
- **MLP**（多层感知机）：由多个 linear layer 与 nonlinear activation 交替堆叠的前馈网络。→ [[L04 神经网络与前向传播]]
- **MLSys**（机器学习系统）：研究机器学习 workload 如何高效、可靠运行的交叉领域。→ [[L01 全景图]]
- **model**（模型）：把输入映射为输出的带参数函数。→ [[L03 机器学习速成]]
- **model weights**（模型权重）：训练得到、决定模型计算行为的参数数值。→ [[L01 全景图]]
- **momentum**（动量）：累积历史更新趋势，以加速一致方向并平滑来回震荡。→ [[L06 优化器]]
- **multi-head attention (MHA)**（多头注意力）：在 $h$ 个宽度为 $d_{head}$ 的子空间并行执行 attention，拼接后再做输出投影。→ [[L11 注意力机制]]

## N

- **ns-3**（ns-3 网络仿真器）：面向 Internet systems 的开源 discrete-event network simulator。→ [[L67 测量仿真与评测]]

- **N+1 redundancy**（N+1 冗余）：正常需要 $N$ 个服务单元时额外准备 1 个冗余单元。→ [[L65 推理服务生存性]]
- **nines**（几个 9）：用 availability 百分比中连续的 9 概括高可用目标，如 99.9% 为 three nines。→ [[L65 推理服务生存性]]

- **NPU**（神经网络处理单元）：面向低精度张量算子加速的专用处理器，实际收益受算子、dtype 与 backend 支持限制。→ [[L64 边缘与端云协同]]

- **non-IID**（非独立同分布）：不同参与方的数据分布、数量或标签比例存在系统性差异。→ [[L63 跨域与跨集群训练]]

- **NIXL**（NVIDIA Inference Xfer Library）：抽象异构 memory/storage 与可插拔 transport 的点到点推理数据搬运库。→ [[L60 分布式推理与PD分离]]

- **n-gram / prompt lookup**（n-gram / 提示查找）：从 prompt 或已见上下文的重复片段检索后续 token 作为免模型草稿。→ [[L59 投机解码]]

- **Nsight Systems**（Nsight 系统级剖析器）：把 CPU threads、CUDA API、GPU kernels、memcpy 和 streams 放到同一 timeline。→ [[L52 性能剖析与MFU核算]]
- **Nsight Compute**（Nsight kernel 级剖析器）：针对单个 CUDA kernel 收集 compute、memory、occupancy 与 stall 等细粒度指标。→ [[L52 性能剖析与MFU核算]]

- **node-limited / group-limited routing**（节点受限 / 分组受限路由）：限制 token 可到达的目标 node/group 数，以控制跨域流量。→ [[L46 专家并行与MoE训练]]

- **no_sync**（暂缓梯度同步）：DDP 中跳过若干次 backward 同步、只本地累积梯度的上下文。→ [[L41 数据并行与DDP]]

- **NCCL**（NVIDIA 集合通信库）：面向 NVIDIA GPU、拓扑感知的 collective 与 P2P 通信库。→ [[L38 NCCL解剖]]
- **nccl-tests**（NCCL 测试套件）：检查 NCCL collective 正确性、时延与带宽的官方 benchmark。→ [[L38 NCCL解剖]]
- **NCCL timeout**（NCCL 集合通信超时）：collective 在规定窗口内未完成的症状，报错 rank 未必是根因。→ [[L38 NCCL解剖]]
- **NVMe**（非易失性内存快速接口）：通过 PCIe 等连接 SSD 的低开销块存储协议/接口，本课主要指节点本地高速缓存盘。→ [[L34 存储与数据供给]]
- **NVMe offload**（NVMe 卸载）：将参数、optimizer state 或其他冷数据显式分层放到 NVMe。→ [[L50 显存优化技术]]

- **NVLS**（NVLink SHARP）：在 NVSwitch/NVLink domain 内用硬件 multicast/reduction 加速 NCCL collectives。→ [[L33 在网计算与智能网卡]]
- **NVMe-oF**（基于 fabric 的 NVMe）：把 NVMe command/queue semantics 扩展到 RDMA/TCP network 上的远端 storage protocol。→ [[L33 在网计算与智能网卡]]

- **NIC/HCA**（网卡 / 主机通道适配器）：执行网络 I/O、DMA 和 RDMA transport offload 的主机设备。→ [[L29 RDMA原理]]

- **non-blocking**（无阻塞）：对可容纳的通信需求，内部存在不因 fabric 容量不足而互相阻挡的路径。→ [[L28 数据中心网络基础]]

- **north-south traffic**（南北向流量）：穿越数据中心或集群边界、面向用户或外部网络的流量。→ [[L27 走进AI数据中心]]

- **NUMA**（非一致内存访问）：CPU core 访问不同内存或 I/O 节点的代价不相同的多 socket 体系结构。→ [[L25 节点内互联]]
- **NVLink**（NVIDIA 高速点对点互联）：在 GPU/加速器之间提供高带宽内存访问与 copy 通道的 scale-up 链路。→ [[L25 节点内互联]]
- **NVLink domain**（NVLink 域）：经 NVLink/NVSwitch 形成、可高带宽互访显存的一组 GPU。→ [[L25 节点内互联]]；集群拓扑层级见 [[L31 AI集群网络拓扑]]
- **NVL72**（72-GPU 机柜级 NVLink 系统）：由 36 Grace CPU 与 72 Blackwell GPU 组成的机柜级 NVLink domain。→ [[L25 节点内互联]]
- **NVSwitch**（NVLink 交换芯片）：把多条 NVLink 接成交换式 fabric，使多 GPU 获得高带宽全互联。→ [[L25 节点内互联]]

- **needle-in-a-haystack**（大海捞针测试）：把事实插入长文本不同位置，检查模型能否检索并复述的压力测试。→ [[L18 注意力变体与长上下文]]
- **num_key_value_heads**（KV 头数字段）：模型配置中记录 $h_{kv}$、直接决定常规 KV cache 头数系数的字段。→ [[L18 注意力变体与长上下文]]

- **next-token prediction**（下一 token 预测）：估计 $p(x_{t+1}\mid x_{1:t})$ 的语言模型任务。→ [[L13 自回归生成与KV缓存]]
- **NaN**（非数）：无效浮点结果；一旦进入 loss 或 gradient，往往会继续传播。→ [[L05 反向传播与梯度]]
- **neuron**（神经元）：对输入做加权和与 bias，再通过 activation function 的基本计算单元。→ [[L04 神经网络与前向传播]]
- **num_layers**（层数）：Transformer block 的堆叠数 $L$，config.json 中常写作 num_hidden_layers。→ [[L12 Transformer全解剖]]

## O

- **on-demand instance**（按需实例）：无长期用量承诺、按实际使用支付按需价的计算实例。→ [[L66 弹性算力与资源效率]]
- **Oobleck**（Oobleck 弹性训练系统）：用预生成 pipeline templates 在资源变化后快速选择新计划。→ [[L66 弹性算力与资源效率]]
- **over-commitment**（超卖）：接受的名义资源承诺超过物理容量，依赖峰值错开并配套回收规则。→ [[L66 弹性算力与资源效率]]

- **on-device LLM**（端侧大语言模型）：权重和执行主要驻留在用户终端、无需依赖远端云即可生成的 LLM。→ [[L64 边缘与端云协同]]

- **OpenDiLoCo**（OpenDiLoCo）：DiLoCo 的开源复现与全球分布式训练框架。→ [[L63 跨域与跨集群训练]]
- **outer optimizer**（外层优化器）：聚合各域 pseudo-gradient 后更新共享参数的优化器，DiLoCo 中使用 Nesterov momentum。→ [[L63 跨域与跨集群训练]]

- **observability**（可观测性）：通过 metrics、logs 与 traces 推断 serving 系统内部状态的能力。→ [[L61 推理服务框架与集群]]
- **OpenAI-compatible API**（OpenAI 兼容 API）：兼容常用 OpenAI 端点与主要请求/响应 schema 的接口；不保证全部参数和行为相同。→ [[L61 推理服务框架与集群]]

- **outlier channel**（离群通道）：幅值显著高于多数通道、容易拉宽共享量化范围的 activation channel。→ [[L58 量化推理]]

- **Orca**（Orca 推理系统）：在 OSDI 2022 系统化提出 iteration-level scheduling 与 selective batching 的 serving 系统。→ [[L57 连续批处理与调度]]
- **offline/batch inference**（离线 / 批量推理）：面向已有数据集、主要优化 deadline、吞吐和成本的非交互推理。→ [[L57 连续批处理与调度]]

- **off-policy**（离策略）：训练数据的采集策略与当前被优化策略不一致的状态。→ [[L54 RL后训练系统]]
- **OpenRLHF**（开源 RLHF 框架）：以 Ray 编排 roles、组合 vLLM 与 DeepSpeed，并支持多种放置和时序方式的框架。→ [[L54 RL后训练系统]]

- **online softmax**（在线 softmax）：用运行最大值、指数和与加权和逐块合并 score，无需同时持有完整 attention 行。→ [[L45 序列与上下文并行]]；单卡 attention kernel 见 [[L51 算子优化与FlashAttention]]

- **oneCCL**（oneAPI 集合通信库）：Intel 面向 AI/HPC 工作负载的 collective communication library。→ [[L38 NCCL解剖]]
- **object storage**（对象存储）：以 bucket/key 标识 object、通过 PUT/GET/LIST 等 API 访问的存储模型。→ [[L34 存储与数据供给]]

- **offload**（卸载）：把工作从 host CPU/GPU 迁移到 NIC、DPU、switch 或其他专用 engine。→ [[L33 在网计算与智能网卡]]

- **out-of-order delivery**（乱序到达 / 交付）：后发 packet 因走更快 path 而先于前发 packet 被 receiver 观察。→ [[L32 路由与负载均衡]]

- **OCS (optical circuit switch)**（光电路交换机）：先配置 input-output 光路，再让数据穿过固定 circuit 的交换设备。→ [[L31 AI集群网络拓扑]]
- **oversubscribed spine**（有收敛的脊层）：spine 上行容量小于下层总注入能力，用较低成本保留有限跨 rail 可达性。→ [[L31 AI集群网络拓扑]]

- **one-sided/two-sided**（单边 / 双边操作）：区分远端是否需要为每次传输预贴 receive 并参与消息语义。→ [[L29 RDMA原理]]

- **oversubscription**（收敛比 / 超额订阅）：端点下行可注入带宽与上层可承载带宽之比。→ [[L28 数据中心网络基础]]

- **operator dispatch**（算子派发）：依据 device、dtype、layout 和功能 keys 为一个 op 选择具体实现。→ [[L26 加速器生态与软件栈]]

- **outlier**（离群值）：相对 tensor 主体分布幅值异常大的少量值，会拉坏共享 scale 的格子利用。→ [[L24 数值格式]]
- **overflow**（上溢）：数值结果超过格式最大有限范围的现象。→ [[L24 数值格式]]

- **operator (op)**（算子）：计算图中的语义操作；它与 GPU kernel 不保证一一对应。→ [[L23 Roofline模型]]

- **occupancy**（占用率）：SM 上 active warps 相对可驻留上限的比例，是判断隐藏时延能力的诊断信号。→ [[L20 GPU体系结构入门]]
- **OOM**（Out of Memory，内存不足）：设备无法满足当前分配请求时抛出的错误。→ [[L21 GPU内存体系]]
- **over-training**（过训练 / 过度训练）：固定模型后使用明显多于 compute-optimal 配比的 token，以换取更便宜的推理。→ [[L16 Scaling Law与算力账]]
- **objective**（目标函数）：优化过程要最小化或最大化的整体数学表达式。→ [[L03 机器学习速成]]
- **one-hot**（独热向量）：仅一个位置为 1、其他位置均为 0 的向量，可概念性地表示一次查表。→ [[L10 Token与嵌入]]
- **optimization**（优化）：反复调整参数以改进 objective 的过程。→ [[L03 机器学习速成]]
- **optimizer**（优化器）：把 gradient、学习率和历史状态组合成参数更新的组件。→ [[L06 优化器]]
- **optimizer states**（优化器状态）：optimizer 跨 step 保存的同规模历史 tensor；Adam 中主要是 $m$ 和 $v$。→ [[L06 优化器]]
- **overhead**（额外开销）：为获得新机制收益而新增的计算、通信、存储、时间或工程代价。→ [[L02 如何读系统论文]]
- **overfitting**（过拟合）：训练表现继续改善，但未见数据上的表现变差。→ [[L03 机器学习速成]]

## P

- **patch**（图像小块）：视觉 tokenize 的基本空间单元，数量由预处理分辨率与 patch size 决定。→ [[L70 选修-多模态与新负载]]
- **projector**（投影连接器）：把视觉特征对齐到 LLM hidden space 的连接模块。→ [[L70 选修-多模态与新负载]]

- **packet-level simulation**（包级仿真）：展开 packet、queue、ACK/ECN/PFC、drop 与 retransmission 状态的仿真。→ [[L67 测量仿真与评测]]

- **preemption notice**（抢占通知）：资源回收前尽力发出的信号，用于 drain、checkpoint 或迁移。→ [[L66 弹性算力与资源效率]]
- **preemptible instance**（可抢占实例）：提供方可按契约收回的计算实例；spot 是常见商业形式。→ [[L66 弹性算力与资源效率]]

- **probe**（探针 / 探测请求）：为检查某层或端到端服务健康而发出的主动测试。→ [[L65 推理服务生存性]]

- **Private Cloud Compute**（私有云计算）：Apple 为复杂 Apple Intelligence 请求设计、强调可验证隐私与安全约束的云侧计算架构。→ [[L64 边缘与端云协同]]

- **periodic averaging**（周期平均）：每隔 $H$ 个本地 steps 对各 worker 的参数或更新做一次平均。→ [[L63 跨域与跨集群训练]]
- **power wall**（电力墙）：园区在供电、并网、冷却和建设周期上的综合容量边界。→ [[L63 跨域与跨集群训练]]
- **PowerSGD**（PowerSGD 低秩压缩）：用 power iteration 得到低秩 gradient 因子，并以 all-reduce 友好的形式聚合。→ [[L63 跨域与跨集群训练]]

- **per-request tracing**（逐请求追踪）：用统一 trace/request ID 串起 gateway、router、queue、prefill、KV transfer、decode 与返回阶段。→ [[L61 推理服务框架与集群]]

- **P/D instance**（P/D 实例）：分别只执行 prefill 或 decode、并通过 KV 状态交接请求的 serving engine 实例。→ [[L60 分布式推理与PD分离]]
- **PP for inference**（推理流水线并行）：把连续 layers 放到不同 stages，以较少的跨 stage activation 传输让模型跨机。→ [[L60 分布式推理与PD分离]]
- **prefill-decode disaggregation**（prefill–decode 分离 / PD 分离）：把两个阶段部署到独立 GPU pools，并通过 KV 交接请求状态。→ [[L60 分布式推理与PD分离]]
- **prefill/decode instance ratio**（prefill/decode 实例配比）：P 与 D 实例数之比，由阶段容量、长度分布、硬件和 SLO 共同决定。→ [[L60 分布式推理与PD分离]]

- **PTQ/QAT**（训练后量化 / 量化感知训练）：PTQ 在高精度训练后校准压缩；QAT 在训练中模拟量化效应让参数适应。→ [[L58 量化推理]]
- **pruning**（剪枝）：删除或置零不重要连接或结构的压缩方法，是否加速取决于硬件与 kernel。→ [[L58 量化推理]]

- **prefill-decode interference**（prefill–decode 干扰）：两阶段共用执行资源时，对彼此 TTFT、TPOT 或吞吐造成的影响。→ [[L57 连续批处理与调度]]

- **PagedAttention**（分页式 attention / KV 管理机制）：允许连续逻辑 KV blocks 存在不连续物理显存，并让 kernel 按映射读取。→ [[L56 KV缓存与PagedAttention]]
- **prefix caching**（前缀缓存）：保留已计算前缀的 KV，让相同后续请求跳过重复 prefill。→ [[L56 KV缓存与PagedAttention]]
- **prefix sharing**（前缀共享）：让多请求或多生成分支共同引用一份物理前缀 KV。→ [[L56 KV缓存与PagedAttention]]

- **partial rollout**（部分 rollout）：在完整 trajectory 结束前暂停、截断、保存或消费部分状态的长尾治理机制。→ [[L54 RL后训练系统]]

- **profiling**（性能剖析）：采集一段执行的事件、时长和资源指标，并把端到端症状归因到具体执行层。→ [[L52 性能剖析与MFU核算]]

- **per-tensor scaling**（每 tensor 缩放）：整个 tensor 共用一个 scale 的量化粒度。→ [[L49 混合精度与FP8训练]]

- **parallel plan / strategy**（并行方案 / 策略）：包含 degrees、tensor sharding、process groups、mapping、batch 与 schedule 的完整配置。→ [[L47 混合并行组装]]

- **partial sum**（部分和）：row-parallel GEMM 在单个 rank 上得到的 $X_iW_i$，必须跨 ranks 相加才是完整输出。→ [[L43 张量并行]]

- **pipeline parallelism (PP)**（流水线并行）：把连续层切成多个 stages，并让不同 micro-batches 流水执行的模型并行方式。→ [[L44 流水线并行]]
- **pipeline bubble**（流水线气泡）：因 stage 依赖与流水线填充、排空而形成的设备空闲时隙。→ [[L44 流水线并行]]
- **PipeDream**（PipeDream 流水系统）：提出 1F1B 等机制并处理双向训练流水、权重版本与 stage partition 的系统。→ [[L44 流水线并行]]
- **P2P activation transfer**（点对点 activation 传输）：相邻 stages 在 forward 传 activation、backward 传输入梯度的 send/recv。→ [[L44 流水线并行]]
- **pipeline flush**（流水线排空）：在参数更新或下一批开始前，让当前所有 micro-batches 离开流水线。→ [[L44 流水线并行]]

- **partitioning / sharding**（分区 / 切分）：把一个逻辑状态沿 ranks 分配所有权，使每个 rank 只常驻其中一片。→ [[L42 ZeRO与FSDP]]

- **parameter server**（参数服务器）：由 servers 持有全局参数、workers 拉参数并推更新的分布式训练架构。→ [[L41 数据并行与DDP]]

- **per-parameter bytes**（每参数字节记账）：把每种同参数规模 tensor 的 dtype 字节相加，快速估算训练静态显存。→ [[L40 训练显存全解剖]]

- **protocol**（NCCL 设备端传输协议）：规定 chunk 怎样搬运、标记就绪和流水，不等同于 TCP/InfiniBand。→ [[L38 NCCL解剖]]
- **proxy thread**（代理进度线程）：CPU 侧推进网络连接和 transport 请求、与 GPU kernel 协作的线程。→ [[L38 NCCL解剖]]
- **pipelining**（流水化）：让多个 chunk 同时处在通信算法的不同阶段。→ [[L37 通信算法与代价模型]]

- **point-to-point (P2P)**（点到点通信）：在指定发送 rank 与接收 rank 之间传数据的通信模式。→ [[L36 集合通信原语]]
- **primitive**（原语）：上层直接调用、规定基本语义而不固定底层算法的通信单元。→ [[L36 集合通信原语]]
- **process group**（进程组）：框架中供 collective/P2P 使用的一组 ranks 及其通信后端。→ [[L36 集合通信原语]]；多维 mesh 中的组装见 [[L47 混合并行组装]]

- **partition**（分区/队列）：Slurm 中组织一组节点并承载准入、时限等策略的逻辑资源集合。→ [[L35 GPU集群调度]]
- **preemption**（抢占）：高优先级工作收回低优先级工作的资源；推理中可暂停 active request，并以 swap 或 recomputation 恢复。→ [[L35 GPU集群调度]]、[[L57 连续批处理与调度]]

- **page cache**（页缓存）：操作系统用 DRAM 缓存文件页、减少重复块设备或远端读取的机制。→ [[L34 存储与数据供给]]
- **parallel filesystem**（并行文件系统）：让多 clients 共享文件命名空间，并把 I/O 并行分散到多 storage targets 的文件系统。→ [[L34 存储与数据供给]]
- **POSIX semantics**（POSIX 文件语义）：向应用提供层次文件名与 open/read/write/stat/rename 等熟悉文件接口及其约定。→ [[L34 存储与数据供给]]

- **P4**（P4 数据面编程语言）：描述 network device 如何解析和处理 packets 的领域专用语言。→ [[L33 在网计算与智能网卡]]
- **programmable switch**（可编程交换机）：允许编程部分 parser、match-action 与 forwarding behavior 的线速 switch。→ [[L33 在网计算与智能网卡]]

- **packet spraying**（包喷洒）：把同一 flow 的 packets 分散到多条 paths 的细粒度转发。→ [[L32 路由与负载均衡]]
- **per-packet load balancing**（逐包负载均衡）：以 packet 为选择路径单位的负载均衡，与 packet spraying 近义。→ [[L32 路由与负载均衡]]

- **path diversity**（路径多样性）：端点间可利用的不同路径及其瓶颈独立程度。→ [[L31 AI集群网络拓扑]]
- **PXN**（PCIe × NVLink 网络中转）：NCCL 通过 NVLink 中间 GPU 使用 non-local NIC，使流量留在目标 rail 并可聚合。→ [[L31 AI集群网络拓扑]]

- **PFC deadlock**（PFC 死锁）：queue 形成循环 buffer 依赖，彼此等待 RESUME 而无人能前进。→ [[L30 无损网络与拥塞控制]]
- **PFC storm**（PFC 暂停风暴）：PAUSE 长时间、高频或跨多跳扩散并造成大范围吞吐下降。→ [[L30 无损网络与拥塞控制]]
- **priority/traffic class**（优先级 / 流量类别）：PCP 提供 8 个 priority 值，设备再把它们映射到实际 traffic class 与 queue。→ [[L30 无损网络与拥塞控制]]

- **packet switching**（分组交换）：把数据切成 packet，让多条通信共享链路并按 header 逐跳转发。→ [[L28 数据中心网络基础]]
- **propagation delay**（传播时延）：信号跨越距离 $d$、以介质速度 $v$ 传播所需的 $d/v$ 时间。→ [[L28 数据中心网络基础]]

- **pod**（资源舱 / 集群单元）：一组可重复部署、网络与运维边界相对清晰的 racks/servers，规模由具体设计定义。→ [[L27 走进AI数据中心]]
- **power density**（功率密度）：单个 rack 或单位机房面积承载的 IT 功率，使用时必须注明分母。→ [[L27 走进AI数据中心]]
- **PUE**（电能使用效率）：数据中心总功率或能量除以 IT 设备功率或能量。→ [[L27 走进AI数据中心]]

- **PTX**（CUDA 虚拟指令集）：面向虚拟 compute architecture、可再编译到具体 GPU binary 的中间表示。→ [[L26 加速器生态与软件栈]]

- **precision**（精度）：数值格式可表示格点的细密程度；浮点的绝对间隔会随 exponent 改变。→ [[L24 数值格式]]
- **per-tensor**（每 tensor 粒度）：整个 tensor 共用一个 scale 的量化粒度。→ [[L24 数值格式]]
- **per-channel**（每通道粒度）：每个 channel 独立使用一个 scale 的量化粒度。→ [[L24 数值格式]]
- **per-group**（每组粒度）：每组若干元素共享一个 scale、在精度与元数据开销间折中的量化粒度。→ [[L24 数值格式]]

- **peak FLOPS**（峰值浮点吞吐）：指定 GPU、dtype、执行单元与 dense/sparse 条件下的理论最高 FLOPS。→ [[L22 算力度量与MFU]]
- **padding waste**（padding 浪费）：padding 占用计算和 activation 槽位，却不贡献有效训练 token。→ [[L50 显存优化技术]]

- **PCIe**（外设组件互连高速总线）：连接 CPU、GPU、NIC 等设备的通用 I/O 通道。→ [[L21 GPU内存体系]]；节点内拓扑见 [[L25 节点内互联]]
- **PCIe bottleneck**（PCIe 瓶颈）：offload 数据在关键路径上的搬运速率受 PCIe 限制。→ [[L50 显存优化技术]]
- **PCIe switch**（PCIe 交换芯片）：在 PCIe 树中扩展端口并转发事务，路径与共享上行会影响设备间性能。→ [[L25 节点内互联]]
- **pinned/page-locked memory**（固定/页锁定内存）：不被操作系统换页的 host 内存，适合 DMA 和异步拷贝。→ [[L21 GPU内存体系]]
- **power law**（幂律）：形如 $x^{-\alpha}$ 的尺度关系，在对数坐标上近似直线。→ [[L16 Scaling Law与算力账]]
- **prefill**（预填充阶段）：并行处理完整 prompt、创建初始 KV cache 并产出首 token 所需 logits 的阶段。→ [[L13 自回归生成与KV缓存]]
- **perplexity**（困惑度）：平均负对数似然的指数，衡量模型对评测文本 next token 的不确定程度。→ [[L13 自回归生成与KV缓存]]
- **positional encoding**（位置编码）：向模型注入 token 顺序信息的方法统称。→ [[L12 Transformer全解剖]]
- **pre-norm**（前置归一化）：把 Norm 放在子层之前、残差主干保持干净的结构；现代 LLM 标配。→ [[L12 Transformer全解剖]]
- **PAD**（填充 token）：为把同一 batch 的序列补齐到相同长度而使用的 token。→ [[L10 Token与嵌入]]
- **parameter**（参数）：训练过程能够调整、并决定模型行为的数值。→ [[L03 机器学习速成]]
- **PEFT**（参数高效微调）：冻结大部分 base 参数、只训练少量附加参数的微调范式。→ [[L15 后训练与对齐]]
- **percentile (p50/p99)**（百分位数）：p50 表示约 50% 样本不超过该值，p99 表示约 99% 样本不超过该值。→ [[L02 如何读系统论文]]
- **PFC**（Priority Flow Control，优先级流控）：以太网逐跳反压机制，实现无损传输，但可能引发 PFC storm 与死锁。→ [[L30 无损网络与拥塞控制]]
- **pooling**（池化）：用固定规则压缩局部区域、降低空间尺寸的操作，如 max pooling。→ [[L08 CNN与RNN简史]]
- **pretraining**（预训练）：用大规模通用数据学习基础能力的训练阶段。→ [[L01 全景图]]、[[L14 预训练]]
- **preference data**（偏好数据）：同一 prompt 下带有 chosen/rejected 或排序标注的候选回答。→ [[L15 后训练与对齐]]
- **post-training**（后训练）：在预训练 base model 上继续训练，以改变指令遵循、偏好或推理行为。→ [[L15 后训练与对齐]]
- **PPO**（近端策略优化）：通过限制策略单步变化来稳定利用 reward 更新 actor 的算法。→ [[L15 后训练与对齐]]
- **prefetch**（预取）：在当前工作计算期间提前准备后续数据或参数，以隐藏数据准备/通信时延。→ [[L07 训练循环解剖]]；参数通信语境见 [[L42 ZeRO与FSDP]]
- **probability distribution**（概率分布）：对候选结果分配非负概率且总和为 1 的数值集合。→ [[L04 神经网络与前向传播]]

## Q

- **quality-latency-cost tradeoff**（质量—时延—成本权衡）：联合比较模型质量、用户时延、云成本与端侧资源的多目标取舍。→ [[L64 边缘与端云协同]]

- **QoS (Quality of Service)**（服务质量策略）：在 Slurm 等系统中表达优先级、限制、用量与抢占关系的策略对象。→ [[L35 GPU集群调度]]
- **queueing delay**（排队时延）：从作业或请求提交/到达到真正开始接受服务之间的墙钟时间。→ [[L35 GPU集群调度]]；推理语境见 [[L55 推理性能模型]]
- **quota**（配额）：限定或保障用户、团队、队列可使用资源份额的策略。→ [[L35 GPU集群调度]]

- **QP scaling**（QP 扩展）：为同一 peer relation 使用多个 QPs，增加可被 hash 或调度的 flows。→ [[L32 路由与负载均衡]]

- **QP**（队列对）：由 send queue 与 receive queue 组成的 RDMA 通信端点对象。→ [[L29 RDMA原理]]

- **quantization**（量化）：把连续浮点值映射到有限离散码、计算时再近似还原的过程。→ [[L24 数值格式]]；推理部署见 [[L58 量化推理]]
- **quantization tax**（量化税）：量化引入的质量、元数据、反量化、kernel 限制和工程转换等全部代价。→ [[L58 量化推理]]

- **QKV**（查询、键、值）：Query、Key、Value 的合称；同源输入通常经三组不同投影生成它们。→ [[L11 注意力机制]]
- **quadratic complexity**（平方复杂度）：工作量或存储量随序列长度 $S$ 按 $S^2$ 增长的性质。→ [[L11 注意力机制]]
- **query**（查询）：表示当前位置想匹配什么信息的向量。→ [[L11 注意力机制]]

## R

- **RAG**（Retrieval-Augmented Generation，检索增强生成）：先从外部知识库取材料，再把材料加入上下文进行生成。→ [[L70 选修-多模态与新负载]]
- **rerank**（重排序）：用更精细的模型重新打分首阶段候选，再选择进入 prompt 的材料。→ [[L70 选修-多模态与新负载]]
- **retrieval latency budget**（检索时延预算）：端到端 SLO 中分给 embedding、检索、取文档与 rerank 的时间份额。→ [[L70 选修-多模态与新负载]]

- **reproducibility**（可复现性）：他人能依据 artifacts、配置、版本和随机性说明重新得到相符结果的性质。→ [[L67 测量仿真与评测]]

- **re-configuration**（重新配置 / 弹性重配）：资源变化后重建成员、communicator、mesh、状态布局与 batch/data 语义。→ [[L66 弹性算力与资源效率]]
- **redundant computation**（冗余计算）：健康期多算可接管工作，以降低故障后的停顿或恢复代价。→ [[L66 弹性算力与资源效率]]
- **reserved instance**（预留 / 承诺用量实例）：用期限、用量或容量承诺换取价格或供给安排的实例。→ [[L66 弹性算力与资源效率]]

- **region outage**（地域级中断）：一个 region 的服务或关键依赖大范围不可用的事件。→ [[L65 推理服务生存性]]
- **request retry**（请求重试）：在错误或超时后重新提交同一逻辑操作。→ [[L65 推理服务生存性]]
- **retry storm**（重试风暴）：大量客户端或上游在故障时同步重试，进一步压垮剩余容量的反馈循环。→ [[L65 推理服务生存性]]
- **RPO**（恢复点目标）：恢复时最多允许丢失到多旧状态的目标边界。→ [[L65 推理服务生存性]]
- **RTO**（恢复时间目标）：从故障发生到恢复规定服务级别所允许的最长目标时间。→ [[L65 推理服务生存性]]

- **routing (model selection)**（路由，模型选择语义）：依据请求、质量和系统状态选择模型与执行位置，而非只选同构实例。→ [[L64 边缘与端云协同]]

- **router (serving)**（服务路由器）：在健康模型池、实例或地域之间为推理请求选择下一跳。→ [[L61 推理服务框架与集群]]

- **rejection sampling**（拒绝采样）：以 $\min(1,p/q)$ 接受候选，并在拒绝时从 $[p-q]_+$ 残差分布补样本。→ [[L59 投机解码]]

- **request-level/static batching**（请求级 / 静态批处理）：批成员在整条生成请求完成前固定不变的 batching 方式。→ [[L57 连续批处理与调度]]

- **RadixAttention**（基数树前缀缓存机制）：SGLang 用 radix tree 自动匹配、复用和淘汰 KV 前缀的运行时方案。→ [[L56 KV缓存与PagedAttention]]
- **radix tree**（基数树 / 压缩前缀树）：用可变长 token 串标记边、紧凑表示公共前缀的索引结构。→ [[L56 KV缓存与PagedAttention]]

- **reasoning workload**（推理型长输出负载）：把较多 test-time compute 花在自回归生成、输出常更长的 workload。→ [[L55 推理性能模型]]
- **RPS**（Requests Per Second，每秒请求数）：单位时间接纳或完成的请求数，必须注明统计口径。→ [[L55 推理性能模型]]

- **replay buffer**（经验回放缓冲区）：保存待消费或可复用 trajectories、解耦 rollout 与 trainer 速率的数据缓冲层。→ [[L54 RL后训练系统]]
- **resharding**（重新切分）：在不同 TP/PP/DP/FSDP owners 或 layouts 之间聚合、重排并重新分发 tensor shards。→ [[L54 RL后训练系统]]
- **RLHF system**（RLHF 系统）：协调 rollout、打分、experience 构造与策略训练的多模型分布式系统。→ [[L54 RL后训练系统]]
- **rollout**（轨迹采样 / 生成轨迹）：actor 对 prompt 或环境逐步采样得到的 tokens、动作、logprob 与反馈记录。→ [[L54 RL后训练系统]]
- **rollout long-tail**（rollout 长尾）：trajectory 生成或交互时长呈高方差、少数极长样本主导尾部的现象。→ [[L54 RL后训练系统]]
- **rollout straggler**（rollout 掉队样本）：在同步边界内完成最晚、迫使其他请求或 trainer 等待的 trajectory。→ [[L54 RL后训练系统]]

- **row-parallel linear**（行并行线性层）：沿 weight 输入维切分，各 rank 产生同形输出的 partial sum，随后求和。→ [[L43 张量并行]]

- **Ring Attention**（环式注意力）：本地 Q 固定、KV blocks 沿逻辑环轮转并以 blockwise attention 累积 exact 输出。→ [[L45 序列与上下文并行]]

- **redundancy**（冗余）：多个 data-parallel ranks 保存逻辑相同状态副本所形成的重复占用。→ [[L42 ZeRO与FSDP]]

- **replica**（副本）：驻留在一个 rank 上、结构和初始状态相同的完整模型实例。→ [[L41 数据并行与DDP]]

- **residency**（驻留性）：数据在某段执行时间内必须真实保留在目标内存层级的要求。→ [[L40 训练显存全解剖]]

- **RCCL**（ROCm 集合通信库）：面向 AMD GPU/ROCm 生态的高性能 collective library。→ [[L38 NCCL解剖]]
- **rendezvous**（会合）：ranks 在 communicator 创建前交换组身份、地址或 ID 等元数据的初始化阶段。→ [[L38 NCCL解剖]]
- **Rabenseifner**（Rabenseifner 算法）：将 recursive halving reduce-scatter 与 recursive doubling all-gather 组合的带宽最优路线。→ [[L37 通信算法与代价模型]]
- **recursive halving/doubling**（递归减半 / 翻倍）：先用消息递减的 RS，再用消息递增的 AG 完成 all-reduce。→ [[L37 通信算法与代价模型]]
- **ring algorithm**（环形算法）：ranks 沿逻辑环逐块 reduce-scatter 再 all-gather 的算法。→ [[L37 通信算法与代价模型]]

- **rank**（进程编号）：一个进程在特定 communicator/process group 内的逻辑编号。→ [[L36 集合通信原语]]
- **reduce**（规约）：各 rank 的同形数据经 operator 合并，结果只交给一个 root。→ [[L36 集合通信原语]]
- **reduce-scatter**（规约-分散）：先逐元素规约各 rank 的贡献，再把结果按 rank 分片。→ [[L36 集合通信原语]]
- **reduction operator**（规约算子）：定义多个对应元素怎样合并的运算，如 sum 或 max。→ [[L36 集合通信原语]]

- **reservation**（预留）：为指定主体和时间窗保留资源、保护未来启动承诺的机制。→ [[L35 GPU集群调度]]

- **restore**（恢复加载）：将 checkpoint 的模型、优化器和训练进度装回 workers 以重新开始训练。→ [[L34 存储与数据供给]]

- **reduction tree**（归约树）：leaves 贡献 inputs、中间节点合并 partial results、root 产生最终结果的 collective 结构。→ [[L33 在网计算与智能网卡]]
- **RMT**（可重构匹配表）：由多级可配置 match-action stages 组成的高速 programmable dataplane 架构。→ [[L33 在网计算与智能网卡]]

- **reordering**（重排）：缓存并按 sequence 或 offset 恢复所需逻辑顺序。→ [[L32 路由与负载均衡]]

- **RDMA**（远程直接内存访问）：NIC 直接访问另一主机预先注册授权的内存，让远端 CPU 不参与每次数据搬运。→ [[L29 RDMA原理]]
- **RDMA read/write**（RDMA 读 / 写）：发起方主动从远端 MR 拉取或向远端 MR 推送的 one-sided 操作。→ [[L29 RDMA原理]]
- **RC/UC/UD**（可靠连接 / 不可靠连接 / 不可靠数据报）：三类 QP transport service，可靠性、连接状态与支持操作不同。→ [[L29 RDMA原理]]
- **RoCE/RoCEv2**（融合以太网上的 RDMA）：在 Ethernet 上承载 RDMA transport；v2 用 UDP/IP 实现三层可路由封装。→ [[L29 RDMA原理]]

- **radix**（端口基数）：一台交换机可用于连接端点或其他交换机的端口数量。→ [[L28 数据中心网络基础]]
- **RTT**（往返时延）：请求到达对端并由响应或确认返回的总时间。→ [[L28 数据中心网络基础]]

- **rack**（机柜）：安装服务器、交换机、配电与线缆管理部件的竖直框架。→ [[L27 走进AI数据中心]]
- **region**（区域）：云厂商定义的独立地理区域，通常包含多个 availability zones。→ [[L27 走进AI数据中心]]

- **ROCm**（AMD 开放计算平台）：包含 HIP、libraries、compiler、runtime 与工具的 AMD GPU 软件栈。→ [[L26 加速器生态与软件栈]]

- **rail**（网络轨道）：多节点中把相同位置 NIC 接到同组 leaf，形成重复的并行网络路径。→ [[L25 节点内互联]]；完整拓扑见 [[L31 AI集群网络拓扑]]
- **rail-optimized topology**（轨道优化拓扑）：优先让同号 GPU/NIC 的高频跨节点通信留在同一 rail。→ [[L31 AI集群网络拓扑]]
- **rail-only**（纯轨道网络）：删除跨 rail spine、以 NVLink domain 内 relay 承担跨 rail 通信的低成本设计。→ [[L31 AI集群网络拓扑]]

- **ridge point**（屋脊点）：Roofline 中带宽斜坡与算力屋顶的交点，横坐标为 $P_{\text{peak}}/BW$。→ [[L23 Roofline模型]]
- **roofline model**（屋顶线模型）：用算力屋顶与带宽斜坡的较低者给出性能上限的瓶颈模型。→ [[L23 Roofline模型]]

- **register file**（寄存器堆）：每个 thread 私有的最快片上存储，容量受限。→ [[L21 GPU内存体系]]
- **reserved vs allocated**（保留量与活跃分配量）：reserved 是缓存池总量；allocated 是当前活跃 tensor 的量。→ [[L21 GPU内存体系]]；诊断流程见 [[L50 显存优化技术]]
- **RoPE scaling**（RoPE 缩放）：调整位置映射或频率，使 RoPE 模型能处理训练窗口之外的位置。→ [[L18 注意力变体与长上下文]]

- **router/gating network**（路由器/门控网络）：为每个 token 对所有 expert 打分并产生选择权重的小网络。→ [[L17 MoE混合专家]]

- **residual stream**（残差流）：贯穿全部层、被各子层读取并累加写回的主干表示。→ [[L12 Transformer全解剖]]
- **reference model**（参考模型）：冻结的 SFT 或旧策略副本，用作 KL 约束的行为锚点。→ [[L15 后训练与对齐]]
- **reasoning model**（推理模型）：经过训练、倾向于在回答前投入更多推理 token 的模型。→ [[L15 后训练与对齐]]
- **reward model**（奖励模型）：从偏好数据学习、为回答输出标量分数的模型。→ [[L15 后训练与对齐]]
- **RLHF**（人类反馈强化学习）：用人类偏好构造反馈，再以强化学习更新生成策略。→ [[L15 后训练与对齐]]
- **RLVR / verifiable reward**（可验证奖励强化学习 / 可验证奖励）：用数学检查器、编译器或测试等 verifier 产生奖励。→ [[L15 后训练与对齐]]
- **RMSNorm**（均方根归一化）：去掉均值中心化、只按均方根缩放的轻量归一化，现代 LLM 标配。→ [[L12 Transformer全解剖]]
- **RoPE**（旋转位置编码）：按位置旋转 Q/K 的维度对，使点积只依赖相对距离的位置编码；不增加参数。→ [[L12 Transformer全解剖]]
- **random seed**（随机种子）：指定伪随机数生成器的起始状态，以提高实验可复现性。→ [[L07 训练循环解剖]]
- **ReLU**（修正线性单元）：$\max(0,z)$，把负输入截为 0、正输入原样保留的 activation function。→ [[L04 神经网络与前向传播]]
- **regression**（回归）：预测连续数值的任务。→ [[L03 机器学习速成]]
- **reinforcement learning**（强化学习）：通过动作、环境反馈和 reward 学习决策的范式。→ [[L03 机器学习速成]]
- **residual connection**（残差连接）：将块输入直接加到变换结果上的连接，常写作 $y=F(x)+x$。→ [[L08 CNN与RNN简史]]
- **ResNet**（残差网络）：以残差块和 residual connection 为核心的深层 CNN 家族。→ [[L08 CNN与RNN简史]]
- **resume**（续训）：从 checkpoint 恢复模型、优化器和进度等状态后继续训练。→ [[L07 训练循环解剖]]
- **RNN**（循环神经网络）：通过重复更新 hidden state 来处理序列的神经网络。→ [[L08 CNN与RNN简史]]

## S

- **session**（会话）：跨多轮请求持续存在的 history、KV、工具与控制状态集合。→ [[L70 选修-多模态与新负载]]
- **step distillation**（步数蒸馏）：把教师的多步生成轨迹压缩成学生的少数在线步骤。→ [[L70 选修-多模态与新负载]]

- **sensitivity analysis**（敏感性分析）：扫描关键参数，检查结论稳健性与翻转边界。→ [[L67 测量仿真与评测]]
- **SimAI**（SimAI 大模型训练仿真器）：面向大规模 LLM 训练、组合 framework/kernel/collective 模型的全栈 simulator。→ [[L67 测量仿真与评测]]
- **simulator**（仿真器）：按明确状态、规则和参数重现目标系统行为的程序化模型。→ [[L67 测量仿真与评测]]

- **serverless inference**（无服务器推理）：平台隐藏实例生命周期并按调用或资源计费，通过共享池处理扩缩与冷启动。→ [[L61 推理服务框架与集群]]、[[L66 弹性算力与资源效率]]
- **spot instance**（spot / 竞价实例）：使用可回收剩余容量、价格较低但可能被提供方中断的实例。→ [[L66 弹性算力与资源效率]]

- **session migration**（会话迁移）：把会话绑定及必要 history/KV 状态转到新的执行位置。→ [[L65 推理服务生存性]]
- **SLO violation rate**（SLO 违约率）：适用请求中错误、超时或按规定拒绝而未满足 SLO 的比例。→ [[L65 推理服务生存性]]

- **session continuity**（会话连续性）：用户移动、切网或实例切换后仍保留上下文与交互状态的能力。→ [[L64 边缘与端云协同]]
- **speculative edge-cloud inference**（端云投机推理）：端侧小模型生成 draft、云侧 target 跨网验证，以 token payload 换取更少 target 串行调用或更多云容量。→ [[L64 边缘与端云协同]]
- **split inference / model partitioning**（切分推理 / 模型切分）：把同一模型的不同层或模块放在端与云，并在切点交换中间结果。→ [[L64 边缘与端云协同]]

- **serving stack**（推理服务栈）：从 API 入口、路由、编排、引擎到模型与观测的分层在线系统。→ [[L61 推理服务框架与集群]]
- **session affinity**（会话亲和）：让同一多轮会话尽量回到保存其 KV/history 状态的实例。→ [[L61 推理服务框架与集群]]
- **sticky routing**（粘性路由）：用显式映射或 hash 实现会话/用户到实例的稳定绑定。→ [[L61 推理服务框架与集群]]
- **S-LoRA**（S-LoRA 多 adapter serving 系统）：以 Unified Paging、heterogeneous batching 和专用 TP 路径扩展并发 LoRA adapter 服务。→ [[L61 推理服务框架与集群]]

- **speculative decoding**（投机解码）：用廉价 proposer 先猜多个 token，再由 target 并行验证，以额外并发计算换更少串行 target calls。→ [[L59 投机解码]]
- **self-speculative decoding**（自投机解码）：用 target 自身的浅层、跳层或其他廉价子路径生成候选，再由完整 target 验证。→ [[L59 投机解码]]
- **speculation length $k$**（投机长度）：每轮预先生成的最大 draft token 数，是接受收益与 draft/verification 成本的核心旋钮。→ [[L59 投机解码]]

- **SmoothQuant**（平滑量化）：用等价 channel scaling 把 activation outlier 的量化难度迁移到 weights，以支持 W8A8。→ [[L58 量化推理]]

- **Sarathi / Sarathi-Serve**（Sarathi-Serve 推理系统）：以 chunked-prefills 和 stall-free scheduling 治理吞吐–时延权衡的系统。→ [[L57 连续批处理与调度]]
- **scheduling policy**（调度策略）：决定 waiting/running 请求下一步获得哪些执行与 KV 资源的规则。→ [[L57 连续批处理与调度]]
- **SLO tier**（SLO 分级）：按 interactive、batch 等类别为请求设置不同服务目标与优先级。→ [[L57 连续批处理与调度]]

- **SGLang**（SGLang 推理系统）：集成 RadixAttention、调度和结构化生成能力的开源 serving 系统。→ [[L56 KV缓存与PagedAttention]]
- **swap vs recomputation**（换出与重计算）：抢占后在“搬回旧 KV”和“用 tokens 重建 KV”之间选择成本更低者。→ [[L56 KV缓存与PagedAttention]]

- **saturation**（饱和）：增加 offered load 不再提高完成吞吐、只继续推高排队与时延的区间。→ [[L55 推理性能模型]]
- **service rate ($\mu$)**（服务率）：固定配置与 workload 下，服务台长期平均可完成的请求数。→ [[L55 推理性能模型]]
- **SLA**（Service Level Agreement，服务级别协议）：包含服务目标及未达标后果的用户协议。→ [[L55 推理性能模型]]
- **stability condition ($\rho<1$)**（稳定条件）：长期平均到达率小于服务率时，简单队列才不会无界增长。→ [[L55 推理性能模型]]

- **single controller**（单控制器）：从一个逻辑控制入口编排全局 RL dataflow、资源、版本与跨 worker 数据移动。→ [[L54 RL后训练系统]]

- **SDC**（静默数据损坏）：数据或计算已经错误，却没有同步错误信号。→ [[L53 大规模训练可靠性]]

- **SDPA**（scaled dot-product attention 入口）：PyTorch 中按输入条件 dispatch 到 fused 或 math backend 的标准 attention API。→ [[L51 算子优化与FlashAttention]]
- **SRAM tiling**（SRAM 分块）：让工作集按 tile 进入片上 SRAM/shared memory，减少 HBM 往返。→ [[L51 算子优化与FlashAttention]]

- **selective recomputation**（选择性重计算）：只重算占内存大、相对便宜的子图，保留重算昂贵的结果。→ [[L50 显存优化技术]]

- **sequence parallelism**（序列并行）：被多套系统复用的名称；可能指 Megatron 的非 TP 区域切分，也可能指 Ulysses 式长序列 attention，须结合 tensor layout 与通信辨析。→ [[L43 张量并行]]、[[L45 序列与上下文并行]]

- **stage**（流水段）：流水线并行中一段连续层及其计算、参数与本地 activation 的执行单元。→ [[L44 流水线并行]]
- **steady**（流水线稳态段）：warmup 后尽可能交替 forward/backward、各 stages 接近持续忙碌的阶段。→ [[L44 流水线并行]]
- **stage balancing**（流水段负载均衡）：按实测时间、FLOPs 与特殊层开销切分 stages，使最慢段不过度拖慢流水节拍。→ [[L44 流水线并行]]

- **server**（参数节点）：parameter server 中持有和更新全局参数、并可按 key range 切分的服务角色。→ [[L41 数据并行与DDP]]
- **staleness**（陈旧度）：worker 计算梯度所用参数版本相对 server 当前版本的落后程度。→ [[L41 数据并行与DDP]]
- **synchronous SGD**（同步 SGD）：所有 workers 完成本轮并聚合梯度后，才共同进入下一次参数更新。→ [[L41 数据并行与DDP]]

- **static/model states**（静态项 / 模型状态）：容量主要由参数量决定的参数、梯度及 optimizer 相关状态；“静态”不表示值不更新。→ [[L40 训练显存全解剖]]

- **Simple**（简单协议）：元数据比例低、偏向大消息稳态带宽的 NCCL protocol。→ [[L38 NCCL解剖]]
- **scatter**（分散）：把 root 的全量数据按 rank 切片并分给各 rank。→ [[L36 集合通信原语]]
- **SPMD**（单程序多数据）：多个进程运行同一程序，并按 rank 处理不同数据或分支。→ [[L36 集合通信原语]]

- **Slurm**（Slurm 工作负载管理器）：HPC/批处理环境常用的资源与作业管理系统。→ [[L35 GPU集群调度]]
- **starvation**（饥饿）：作业因持续被插队或缺少完整资源窗口而长期得不到服务。→ [[L35 GPU集群调度]]

- **S3**（S3 对象存储语义）：Amazon S3 的 bucket/object/key API；general purpose bucket 使用扁平 key 空间。→ [[L34 存储与数据供给]]
- **sequence packing**（序列装箱）：将多个短样本装入固定长度训练序列以减少 padding，同时保留必要边界信息。→ [[L34 存储与数据供给]]；显存收益见 [[L50 显存优化技术]]
- **sharded checkpoint**（分片检查点）：各 ranks 保存自己持有的 state shards，并由 manifest 组成一个逻辑版本。→ [[L34 存储与数据供给]]
- **striping**（条带化）：把一个文件按 chunks 分布到多个 storage targets，以取得并行带宽。→ [[L34 存储与数据供给]]

- **SHARP**（可扩展分层聚合与归约协议）：NVIDIA InfiniBand 生态中把 collective reduction offload 到 fabric aggregation nodes 的技术。→ [[L33 在网计算与智能网卡]]
- **SmartNIC**（智能网卡）：在 NIC 上加入可编程处理能力、可 offload network/storage/security functions 的设备类别。→ [[L33 在网计算与智能网卡]]
- **streaming aggregation**（流式聚合）：将大 tensor 切成 chunks，在有限片上 state 中边到达、边归约、边转发。→ [[L33 在网计算与智能网卡]]
- **SwitchML**（交换机内 ML 聚合系统）：以 switch–host co-design、定点整数转换和片上 slots 聚合 distributed training gradients。→ [[L33 在网计算与智能网卡]]

- **Spectrum-X**（NVIDIA AI Ethernet 网络平台）：组合 Spectrum switches、SuperNIC/DPU 与软件，并提供 adaptive routing 等能力。→ [[L32 路由与负载均衡]]

- **Slingshot**（HPE HPC interconnect）：采用 Dragonfly、adaptive routing 与 congestion management 的 HPC 网络产品族。→ [[L31 AI集群网络拓扑]]

- **selective retransmission**（选择性重传）：只重发确认缺失的 packet，并保留/记录已到达的乱序 packet。→ [[L30 无损网络与拥塞控制]]
- **send/recv**（发送 / 接收）：需要发送方和接收方分别 post 匹配操作的 two-sided 消息模式；RDMA 语境中对应 post WR。→ [[L29 RDMA原理]]；集合通信对照见 [[L36 集合通信原语]]
- **subnet manager**（子网管理器）：发现、激活并配置 IB subnet 路径、地址、partition 与 QoS 的管理实体。→ [[L29 RDMA原理]]

- **serialization delay**（串行化时延）：把 $L$ bit 数据压上速率 $R$ 链路所需的 $L/R$ 时间。→ [[L28 数据中心网络基础]]
- **spine**（脊交换机）：汇聚所有 leaf、为 leaf 间通信提供等价上行路径的交换机。→ [[L28 数据中心网络基础]]
- **synchronized traffic**（同步流量）：多个发送者受共同 step 或 barrier 触发、在相近时刻开始的流量。→ [[L28 数据中心网络基础]]

- **storage network**（存储网）：承载 dataset、checkpoint 和模型文件读写流量的网络。→ [[L27 走进AI数据中心]]

- **SASS**（GPU 原生指令的汇编表示）：对应具体 SM 架构、最终由 NVIDIA GPU 执行的机器指令层。→ [[L26 加速器生态与软件栈]]
- **systolic array**（脉动阵列）：让数据按节拍穿过 PE 网格并就地乘加累积的规则数据流结构。→ [[L26 加速器生态与软件栈]]

- **sign**（符号位）：浮点编码中决定数值正负的位域。→ [[L24 数值格式]]
- **scale factor**（缩放因子）：决定低精度码格对应多大实数间隔和范围的系数。→ [[L24 数值格式]]

- **SOL (speed-of-light)**（速度上限占比）：profiler 中某项 compute/memory throughput 相对其理论最大值的比例。→ [[L22 算力度量与MFU]]
- **shared memory (SRAM)**（共享内存）：同一 block 的线程显式协作使用的片上静态随机存取存储器。→ [[L21 GPU内存体系]]
- **SIMT**（单指令多线程）：一个 warp 的多条线程通常共同执行一条指令，各线程仍保留自己的寄存器和数据。→ [[L20 GPU体系结构入门]]
- **SM (streaming multiprocessor)**（流式多处理器）：GPU 内负责驻留 blocks、调度 warps 并提供算术/片上存储资源的基本处理单元。→ [[L20 GPU体系结构入门]]

- **sliding window attention**（滑动窗口注意力）：每个位置只关注最近 $W$ 个 token，使连接数从 $O(S^2)$ 变为 $O(SW)$。→ [[L18 注意力变体与长上下文]]
- **state space model (SSM)**（状态空间模型）：用固定维度状态递推汇总历史的序列模型。→ [[L18 注意力变体与长上下文]]

- **shared expert**（共享专家）：所有 token 都经过的公共 FFN，补充路由 expert 的通用能力。→ [[L17 MoE混合专家]]
- **sparse activation**（稀疏激活）：模型总参数很多，但一次 token 只执行其中一小部分。→ [[L17 MoE混合专家]]
- **sparse model**（稀疏模型）：每个 token 只激活部分参数或计算路径的模型，MoE 是其中一种。→ [[L17 MoE混合专家]]

- **scaling law**（规模定律）：用经验拟合描述 loss 随模型、数据或计算规模变化的关系。→ [[L16 Scaling Law与算力账]]
- **sampling**（采样）：按指定规则从 next-token 概率分布中选择 token。→ [[L13 自回归生成与KV缓存]]
- **SFT / instruction tuning**（监督微调 / 指令微调）：在高质量指令-回答示范上继续做语言模型训练。→ [[L15 后训练与对齐]]
- **SSE / streaming**（Server-Sent Events / 流式返回）：不等待整段完成，在同一响应上随生成进度持续推送增量结果。→ [[L13 自回归生成与KV缓存]]、[[L61 推理服务框架与集群]]
- **scaled dot-product attention**（缩放点积注意力）：用 $\operatorname{softmax}(QK^T/\sqrt{d_{head}})V$ 完成匹配与加权取值的 attention。→ [[L11 注意力机制]]
- **sample**（样本）：dataset 中的一条记录，监督学习中通常由输入和 label 组成。→ [[L03 机器学习速成]]
- **scalability**（可扩展性）：资源或负载增加时，系统性能继续以可接受效率变化的能力。→ [[L02 如何读系统论文]]
- **scale-out**（横向扩展）：增加服务器或节点，并通过集群网络协同工作。→ [[L01 全景图]]；正式定义见 [[L25 节点内互联]]
- **scale-up**（纵向扩展）：在高带宽互联域内增加或组合更多 accelerator。→ [[L01 全景图]]；正式定义见 [[L25 节点内互联]]
- **self-attention**（自注意力）：Q、K、V 来自同一输入序列的 attention。→ [[L11 注意力机制]]
- **self-supervised learning**（自监督学习）：从数据本身构造 label，而非依赖人工逐条标注。→ [[L03 机器学习速成]]
- **second moment**（二阶矩）：Adam 中 gradient 平方的指数移动平均 $v$，用于估计近期尺度。→ [[L06 优化器]]
- **seq2seq**（序列到序列）：将一个输入序列映射为另一个输出序列的任务或建模框架。→ [[L08 CNN与RNN简史]]
- **sequential dependency**（顺序依赖）：后一个位置必须等待前一个位置结果的计算依赖。→ [[L08 CNN与RNN简史]]
- **serving**（推理服务）：将 inference 包装成持续接收、调度并响应请求的在线系统。→ [[L01 全景图]]
- **shape**（形状）：tensor 每个维度的长度，例如 `[B, D]`。→ [[L04 神经网络与前向传播]]
- **shuffle**（随机打乱）：在训练轮次中改变样本读取顺序，减少有序数据对 mini-batch gradient 的偏置。→ [[L07 训练循环解剖]]
- **shard**（数据分片）：可独立校验、读取和重试的一块 tokenized 训练数据。→ [[L14 预训练]]
- **skip connection**（跳跃连接）：跨过一个或多个计算层直接连接输入与后续输出的路径；residual connection 是常见形式。→ [[L08 CNN与RNN简史]]
- **SLO**（Service Level Objective，服务级别目标）：针对服务指标、阈值、统计窗口和适用流量设定的可测目标。→ [[L55 推理性能模型]]
- **SGD**（随机梯度下降）：用随机样本估计 gradient 并更新参数；实践中常泛指 mini-batch SGD。→ [[L06 优化器]]
- **softmax**（softmax）：把一组 logits 归一化为总和为 1 的 probability distribution。→ [[L04 神经网络与前向传播]]；attention 语境见 [[L11 注意力机制]]
- **special token**（特殊 token）：表示序列边界、填充或角色结构等控制用途的保留 token。→ [[L10 Token与嵌入]]
- **speedup**（加速比）：baseline 完成时间除以新方案完成时间，必须同时交代比较口径。→ [[L02 如何读系统论文]]
- **state_dict**（状态字典）：PyTorch 中以名称到 tensor 或配置的映射表示模块可保存状态的字典。→ [[L07 训练循环解剖]]
- **state-of-the-art (SOTA)**（当前先进水平）：特定时间、任务和约束下被认为具有最强竞争力的方法或结果。→ [[L02 如何读系统论文]]
- **step**（步）：训练中常指一次 optimizer 参数更新，但阅读论文时必须核对作者定义。→ [[L07 训练循环解剖]]
- **strong scaling**（强扩展）：固定总工作量增加资源，观察完成时间能缩短多少。→ [[L02 如何读系统论文]]
- **straggler**（掉队者 / 慢 rank）：在同步任务中完成更慢、迫使其他参与者等待的进程或设备。→ [[L39 实践-动手集合通信]]；MoE 路由负载语境见 [[L46 专家并行与MoE训练]]
- **subword**（子词）：介于字符和完整单词之间、可在多个词中复用的 token 片段。→ [[L10 Token与嵌入]]
- **supervised learning**（监督学习）：用输入与 label 配对的数据指导模型学习。→ [[L03 机器学习速成]]
- **SwiGLU**（Swish 门控线性单元）：gate 投影过 SiLU 后与 up 投影相乘、再经 down 投影的 FFN 变体，Llama 系标配。→ [[L12 Transformer全解剖]]

## T

- **tool call**（工具调用）：模型产出并由控制器执行的一次工具名、参数与调用标识。→ [[L70 选修-多模态与新负载]]

- **trace-driven simulation**（轨迹驱动仿真）：用真实或派生 trace 的事件顺序与属性驱动 simulator。→ [[L67 测量仿真与评测]]

- **TCO (Total Cost of Ownership)**（总拥有成本）：设备或租赁、能源设施、网络存储、运维和故障等全周期成本。→ [[L66 弹性算力与资源效率]]
- **tokens per joule**（每焦耳 token 数）：满足既定质量/SLO 的有用 token 数除以相应能量。→ [[L66 弹性算力与资源效率]]

- **Top-K sparsification**（Top-K 稀疏化）：只发送绝对值最大的 $K$ 个更新坐标及其索引。→ [[L63 跨域与跨集群训练]]

- **TensorRT-LLM**（TensorRT-LLM）：NVIDIA 面向 NVIDIA GPU 的 LLM inference library/runtime，提供量化、kernel 和多 GPU 优化路径。→ [[L61 推理服务框架与集群]]

- **TP for inference**（推理张量并行）：跨 ranks 切分层内权重与计算，以摊容量/带宽，代价是频繁的小 collective。→ [[L60 分布式推理与PD分离]]
- **Transfer Engine**（Mooncake 传输引擎）：支持多介质、多协议、多 NIC 与拓扑感知路径的统一数据传输层。→ [[L60 分布式推理与PD分离]]

- **target model**（目标模型）：定义最终期望输出分布、负责验证与纠正候选的模型。→ [[L59 投机解码]]
- **tree speculation / verification**（树形投机 / 验证）：并行保留多条共享前缀的候选分支，再用 target 一次验证多个树节点。→ [[L59 投机解码]]

- **TorchElastic**（PyTorch 弹性运行控制）：监控 worker、处理失败/成员变化并重新 rendezvous 与重启 worker group 的控制平面。→ [[L53 大规模训练可靠性]]

- **timeline**（时间线）：以时间为横轴、资源或软件实体为纵轴排列执行事件的视图。→ [[L52 性能剖析与MFU核算]]
- **torch.profiler**（PyTorch profiler）：记录 PyTorch ops 与 CPU/device activity，并可导出 trace 的框架级工具。→ [[L52 性能剖析与MFU核算]]

- **tiling**（分块）：把大 tensor 或计算域拆成适合片上存储和并行执行的小块。→ [[L51 算子优化与FlashAttention]]

- **Transformer Engine**（Transformer Engine）：NVIDIA 提供的 Transformer 低精度训练库，封装 FP8 recipe、scale 与相关 kernels。→ [[L49 混合精度与FP8训练]]
- **tile-wise scaling**（按 tile 缩放）：把 tensor 切成较小 tiles，各自使用 scale 以隔离局部 outlier。→ [[L49 混合精度与FP8训练]]

- **TP intra-node law**（“TP 留在节点内”规则）：把高频、关键路径上的 TP group 留在最快 scale-up 域；节点只是常见边界。→ [[L47 混合并行组装]]

- **token routing**（token 路由）：router 为每个 token 选择目标 experts，并生成索引与权重。→ [[L46 专家并行与MoE训练]]

- **tensor parallelism (TP)**（张量并行）：把同一层的 weight tensor、activation 与 GEMM 沿维度分给多个 ranks 共同执行。→ [[L43 张量并行]]
- **TP degree**（张量并行度）：一个 TP group 中共同完成同一层的 rank 数，记作 $p$。→ [[L43 张量并行]]

- **topology detection**（拓扑发现）：枚举 GPU、CPU、PCIe、NVLink 与 NIC 并计算可用路径。→ [[L38 NCCL解剖]]
- **tree algorithm**（树形算法）：沿树向 root 规约、再沿树广播结果的算法家族。→ [[L37 通信算法与代价模型]]

- **time-slicing**（分时共享）：让多个工作按时间片轮流使用同一物理 GPU 的复用方式。→ [[L35 GPU集群调度]]；与 MIG/MPS 的对照见 [[L66 弹性算力与资源效率]]
- **topology-aware placement**（拓扑感知放置）：选 GPU 时显式考虑节点、NVLink domain、leaf、rail、spine 等连接层级。→ [[L35 GPU集群调度]]

- **Tofino**（P4 可编程交换芯片系列）：Barefoot/Intel 的 programmable switch ASIC，曾被大量 P4 research prototypes 使用。→ [[L33 在网计算与智能网卡]]

- **traffic engineering (TE)**（流量工程）：根据 topology、traffic matrix 与 policy 全局规划 traffic/path allocation。→ [[L32 路由与负载均衡]]

- **topology reconfiguration**（拓扑重构）：改变物理 circuit/邻接关系，使容量匹配作业、故障或扩容状态。→ [[L31 AI集群网络拓扑]]
- **topology-aware**（拓扑感知）：placement、routing 或 collective algorithm 显式利用物理连接与带宽层级。→ [[L31 AI集群网络拓扑]]
- **torus**（环面拓扑）：在多个维度连接规则邻居，并用首尾回环消除边界的直接网络。→ [[L31 AI集群网络拓扑]]

- **TIMELY/Swift**（基于时延信号的拥塞控制）：用精细 RTT/端到端 delay target 推断 queue 并调节发送。→ [[L30 无损网络与拥塞控制]]

- **ToR (top-of-rack)**（机架顶部交换机）：位于 rack 接入侧、连接服务器或 GPU NIC 的 leaf 交换机。→ [[L28 数据中心网络基础]]

- **TPU**（张量处理单元）：Google 面向机器学习工作负载设计的专用加速器。→ [[L26 加速器生态与软件栈]]
- **TPU Pod**（TPU 集群资源域）：由专用网络连接的一组连续 TPU，用户通常取得其中一个 slice。→ [[L26 加速器生态与软件栈]]
- **Trainium**（AWS 训练加速器）：AWS 自研并通过 Neuron 软件栈和 EC2 集群提供的训练芯片。→ [[L26 加速器生态与软件栈]]
- **Triton**（GPU kernel 语言与编译器）：用 Python DSL 表达 tiled parallel kernels，并向 GPU 目标生成代码。→ [[L26 加速器生态与软件栈]]；自动融合与 custom kernel 见 [[L51 算子优化与FlashAttention]]
- **torch.distributed**（PyTorch 分布式通信包）：用 process group 与多种 backend 提供 collective 和 P2P 通信 API。→ [[L39 实践-动手集合通信]]
- **torchrun**（PyTorch 分布式进程启动器）：为单机或多机作业启动多个进程，并设置 rank、world size 与 rendezvous 信息。→ [[L39 实践-动手集合通信]]
- **torch.compile**（PyTorch 图编译入口）：捕获可编译的 PyTorch 程序区域并交给 backend compiler 优化。→ [[L26 加速器生态与软件栈]]；Inductor/Triton 融合路径见 [[L51 算子优化与FlashAttention]]
- **TorchInductor**（PyTorch 默认编译后端）：对捕获图做融合、调度和 code generation，GPU 上常生成 Triton kernel。→ [[L26 加速器生态与软件栈]]

- **TF32**（TensorFloat-32 计算模式）：FP32 工作流使用 Tensor Core matrix math 的计算模式，课内存储仍按 4 B。→ [[L22 算力度量与MFU]]；格式对照见 [[L24 数值格式]]
- **token dropping**（token 丢弃）：expert 缓冲区达到 capacity 上限时，跳过某 token 的 expert 计算的处理策略。→ [[L17 MoE混合专家]]
- **top-k routing**（top-k 路由）：只把 token 发给 router 分数最高的 $k$ 个 expert，并加权合并输出。→ [[L17 MoE混合专家]]
- **total parameters**（总参数）：模型所有权重的总量，决定模型容量和主要权重显存。→ [[L17 MoE混合专家]]

- **temperature**（温度）：通过缩放 logits 调节 next-token 概率分布的尖锐或平坦程度。→ [[L13 自回归生成与KV缓存]]
- **Tensor Core**（张量核心）：面向小块矩阵乘累加（MMA）的 GPU 专用单元，Transformer GEMM 常用它。→ [[L20 GPU体系结构入门]]；峰值与 MFU 见 [[L22 算力度量与MFU]]
- **thread**（线程）：GPU kernel 中处理一个逻辑索引或数据片段的最小程序抽象。→ [[L20 GPU体系结构入门]]
- **thread block**（线程块）：可被整体调度到一个 SM、可用 shared memory 协作的一组 threads。→ [[L20 GPU体系结构入门]]
- **test-time compute**（测试时计算）：请求处理阶段额外用于生成、搜索或验证的计算预算。→ [[L15 后训练与对齐]]
- **tokens-per-parameter**（每参数 token 数）：$D/N$，用来描述模型容量与训练数据的比例。→ [[L16 Scaling Law与算力账]]
- **top-k sampling**（top-k 采样）：只保留概率最高的 $k$ 个候选，重新归一化后采样。→ [[L13 自回归生成与KV缓存]]
- **top-p (nucleus) sampling**（top-p（核）采样）：保留累计概率首次达到 $p$ 的最小候选集合，再归一化采样。→ [[L13 自回归生成与KV缓存]]
- **TTFT**（首 token 时延）：从请求到达到用户收到第一个输出 token 的时间。→ [[L13 自回归生成与KV缓存]]；指标与 SLO 语境见 [[L55 推理性能模型]]
- **TPOT**（每输出 token 时间）：首 token 之后生成一个输出 token 的平均用时。→ [[L13 自回归生成与KV缓存]]；指标与 SLO 语境见 [[L55 推理性能模型]]
- **Transformer block**（Transformer 块/层）：Norm → attention → 残差 → Norm → FFN → 残差 的完整堆叠单元。→ [[L12 Transformer全解剖]]
- **tail latency**（尾时延）：latency 分布高端少量慢请求所经历的时延。→ [[L02 如何读系统论文]]
- **tensor**（张量）：对标量、向量、矩阵和更高维数组的统一称呼。→ [[L04 神经网络与前向传播]]
- **TensorBoard**（TensorBoard 可视化工具）：展示标量曲线、直方图等训练日志的常用工具。→ [[L07 训练循环解剖]]
- **test set**（测试集）：留到最后估计模型泛化表现的独立数据。→ [[L03 机器学习速成]]
- **testbed**（试验床）：为可控、可重复实验搭建的一组真实软硬件环境。→ [[L02 如何读系统论文]]；仿真校准语境见 [[L67 测量仿真与评测]]
- **throughput**（吞吐）：单位时间完成的总工作量。→ [[L02 如何读系统论文]]
- **throughput-oriented**（吞吐导向）：优先提高单位时间完成的有效工作量。→ [[L01 全景图]]；CPU/GPU 对照见 [[L20 GPU体系结构入门]]
- **token**（词元）：tokenizer 切分出的模型离散处理单位，不等同于单词或字符。→ [[L10 Token与嵌入]]；全景定位见 [[L01 全景图]]
- **tokenize**（词元化）：把原始文本按既定规则切成 token 序列的动作。→ [[L10 Token与嵌入]]
- **tokenizer**（词元切分器）：定义并执行文本到 token IDs 映射的规则和程序。→ [[L10 Token与嵌入]]
- **tokens/s (tokens per second)**（每秒 token 数）：每单位时间处理或生成的 token 数；必须说明训练/推理及聚合口径。→ [[L10 Token与嵌入]]；推理口径见 [[L55 推理性能模型]]
- **token budget**（token 预算）：计划在一次训练中消费的 token 总量，记作 $D$。→ [[L14 预训练]]
- **trace**（轨迹）：按时间记录的请求、作业、资源、网络或故障事件序列，可来自真实系统或合成生成。→ [[L02 如何读系统论文]]；trace-driven 评测见 [[L67 测量仿真与评测]]
- **training**（训练）：用数据计算误差并更新模型参数的过程。→ [[L01 全景图]]
- **training-inference tradeoff**（训练-推理算力权衡）：训练阶段多花 token/算力，换取部署阶段更小模型、更低每请求成本的取舍。→ [[L16 Scaling Law与算力账]]
- **training loop**（训练循环）：反复执行取数据、forward、计算 loss、backward、参数更新与记录的控制流程。→ [[L07 训练循环解剖]]
- **training run**（训练运行）：从初始化或 checkpoint 到目标进度/终止的一次完整训练实验。→ [[L14 预训练]]
- **training set**（训练集）：用来计算训练损失并更新参数的数据。→ [[L03 机器学习速成]]

## U

- **U-Net**（U 形编码器—解码器）：通过下采样、上采样和同尺度 skip connections 处理空间特征的经典扩散骨干。→ [[L70 选修-多模态与新负载]]

- **unified memory**（统一内存）：多类处理器在统一体系中共享的内存资源与地址空间，不等于全部容量可被模型独占。→ [[L64 边缘与端云协同]]

- **utilization ($\rho$)**（利用率 / 负载率）：简单单服务台模型中的 $\lambda/\mu$；不同于 GPU SM 或 HBM utilization。→ [[L55 推理性能模型]]

- **UGAL**（通用全局自适应负载均衡路由）：在 minimal 与经中间节点的 non-minimal path 间权衡 hops 和 congestion。→ [[L32 路由与负载均衡]]

- **Ultra Ethernet Consortium (UEC)**（超以太网联盟）：制定面向 AI/HPC 的 UET、软件 API、link 与 telemetry 等规范的产业联盟。→ [[L30 无损网络与拥塞控制]]

- **U (rack unit)**（机架单位）：描述设备占用机柜垂直高度的标准化安装单位。→ [[L27 走进AI数据中心]]

- **UALink**（Ultra Accelerator Link）：面向 pod 内 accelerator 的开放 scale-up 互联标准。→ [[L25 节点内互联]]

- **underflow**（下溢）：数值小到进入 subnormal、甚至最终舍入为 0 的现象。→ [[L24 数值格式]]

- **underfitting**（欠拟合）：模型连训练数据中的主要规律也没有学好。→ [[L03 机器学习速成]]
- **up projection**（升维投影）：FFN 中把 $d$ 维升到 $d_{ff}$ 维的输入矩阵。→ [[L12 Transformer全解剖]]

## V

- **vector database**（向量数据库）：保存向量、元数据与 ANN 索引，并提供更新、过滤、分片和查询。→ [[L70 选修-多模态与新负载]]
- **video generation**（视频生成）：生成带时间维的视觉序列，工作量受时空 token、步数与模型规模共同影响。→ [[L70 选修-多模态与新负载]]
- **vision encoder**（视觉编码器）：把像素/patch 编成包含视觉语义与空间关系的特征。→ [[L70 选修-多模态与新负载]]
- **ViT**（Vision Transformer，视觉 Transformer）：把图像 patch 当序列项交给 Transformer 编码。→ [[L70 选修-多模态与新负载]]
- **VLM**（Vision-Language Model，视觉语言模型）：把视觉表示接入语言理解或生成的一类模型。→ [[L70 选修-多模态与新负载]]

- **validation**（验证）：在未参与调参的配置或数据上检查模型预测能力与误差。→ [[L67 测量仿真与评测]]

- **Varuna**（Varuna 大模型训练系统）：让大模型训练自动适配动态资源与 commodity networking。→ [[L66 弹性算力与资源效率]]

- **verify / verification**（验证）：target 对候选位置并行算 logits，并从左到右接受或在首个分歧处纠正。→ [[L59 投机解码]]

- **vLLM**（vLLM 推理引擎）：以 PagedAttention 与高吞吐调度著称的开源 LLM serving engine。→ [[L56 KV缓存与PagedAttention]]

- **veRL**（Volcano Engine Reinforcement Learning）：HybridFlow 的开源 RL 后训练框架，集成训练与 rollout backends。→ [[L54 RL后训练系统]]

- **virtual stage**（虚拟流水段）：interleaved 调度中映射到物理 GPU 的较小、非连续模型分段。→ [[L44 流水线并行]]

- **Volcano**（Volcano 批调度系统）：以 PodGroup/Job 等对象为 Kubernetes 补充 gang、queue 等批作业语义。→ [[L35 GPU集群调度]]

- **verbs**（RDMA 操作接口 / 动词集）：注册内存、创建队列并投递 send/read/write 等操作的编程抽象。→ [[L29 RDMA原理]]

- **validation set**（验证集）：用来选择模型版本、训练配置和停止时机的数据。→ [[L03 机器学习速成]]
- **validation loss**（验证损失）：在 validation set 上测量的 loss，常用于比较 checkpoint 和配置。→ [[L14 预训练]]
- **value**（值）：候选位置在获得权重后真正贡献给输出的内容向量。→ [[L11 注意力机制]]
- **vanishing gradient**（梯度消失）：深层反传中 gradient 因连续相乘而缩到接近 0，早期层难以学习。→ [[L05 反向传播与梯度]]
- **venue**（发表场所）：论文投稿或发表的会议、期刊等学术场所。→ [[L02 如何读系统论文]]
- **vocab size**（词表大小）：vocabulary 中 token 条目的总数，通常记为 $V$。→ [[L10 Token与嵌入]]
- **vocabulary**（词表）：token 片段与整数 ID 的固定映射集合。→ [[L10 Token与嵌入]]

## W

- **workload characterization**（工作负载刻画）：把原始日志提炼成 arrival、size、collective、resource、failure 等可用于设计的结构。→ [[L67 测量仿真与评测]]

- **WAN**（Wide Area Network，广域网）：连接远距离站点、带宽和时延显著不同于域内 fabric 的网络。→ [[L63 跨域与跨集群训练]]

- **wide-EP / large-scale EP**（宽 / 大规模专家并行）：把 experts 分到数十至数百 GPUs，以 A2A 换每卡更少权重与更大聚合 batch。→ [[L60 分布式推理与PD分离]]

- **W{x}A{y} (W4A16/W8A8)**（权重 / 激活位宽记法）：W4A16 表示 4-bit 权重、16-bit activation；W8A8 表示两者均 8 bit。→ [[L58 量化推理]]
- **weight-only quantization**（仅权重量化）：只压低权重存储位宽，activation 保持 FP16/BF16。→ [[L58 量化推理]]

- **workload trace**（工作负载轨迹）：按时间记录请求到达、长度、类别与相关服务属性的数据。→ [[L55 推理性能模型]]

- **weight sync**（权重同步）：把训练侧的新 actor 版本发布并安装到 rollout engine。→ [[L54 RL后训练系统]]
- **weight update**（权重更新）：optimizer 在训练 engine 内根据 gradient 产生新 actor 参数版本。→ [[L54 RL后训练系统]]

- **wrapping**（包装 / 划分单元）：确定哪些 parameters 一起 all-gather、计算与 reshard 的模块边界。→ [[L42 ZeRO与FSDP]]

- **worker**（工作节点）：读取参数、处理本地数据并计算、提交梯度或更新的执行角色。→ [[L41 数据并行与DDP]]

- **world size**（全局进程数）：默认全局进程组中的进程总数；当前子组大小可能更小。→ [[L36 集合通信原语]]

- **wrap-around**（首尾回环链路）：把某一维的两个边界位置直接相连，缩短环上最远距离。→ [[L31 AI集群网络拓扑]]

- **WR/WQE**（工作请求 / 工作队列项）：WR 是软件请求，WQE 是它在 provider/NIC work queue 中的描述符表示。→ [[L29 RDMA原理]]

- **warp**（线程束）：32 个 threads 组成的 GPU 硬件发射与调度单位。→ [[L20 GPU体系结构入门]]
- **warp divergence**（warp 分歧）：同一 warp 的线程走不同分支，导致分支路径分段串行执行。→ [[L20 GPU体系结构入门]]
- **warmup**（预热）：优化器语境指 learning rate 从小值逐步升到峰值；流水线语境指 step 开头逐步灌入 forward 的阶段。→ [[L06 优化器]]；流水线语境见 [[L44 流水线并行]]
- **wandb**（Weights & Biases 实验跟踪工具）：集中记录训练指标、配置和实验对比的常用平台。→ [[L07 训练循环解剖]]
- **weak scaling**（弱扩展）：固定每个处理单元的工作量增加资源，观察总规模能否同比增长。→ [[L02 如何读系统论文]]
- **weight**（权重）：神经网络中参与加权计算的 parameter，口语中常泛指模型参数。→ [[L03 机器学习速成]]
- **weight decay**（权重衰减）：更新时把 weights 轻微拉向 0 的正则化机制。→ [[L06 优化器]]
- **weight tying**（权重绑定）：输入 embedding 与 lm_head 共享同一张参数表的做法。→ [[L12 Transformer全解剖]]
- **width**（宽度）：某一层包含的 neuron 数或输出特征维数。→ [[L04 神经网络与前向传播]]
- **workload**（工作负载）：系统实际承受的输入、到达模式、操作序列与资源需求的组合。→ [[L01 全景图]]

## X

- **xFormers**（Transformer 优化组件库）：提供可组合组件和 memory-efficient attention 等 optimized operators。→ [[L51 算子优化与FlashAttention]]

- **XOFF/XON**（暂停 / 恢复水线）：queue 到 XOFF 触发 PAUSE，排空到较低 XON 后恢复发送。→ [[L30 无损网络与拥塞控制]]

- **XLA**（加速线性代数编译器）：把框架计算图优化并编译到 TPU/GPU 等后端的 compiler。→ [[L26 加速器生态与软件栈]]

## Y

- **YaRN**（一种 RoPE 扩窗方法）：Yet another RoPE extensioN，通过频率与缩放策略配合继续训练扩展窗口。→ [[L18 注意力变体与长上下文]]

## Z

- **zigzag / load-balanced sharding**（之字形 / 负载均衡切分）：把早、晚序列 chunks 配给同一 rank，使 causal attention 的有效工作更均匀。→ [[L45 序列与上下文并行]]

- **zero-bubble pipeline**（零气泡流水线）：拆分 input-gradient 与 weight-gradient backward 并重排，以填充同步流水气泡的调度家族。→ [[L44 流水线并行]]

- **ZeRO**（Zero Redundancy Optimizer，零冗余优化器）：逐步切分数据并行中的 optimizer states、gradients 和 parameters，以通信换静态显存。→ [[L42 ZeRO与FSDP]]
- **ZeRO stage 1**（ZeRO 第一阶段）：只切分 FP32 master parameter 与 Adam moments，本课口径每 rank 为 $4N+12N/p$ B。→ [[L42 ZeRO与FSDP]]
- **ZeRO stage 2**（ZeRO 第二阶段）：在 stage 1 上再切 gradients，本课口径每 rank 为 $2N+14N/p$ B。→ [[L42 ZeRO与FSDP]]
- **ZeRO stage 3**（ZeRO 第三阶段）：再切 parameters，使静态模型状态降为 $16N/p$ B，并在计算前按需聚合参数。→ [[L42 ZeRO与FSDP]]
- **ZeRO-Offload**（ZeRO 卸载）：将 optimizer memory/compute 等迁到 CPU 的 ZeRO 扩展。→ [[L42 ZeRO与FSDP]]；带宽账见 [[L50 显存优化技术]]
- **ZeRO-Infinity**（ZeRO Infinity）：将 ZeRO-3 与 CPU/NVMe 异构内存 offload、prefetch 和 tiling 结合的系统。→ [[L42 ZeRO与FSDP]]；异构内存层级见 [[L50 显存优化技术]]
- **ZeRO++**（ZeRO++）：用量化与层次化 partition 等机制降低 ZeRO-3 通信成本的扩展。→ [[L42 ZeRO与FSDP]]

- **zero copy**（零拷贝）：NIC DMA 直接读写应用 buffer，避免 CPU 主导的中间 buffer copy。→ [[L29 RDMA原理]]

- **zero point**（零点）：affine quantization 中与实数 0 对应的整数码。→ [[L24 数值格式]]

## 中文起头的术语

（原则上术语以英文为主键；确无通行英文名的才放这里）
