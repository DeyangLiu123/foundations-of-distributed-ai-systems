---
title: 约定与符号
tags:
  - course/meta
---

# 约定与符号

> 本文档是全课程的**统一口径**。所有课程笔记中的单位、符号、硬件参数、经验公式一律以本文为准，不要凭记忆另写一套，避免课与课之间数字打架。若发现本文有误，先在 [[01 进度看板]] 勘误区登记，经确认后统一修改。

## 一、单位与换算（本领域最常见的坑）

| 约定 | 说明 |
|------|------|
| **b = bit，B = Byte**，1 B = 8 b | 网络带宽习惯用 **Gb/s（Gbps）**，内存/显存/总线带宽习惯用 **GB/s**。看到 400G 网卡，先除以 8：400 Gb/s = 50 GB/s |
| 容量与带宽默认按 SI（1 k = 1000） | 400 Gb/s = 4×10¹¹ b/s。显存容量的 80 GB 实为厂商口径，与 GiB（2³⁰）的差异（约 7%）在估算中忽略，不必纠结 |
| FLOPs vs FLOPS | **FLOPs** = 浮点运算次数（工作量，复数 s）；**FLOPS** = 每秒浮点运算次数（速率）。TFLOPS = 10¹²/s，PFLOPS = 10¹⁵/s |
| 双向带宽陷阱 | NVLink 官方数字是**双向合计**（900 GB/s = 每方向 450）；PCIe 官方常报单向；网卡端口速率（400 Gb/s）指每方向。引用时必须注明「单向/双向」 |
| token 速率 | 训练吞吐用 tokens/s（全局）；推理用 tokens/s（单请求或整机，注明）。1 个英文单词 ≈ 1.3 token，1 个汉字 ≈ 0.5–1 token（因 tokenizer 而异） |
| 时间 | ms = 10⁻³ s，µs = 10⁻⁶ s。RTT 指往返。p50/p99 = 第 50/99 百分位 |

## 二、术语与译名策略

- **通信原语、算子名、API 名一律保留英文**：all-reduce、all-gather、reduce-scatter、all-to-all、broadcast、send/recv、GEMM、softmax……不翻译。
- **概念名**：首次出现用「**英文**（中文注解）」，如 **oversubscription**（收敛比 / 超额订阅）；此后可只用英文。
- 固定译名（全课程统一）：latency → **时延**；throughput → **吞吐**；bandwidth → **带宽**；parallelism → **并行**；sharding → **切分**；checkpoint → **检查点**（可保留英文）；KV cache → **KV 缓存**（可保留英文）。
- 已收录术语以 [[02 术语总表]] 为准；写新课前先查表，与已有译名冲突时改自己的。

## 三、数学符号表

| 符号 | 含义 | 备注 |
|------|------|------|
| $N$ | 模型参数量 | 如 8B = 8×10⁹ |
| $D$ | 训练 token 总量 | 如 15T = 1.5×10¹³ |
| $L$ | Transformer 层数 | |
| $d$ | 隐层宽度 hidden size ($d_{model}$) | |
| $h$ / $h_{kv}$ | attention 头数 / KV 头数（GQA） | $d_{head} = d/h$ |
| $d_{ff}$ | FFN 中间维度 | |
| $V$ | 词表大小 | |
| $S$ | 序列长度 | |
| $b$ / $B$ | micro-batch size / global batch size | $B = b \times \text{DP} \times \text{GA}$（GA=梯度累积步数） |
| $p$ | 参与通信的 rank 数 | |
| $n$ | 一次通信的消息字节数 | |
| $\alpha$ / $\beta$ | 每次通信的固定时延 / 每字节传输时间（1/带宽） | α-β 代价模型 |
| DP/TP/PP/CP/EP | 数据/张量/流水线/上下文/专家并行的并行度 | 总卡数 = DP×TP×PP×CP（EP 复用 DP 维度时另行说明） |

## 四、记账常数与经验公式

| 项目 | 口径 | 备注 |
|------|------|------|
| dtype 字节数 | FP32=4B，FP16/BF16=2B，FP8/INT8=1B，INT4=0.5B | TF32 是计算模式，存储仍 4B |
| **训练显存（Adam 混合精度）** | **≈ 16 B/参数** = BF16 参数 2 + BF16 梯度 2 + FP32 主参数 4 + FP32 动量 4 + FP32 二阶矩 4 | 有实现用 FP32 梯度（→18B）或其他变体，引用论文时注意其口径；activation 另算 |
| 推理显存（权重部分） | BF16: 2 B/参数；INT8: 1；INT4: 0.5 | KV cache 另算 |
| KV cache 大小 | $2 \times L \times S \times h_{kv} \times d_{head} \times \text{bytes}$（×batch） | 2 = K 和 V |
| **训练总计算量** | **≈ 6ND FLOPs**（前向 2ND + 反向 4ND） | 长序列时注意力的 $O(S^2)$ 项需加回，精确式由 L16 推导并注明来源 |
| 推理计算量 | 每 token 前向 ≈ 2N FLOPs（decode，不含注意力二次项） | |
| MFU | 实测 tokens/s × 每 token FLOPs ÷ (卡数 × 单卡峰值 FLOPS) | 峰值一律用**稠密（dense）**算力，不用 2:4 稀疏营销值 |
| ring all-reduce 通信量 | 每 rank 收发各 $2n(p-1)/p \approx 2n$ 字节 | 推导见 L37 |
| 流水线气泡率 | $(p-1)/(m+p-1)$，$p$=stage 数，$m$=micro-batch 数 | 1F1B 与 GPipe 气泡率相同，省显存 |

## 五、硬件速查表（课内统一口径）

> 均为公开规格的常用近似值，用于估算；写论文引用请查官方 datasheet。**规格表中的「1979 TFLOPS」类数字是 2:4 结构化稀疏值，本课程一律用稠密值。**

### GPU

| GPU | 显存 | 显存带宽 | BF16 稠密 | FP8 稠密 | NVLink（双向合计） | 功耗 |
|-----|------|----------|-----------|----------|--------------------|------|
| A100 SXM 80GB | 80 GB | ~2.0 TB/s | 312 TFLOPS | — | 600 GB/s | 400 W |
| H100 SXM | 80 GB | 3.35 TB/s | ~989 TFLOPS | ~1979 TFLOPS | 900 GB/s | 700 W |
| H800 SXM（出口版） | 80 GB | 3.35 TB/s | ~989 TFLOPS | ~1979 TFLOPS | **400 GB/s** | 700 W |
| H20（出口版） | 96 GB | 4.0 TB/s | ~148 TFLOPS | ~296 TFLOPS | 900 GB/s | 500 W |
| B200 | 192 GB | ~8 TB/s | ~2.25 PFLOPS | ~4.5 PFLOPS | 1.8 TB/s | ~1000 W |

### 互联与网络

| 链路 | 速率 | 换算 |
|------|------|------|
| PCIe Gen4 x16 | ~32 GB/s 单向 | |
| PCIe Gen5 x16 | ~64 GB/s 单向 | |
| InfiniBand EDR / HDR / NDR / XDR | 100 / 200 / 400 / 800 Gb/s（每端口每方向） | ÷8 → 12.5 / 25 / 50 / 100 GB/s |
| 以太网 RoCE 常见网卡 | ConnectX-6 200G，ConnectX-7 400G，ConnectX-8 800G | |
| 典型 H100 节点 scale-out 配置 | 8 × 400G = 3.2 Tb/s/节点（每 GPU 一张 400G 网卡） | 每 GPU 50 GB/s，对比 NVLink 单方向 450 GB/s——**9 倍差距**，这是「TP 不出机箱」的根源 |

### 参考模型（课内统一例子）

| 模型 | 参数量 | 层数 $L$ | $d$ | 头数 $h/h_{kv}$ | $d_{ff}$ | 词表 $V$ | 备注 |
|------|--------|----------|-----|------------------|----------|----------|------|
| Llama-2-7B | 6.7B | 32 | 4096 | 32/32（MHA） | 11008 | 32000 | 经典教学例子 |
| Llama-3-8B | 8.0B | 32 | 4096 | 32/8（GQA） | 14336 | 128256 | L12 手算对象 |
| Llama-3-70B | 70B | 80 | 8192 | 64/8（GQA） | 28672 | 128256 | |
| Llama-3-405B | 405B | 126 | 16384 | 128/8（GQA） | 53248 | 128256 | 训练用 ~16K H100，D≈15.6T tokens |
| Mixtral-8x7B | 46.7B 总 / ~12.9B 激活 | 32 | 4096 | 32/8 | 14336 | 32000 | 8 专家 top-2 |
| DeepSeek-V3 | 671B 总 / 37B 激活 | 61 | 7168 | MLA | — | — | 256 路由专家 top-8 + 1 共享；FP8 训练；2048×H800 |

> 表中未列的参数（如某模型的精确 $d_{ff}$）**查官方 config.json 或技术报告**，不要凭记忆填。

## 六、写作中的引用纪律

1. 论文引用格式：`《论文标题》（会议/来源 年份）`，只写确信的；arXiv 编号拿不准就不写。
2. 硬件数字一律引用本文速查表；本文没有的，要么查证后补进本文（并在勘误区登记），要么定性表述。
3. 涉及仍在快速变化的事实（框架版本、SOTA、厂商路线图），标注「截至 2026 年」类时间戳。
