---
title: 进度看板
tags:
  - course/meta
---

# 进度看板

> **领任务流程**（实现者必读，完整规范见 [[AGENTS]]）：从下面清单找到**第一个未勾选**的课（或用户指定的课）→ 读 `_课程设计/` 对应模块设计稿 → 按 [[_模板/课程模板]] 写课 → 更新 [[02 术语总表]] → 回来打勾并填日期。

## 状态图例

`- [ ]` 未开始 · `- [x]` 已完成（后附完成日期）· 返工中在行尾标 `🔧`

## M0 全景与方法

- [x] L01 全景图（2026-07-16）
- [x] L02 如何读系统论文（2026-07-17）

## M1 深度学习基础

- [x] L03 机器学习速成（2026-07-17）
- [x] L04 神经网络与前向传播（2026-07-18）
- [x] L05 反向传播与梯度（2026-07-20）
- [x] L06 优化器（2026-07-20）
- [x] L07 训练循环解剖（2026-07-20）
- [x] L08 CNN与RNN简史（2026-07-20）
- [x] L09 实践-训练第一个模型（2026-07-21）

## M2 Transformer与大模型

- [x] L10 Token与嵌入（2026-07-22）
- [x] L11 注意力机制（2026-07-23）
- [x] L12 Transformer全解剖（2026-07-24）
- [x] L13 自回归生成与KV缓存（2026-07-29）
- [x] L14 预训练（2026-08-03）
- [x] L15 后训练与对齐（2026-08-03）
- [x] L16 Scaling Law与算力账（2026-08-03）
- [x] L17 MoE混合专家（2026-08-03）
- [x] L18 注意力变体与长上下文（2026-08-03）
- [x] L19 实践-解剖迷你GPT（2026-08-03）

## M3 GPU与加速器

- [x] L20 GPU体系结构入门（2026-08-03）
- [x] L21 GPU内存体系（2026-08-03）
- [x] L22 算力度量与MFU（2026-08-04）
- [x] L23 Roofline模型（2026-08-04）
- [x] L24 数值格式（2026-08-04）
- [x] L25 节点内互联（2026-08-04）
- [x] L26 加速器生态与软件栈（2026-08-04）

## M4 数据中心与网络

- [x] L27 走进AI数据中心（2026-08-04）
- [x] L28 数据中心网络基础（2026-08-04）
- [x] L29 RDMA原理（2026-08-04）
- [x] L30 无损网络与拥塞控制（2026-08-04）
- [x] L31 AI集群网络拓扑（2026-08-04）
- [x] L32 路由与负载均衡（2026-08-04）
- [x] L33 在网计算与智能网卡（2026-08-04）
- [x] L34 存储与数据供给（2026-08-04）
- [x] L35 GPU集群调度（2026-08-04）

## M5 集合通信

- [x] L36 集合通信原语（2026-08-04）
- [x] L37 通信算法与代价模型（2026-08-04）
- [x] L38 NCCL解剖（2026-08-04）
- [x] L39 实践-动手集合通信（2026-08-05）

## M6 分布式训练并行

- [x] L40 训练显存全解剖（2026-08-05）
- [ ] L41 数据并行与DDP
- [ ] L42 ZeRO与FSDP
- [ ] L43 张量并行
- [ ] L44 流水线并行
- [ ] L45 序列与上下文并行
- [ ] L46 专家并行与MoE训练
- [ ] L47 混合并行组装
- [ ] L48 实践-并行策略推演

## M7 训练工程与可靠性

- [ ] L49 混合精度与FP8训练
- [ ] L50 显存优化技术
- [ ] L51 算子优化与FlashAttention
- [ ] L52 性能剖析与MFU核算
- [ ] L53 大规模训练可靠性
- [ ] L54 RL后训练系统

## M8 推理系统

- [ ] L55 推理性能模型
- [ ] L56 KV缓存与PagedAttention
- [ ] L57 连续批处理与调度
- [ ] L58 量化推理
- [ ] L59 投机解码
- [ ] L60 分布式推理与PD分离
- [ ] L61 推理服务框架与集群
- [ ] L62 实践-跑起来一个推理服务

## M9 前沿专题

- [ ] L63 跨域与跨集群训练
- [ ] L64 边缘与端云协同
- [ ] L65 推理服务生存性
- [ ] L66 弹性算力与资源效率
- [ ] L67 测量仿真与评测
- [ ] L68 实践-仿真上手
- [ ] L69 研究地图与论文清单
- [ ] L70 选修-多模态与新负载

---

## 设计偏差记录

> 实现某课时若发现设计稿与事实冲突、或必须偏离设计稿，在此登记：`Lxx：偏离点 + 理由 + 日期`。

- L01：将设计稿中“全球推理总算力开销已远超训练”改为不依赖不稳定全球统计口径的定性表述“累计资源开销可能非常大”；遵守不造假红线（2026-07-16）。
- L02：将设计稿中“扇出 100 个子请求时 p99 几乎必然命中”改为独立近似下“至少一个进入最慢 1% 的概率约 63.4%（期望 1 个）”；避免把 63.4% 表述为“几乎必然”（2026-07-17）。
- L10：将设计稿中“50 万字中文小说 ≈ 30–50 万 token”按 [[03 约定与符号]] 的“1 汉字 ≈ 0.5–1 token”统一口径改为约 25–50 万 token；避免下界与统一口径不一致（2026-07-22）。
- L12：审核修正——①将参数手算公式的适用范围从「任何 dense LLM」限定为 Llama / Qwen2 式架构（设计稿「任何模型卡都能读」的口径过宽：GPT-2 等老架构为两矩阵 FFN、带 bias、另有 learned 位置 embedding 表，公式形状不同）；②pre-norm 表述从设计稿的「现代模型全是 pre-norm」软化为「多数现代 decoder-only LLM 采用 pre-norm」，并保留 warmup 必要性说明（2026-07-24）。
- L14：将设计稿中“54 天”处理为公开材料常见的主训练数量级，并明确官方报告表格提供的是 16,384 卡、400 TFLOPs/GPU、41% MFU 等阶段口径；估算按假设给出约 68 天，不把项目总 wall-clock 写成精确事实（2026-08-03）。
- L16：将设计稿中“Llama 系 D/N 达 200+”改为按模型规模和训练目标解释，避免把小模型的过训练比例套到 405B 旗舰；长序列 attention 修正采用 Kaplan 与 Chinchilla 附录已核实的逐项计数，并把“约 6d”保留为量级提示而非硬阈值（2026-08-03）。
- L18：将设计稿中“截至 2026 主流 frontier 仍是 Transformer”改为“attention-centric Transformer 仍是关键主干与基线，但公开 hybrid 已进入实际模型路线”；Falcon-H1（2025）与 NVIDIA Nemotron 3 Ultra 官方技术说明（2026）已出现 Mamba-Transformer hybrid，避免把快速演化的架构格局写成绝对判断（2026-08-03）。
- L20：设计稿定量环节使用“每 SM 2048 个并发线程”，但 [[03 约定与符号]] 尚未列出该字段；正文保留为带教学估算说明的量级计算（约 2.7×10^5 在飞线程），建议后续在硬件速查表补录“最大 resident threads/SM”口径（2026-08-03）。
- L21：设计稿列出 L2 约 10 TB/s，但 [[03 约定与符号]] 未登记该精确带宽且实际受访问模式影响；正文按“TB/s 量级”表述，避免引入未统一的规格数字；另将“nvidia-smi 显示 reserved”细化为“通常包含 allocator 保留块且可能含 CUDA context/通信库”，避免把驱动统计误读成活跃 tensor 精确值（2026-08-03）。
- L22：将设计稿“H100 的 1979 TFLOPS 为什么要打对折读”改为 dtype 限定的判断：BF16 1979 是 2:4 sparse 值，应还原为 dense 约 989；但 FP8 dense 本身约为 1979，不能再减半。依据 NVIDIA H100 官方规格页的 dtype 行与 sparsity 脚注，避免把裸数字当成唯一口径（2026-08-04）。
- L23：将“prefill attention compute-bound”限定为 IO-aware tiling/fusion 能减少 $S\times S$ 中间量 HBM 流量时的结论；另补算朴素物化 score 时 AI 约 62、仍可能 memory-bound，避免把 attention 的数学 FLOPs 直接当作固定的 operational intensity（2026-08-04）。
- L24：设计稿要求格式主表逐项列硬件峰值，但 [[03 约定与符号]] 只登记了 BF16/FP8 的部分 dense 峰值，且峰值本就不是格式的固有属性；正文仅填写 03 已有的 H100/B200 BF16/FP8 口径，其余标为“未统一登记”，避免补造规格（2026-08-04）。
- L26：将设计稿误区中的“H20 反而性价比高”收敛为“H20 的规格与 memory-bound decode 更匹配”；[[03 约定与符号]] 未登记 H20 价格，且出口许可与供给状态会变化，不能由算力/带宽规格推出确定性价比（2026-08-04）。
- L27：设计稿中的传统机柜约 10 kW、GB200 NVL72 约 120 kW、电价 $0.08/\text{kWh}$ 与“约 2.5 万户家庭”换算均未在 [[03 约定与符号]] 登记；正文将 rack power density 收敛为十千瓦级/百千瓦级，把电费写成当地电价变量并省略居民户数类比，只保留按统一 H100 节点功耗与 PUE 复算的约 25 MW 设施负载（2026-08-04）。
- L28：①设计稿把 64 口交换机、leaf 32 下/32 上的两层 Clos 写成“最多 1,024 端点”；正文保留 32 台 leaf 的 1,024-GPU 实例与 204.8 Tb/s 单向对分带宽，但按端口守恒将硬件上限修正为 64 台 leaf、2,048 端点（每台 64 口 spine 可接 64 台 leaf）；②将“AI 后端网普遍 1:1”收敛为“性能敏感的 AI domain 倾向 1:1”，并补充 Meta 生产网络单 zone non-blocking、跨 zone 有意 oversubscribe 的边界，避免把局部设计泛化到整张后端网（2026-08-04）。
- L29：①将 $p=1024$ 的 TCP/RDMA latency term 从笼统“差两个量级”修正为按 [[03 约定与符号]] 区间可复算的约 10–100 倍；②将 go-back-N 明确限定为传统 RoCE RC 恢复，并补充 ConnectX-6 及更新设备可协商 selective-repeat；③未把“400G TCP 烧多少核”写成硬件无关常数，而用显式 cycles/Byte 与频率假设给出参数化 CPU budget；④将“NVIDIA 系多 IB、超大厂多 RoCE”收敛为按既有 fabric、运维能力和应用支持选型，避免把厂商/规模当协议定律（2026-08-04）。
- L30：①将设计稿内容要点中的“IRN（NSDI 2018）”按正式发表版修正为 SIGCOMM 2018；②将“8 个优先级队列”细化为 8 个 PCP priority 值、再由设备映射到 traffic class/queue；③未把“64×400G 交换机总 buffer 几十 MB”写成跨设备规格，改为按 64 端口×lossless class×百 KB headroom 复算 6.4/25.6/51.2 MB 的静态预留思维实验，避免把 ASIC shared/dynamic buffer 泛化为固定值（2026-08-04）。
- L31：①按 IEEE Symposium on High-Performance Interconnects（HOTI 2024）正式版 Table II，将设计稿正文中的 rail-only“省 25%–40% 网络成本”修正为论文场景的 38%–77%，并复算 32,768-GPU、64/128-port 场景；②将“网络占集群成本 10%–20%”限定为 2024 成本研究引用的 A100/InfiniBand 专家估计及约 19% SuperPOD 模型，不作为跨代定律；③明确 torus 的 24 条 inter-node links 与 fat-tree 的 5 个 switching stages 使用不同 hop 节点定义，避免把数字无条件横比（2026-08-04）。
- L32：①将设计稿“4 流随机哈希进 16 路≈31%”明确为生日悖论指数近似 31.27%，并按精确无碰撞概率修正为 33.35%；②将“ConnectX-7 硬件重排”收紧为 NVIDIA 文档所述 ConnectX-5 及更新设备在指定 InfiniBand RC/XRC QP 与 DC transport 上支持 out-of-order data placement，不泛化成任意 Ethernet/RDMA 模式的透明 reorder buffer（2026-08-04）。
- L35：设计稿要求由“1/2/4/8 卡作业随机到达”简化算出稳态 10%–25% 碎片损耗，但未给作业尺寸概率、到达率、服务时间分布、放置/回填策略与拓扑约束，无法唯一推出稳态比例；正文改为可手算的 1,024 卡确定性快照（160 卡碎片空闲，15.625%），并明确真实稳态应由 trace-driven simulation 或实测给出（2026-08-04）。
- L37：设计稿将“节点内 reduce → 节点间 all-reduce → 节点内 broadcast”与“8 卡时跨网数据降为 $1/8$”并列；单 leader reduce 后的域间 payload 仍为完整 $n$。正文改为可实现每 lane $n/8$ 的三阶段 2D ring（节点内 reduce-scatter → 8 条节点间 all-reduce lanes → 节点内 all-gather），并明确这是 per-GPU/per-NIC lane 口径，节点聚合跨网字节未减 8 倍（2026-08-04）。
- L38：设计稿称为 L39 “附一份 8×H100 的典型数值表”，但仓库未提供该附件或可追溯 benchmark 原始日志；为遵守不造假红线，正文改用按 $\alpha\approx10\ \mu\text{s}$ 与 NDR 50 GB/s×0.9 健康线推导、逐行可复算且明确标注“非实测”的 64 卡教学表，不把构造数据冒充集群成绩（2026-08-04）。
- L39：①设计稿所称附带的 8×H100 数值表实际不在仓库，正文改用 NVIDIA `nccl-tests` 官方仓库 issue #212 中可追溯的单节点 8×H100 公开实测片段，并明确环境信息不全、非通用健康阈值；②PyTorch 官方能力表显示 Gloo 不原生支持 all-to-all，CPU 路径因此用 all-gather + 本地选列只验证等长 all-to-all 语义，NCCL 路径仍调用原生 API，避免把后备算法冒充原生性能（2026-08-05）。
- L40：①MLSys 2023 正式版《Reducing Activation Recomputation in Large Transformer Models》中，无模型并行的 $sbh(34+5as/h)$ 是公式 (1)，而非设计稿所指的公式 (2)，正文按正式版编号修正；②FlashAttention 可消除 $S^2$ attention 中间矩阵的常驻需求，但 $34Sbd$ 只是沿用该论文口径移除二次项后的教学估算，不作为 Llama-3/具体 kernel 的实测峰值（2026-08-05）。

## 勘误与反馈

> 读者发现任何错误（数字、概念、链接）记在这里：`Lxx：问题描述 + 发现人 + 日期`。修复后划掉并注明。

- L03：建议在 [[03 约定与符号]] 的“其他常用量级”补录数据集估算口径：MNIST 训练集 60,000×28×28×1 B≈47 MB；ImageNet/ILSVRC 2012 训练集约 1.3M 张 JPEG、磁盘量级约 150 GB（随版本和编码变化）；15.6T token 的 token-id 数组按 2/4 B 约为 31.2/62.4 TB。正文已按“约/量级”表述（2026-07-17）。
- L22：[[03 约定与符号]] 硬件表前的“1979 TFLOPS 类数字是 2:4 结构化稀疏值”容易与同表“H100 FP8 dense ≈1979 TFLOPS”冲突；建议改成 dtype 限定示例：“H100 BF16 1979 是 sparse，BF16 dense 约 989；H100 FP8 dense 约 1979”。另建议在“其他常用量级”补录“良好调优的大而方 GEMM 可达约 80%–95% 匹配口径峰值（非 shape 无关保证）”，统一后续课程引用（2026-08-04）。
- L24：建议在 [[03 约定与符号]] 补一张数值格式速查表，统一 FP32/TF32/FP16/BF16/FP8 E4M3/E5M2/FP4 的位域与动态范围量级，并在确有课程需求时补录指定 GPU 的 FP32/TF32/FP16/INT8/INT4 dense 峰值；L24 现按 CUDA/OCP 官方资料使用量级表述，未登记峰值保持空缺（2026-08-04）。
- L27：建议在 [[03 约定与符号]] 的“其他常用量级”补录代表性 8-GPU H100 整机高度（DGX H100 为 8U）、传统与 rack-scale AI 机柜的 power density 教学量级；若后续课程需要统一电费或居民用电类比，再单独登记电价场景与户均功率/能耗口径。L27 已用 NVIDIA/DOE 一手资料核对概念，但未把未登记精确值当作课程常数（2026-08-04）。
- L29：设计稿要求估算“跑满 400G 的 TCP 要烧掉多少核”，但 [[03 约定与符号]] 未登记可跨平台复用的 CPU 频率或 host-networking cycles/Byte，且该成本强依赖 packet size、TSO/GRO、copy、NUMA、flow 数与实现。正文使用参数式 $N_{core}=Rc/f$ 并把 3 GHz、1–2 cycles/Byte 明确标成教学场景；建议 03 若后续仍需此类估算，补录参数化公式而非单一核数（2026-08-04）。
- L30：建议在 [[03 约定与符号]] 的“其他常用量级”补录 PFC headroom 的参数化口径 $H\gtrsim Rt_{react}/8+H_{margin}$，并明确 $t_{react}$ 应包含 cable、PHY/MAC 与设备反应时间；若保留 100 m 教学例，可登记光纤传播速度约 $2\times10^8$ m/s。L30 现将这些量标为教学场景，并明确不能替代 vendor 公式；未为任意 64×400G switch 编造 total-buffer 规格（2026-08-04）。
